const BOARD_SIZE = 7;
    const GRID_SIZE = BOARD_SIZE * 2 - 1; // 13 (7マス + 6壁スロット)

    // カード画像データ
    const CARD_IMAGES = {};
    CARD_IMAGES['swap'] = 'data:image/jpeg;base64,';
    CARD_IMAGES['slide'] = 'data:image/jpeg;base64,';
    CARD_IMAGES['dash'] = 'data:image/jpeg;base64,';
    CARD_IMAGES['jump'] = 'data:image/jpeg;base64,';
    CARD_IMAGES['push'] = 'data:image/jpeg;base64,';
    CARD_IMAGES['rotate'] = 'data:image/jpeg;base64,';
    CARD_IMAGES['recover'] = 'data:image/jpeg;base64,';
    CARD_IMAGES['freeze'] = 'data:image/jpeg;base64,';
    CARD_IMAGES['wallplus'] = 'data:image/jpeg;base64,';

    // ============================================================
    // === キャラクター＆カードデータ ===
    // ============================================================
    const CHARACTERS = [
      { id: 'ninja', name: 'にんじゃねこ', emoji: '🥷', subtitle: '神出鬼没の隠密猫', desc: 'トリッキーな動きで翻弄する', cards: ['ninja_shinobi', 'ninja_musasabi', 'ninja_kawarimi'] },
      { id: 'thief', name: 'どろぼうねこ', emoji: '💰', subtitle: '逃げ足の速い怪盗猫', desc: '逃げと奪取の達人', cards: ['thief_zurakaru', 'thief_kakureru', 'thief_itadaki'] },
      { id: 'sumo', name: 'すもうねこ', emoji: '🌑', subtitle: '怪力無双の横綱猫', desc: '力で盤面を支配する', cards: ['sumo_oshidashi', 'sumo_uwatenage', 'sumo_teppou'] },
      { id: 'witch', name: 'まじょねこ', emoji: '🔮', subtitle: '魔法を操る不思議猫', desc: '魔法で盤面を自在に操る', cards: ['witch_teleport', 'witch_seal', 'witch_shield'] },
      { id: 'ghost', name: 'ゆうれいねこ', emoji: '👻', subtitle: '壁を抜ける幽体猫', desc: '幽霊ならではの能力で攻める', cards: ['ghost_walk', 'ghost_kanashibari', 'ghost_hyoui'] },
      { id: 'electric', name: 'でんきねこ', emoji: '⚡', subtitle: 'シビれる稲妻猫', desc: '高速移動と感電で封じる', cards: ['electric_raiden', 'electric_shock', 'electric_magnet'] },
      { id: 'professor', name: 'はかせねこ', emoji: '🎓', subtitle: '発明好きの天才猫', desc: '発明品で戦況を変える', cards: ['prof_wallmachine', 'prof_copy', 'prof_remote'] },
      { id: 'construction', name: 'こうじねこ', emoji: '⛑️', subtitle: '現場一筋の職人猫', desc: '迷路そのものを作り替える', cards: ['construction_move', 'construction_rotate', 'construction_blast'] }
    ];

    const CHARACTER_CARDS = [
      // 🥷 にんじゃねこ
      { id: 'ninja_shinobi', icon: '🥷', name: '忍び足', desc: '斜め方向に1マス移動する（壁は越えられない）', actionType: 'move_act', character: 'ninja', limit: 1 },
      { id: 'ninja_musasabi', icon: '🦅', name: 'ムササビの術', desc: '1マス以内にいる相手を飛び越えて移動する', actionType: 'move_act', character: 'ninja', limit: 1 },
      { id: 'ninja_kawarimi', icon: '🍃', name: '変わり身の術', desc: '【カウンター】妨害を無効化して別のマスへ移動', actionType: 'counter', character: 'ninja', limit: 1 },
      // 💰 どろぼうねこ
      { id: 'thief_zurakaru', icon: '💨', name: 'ずらかる', desc: '横方向に2マス移動できる（間の壁は無視）', actionType: 'move_act', character: 'thief', limit: 1 },
      { id: 'thief_kakureru', icon: '🃏', name: '隠れる', desc: '次のターンの間、相手のカード効果を受けない', actionType: 'move_ok', character: 'thief', limit: 1 },
      { id: 'thief_itadaki', icon: '🧱', name: 'いただき', desc: '相手の持ち壁を1枚盗んで自分のものにする', actionType: 'move_ok', character: 'thief', limit: 1 },
      // 🌑 すもうねこ
      { id: 'sumo_oshidashi', icon: '✋', name: '押し出し', desc: '隣の相手を1マス後ろへ押し下げる', actionType: 'move_ok', character: 'sumo', limit: 1 },
      { id: 'sumo_uwatenage', icon: '🔄', name: '上手投げ', desc: '隣接する相手を反対側に2マス投げ飛ばす', actionType: 'turn_end', character: 'sumo', limit: 1 },
      { id: 'sumo_teppou', icon: '💥', name: '鉄砲柱', desc: '壁を1マス動かす（スライドさせる）', actionType: 'move_ok', character: 'sumo', limit: 1 },
      // 🔮 まじょねこ
      { id: 'witch_teleport', icon: '🌀', name: 'テレポート', desc: '横一列の好きなマスへ移動（壁無視・入れ替わり）', actionType: 'move_act', character: 'witch', limit: 1 },
      { id: 'witch_seal', icon: '🤐', name: '呪いの封印', desc: '相手のカード1枚を2ターン使用不可にする', actionType: 'move_ok', character: 'witch', limit: 1 },
      { id: 'witch_shield', icon: '🛡️', name: '魔法の盾', desc: '【カウンター】相手の妨害カードを1回無効化', actionType: 'counter', character: 'witch', limit: 1 },
      // 👻 ゆうれいねこ
      { id: 'ghost_walk', icon: '👻', name: 'ゴーストウォーク', desc: '壁を1枚すり抜けて移動できる', actionType: 'move_act', character: 'ghost', limit: 1 },
      { id: 'ghost_kanashibari', icon: '🥶', name: '金縛り', desc: '次の相手ターン、相手は移動できなくなる', actionType: 'turn_end', character: 'ghost', limit: 1 },
      { id: 'ghost_hyoui', icon: '👻', name: '憑依', desc: '相手を1マス自由に動かす（操る）', actionType: 'move_ok', character: 'ghost', limit: 1 },
      // ⚡ でんきねこ
      { id: 'electric_raiden', icon: '⚡', name: '電光石火', desc: '自由に2マス移動できる（壁は越えられない）', actionType: 'move_act', character: 'electric', limit: 1 },
      { id: 'electric_shock', icon: '⚡', name: '感電ショック', desc: '隣の相手の移動を2ターン封じる', actionType: 'turn_end', character: 'electric', limit: 1 },
      { id: 'electric_magnet', icon: '🧲', name: '超電導マグネット', desc: '2マス以内の相手を自分の隣に引き寄せる', actionType: 'turn_end', character: 'electric', limit: 1 },
      // 🎓 はかせねこ
      { id: 'prof_wallmachine', icon: '🧪', name: '壁増殖マシン', desc: '手持ちの壁を2枚増やす', actionType: 'move_ok', character: 'professor', limit: 1 },
      { id: 'prof_copy', icon: '📠', name: 'コピー機', desc: '相手のカードを1枚コピーして使う', actionType: 'turn_end', character: 'professor', limit: 1 },
      { id: 'prof_remote', icon: '🎮', name: 'リモート操作', desc: '2マス以内の相手を1マス自由に動かす', actionType: 'move_ok', character: 'professor', limit: 1 },
      // ⛑️ こうじねこ
      { id: 'construction_move', icon: '🚜', name: '移設工事', desc: '壁を1マス動かす（スライドさせる）', actionType: 'move_ok', character: 'construction', limit: 1 },
      { id: 'construction_rotate', icon: '🔄', name: '回転工事', desc: '壁を90度回転させる', actionType: 'move_ok', character: 'construction', limit: 1 },
      { id: 'construction_blast', icon: '🧨', name: '発破', desc: '盤上の壁を1枚選んで完全に破壊する', actionType: 'turn_end', character: 'construction', limit: 1 }
    ];

    // 後方互換
    const SKILL_CARDS = CHARACTER_CARDS;

    const AI_FACES = {
      easy: '😺',
      medium: '😼',
      hard: '😾'
    };

    // ゲームモード設定
    let gameMode = 'pvp'; // 'pvp' or 'ai'
    let aiDifficulty = 'easy'; // 'easy', 'medium', 'hard'
    let cardDistMode = 'all'; // 'all'(従来), 'choose'(選択), 'random'(ランダム)
    // 手札: null=全カード使える(従来/AI), 配列=その手札のみ
    let playerHands = { 1: null, 2: null };

    // キャラクター選択状態
    let playerCharacters = { 1: null, 2: null }; // 各プレイヤーが選んだキャラクターID

    // AI学習データ（事前学習済み - 1000戦でAIが889勝）
    // AIはプレイヤー（ちゃとら）が下の白マスに行くのを妨害する壁を学習
    const PRETRAINED_WALL_DATA = {"5,1,h":10533,"1,3,h":10357,"1,7,h":10355,"1,7,v":10351,"3,11,h":9695,"9,11,h":9620,"5,9,h":7236,"3,3,h":7233,"11,11,h":7228,"9,7,h":7198,"5,5,h":7178,"11,7,h":7162,"1,3,v":6425,"7,11,h":4848,"7,3,h":1670,"9,1,h":629,"3,9,h":562,"1,9,h":488,"5,11,h":460,"5,3,h":454,"3,7,h":450,"7,1,h":427,"11,1,h":391,"3,1,h":362,"1,5,v":317,"7,7,h":293,"9,3,h":247,"5,9,v":238,"3,5,v":236,"3,7,v":235};

    let aiLearningData = {
      goodWallPositions: { ...PRETRAINED_WALL_DATA },
      goodMovePatterns: {},
      trained: true,
      totalGames: 1000,
      wins: { 1: 111, 2: 889 }
    };

    // 自己対戦用の状態
    let trainingMode = false;
    let trainingGames = 0;
    let trainingTotal = 1000;
    let currentGameMoves = [];

    // 壁は {cornerRow, cornerCol, orientation} で管理
    // cornerRow, cornerCol はコーナー位置（奇数行・奇数列）
    // orientation: 'h' (horizontal/横) or 'v' (vertical/縦)

    // ==================== 勝ちパターンシステム ====================
    // AI (player2) は row=6 スタート → row=0 がゴール
    // Player (player1) は row=0 スタート → row=6 がゴール

    // 壁コンボ: 一緒に置くと相手に大迂回を強いる壁ペア
    const WALL_COMBOS = [
      // 中央上部封鎖: プレイヤーのゴール付近を左右から挟む
      { walls: [{r:9,c:3,o:'h'}, {r:9,c:7,o:'h'}], name: 'lower_pinch' },
      { walls: [{r:11,c:3,o:'h'}, {r:11,c:7,o:'h'}], name: 'goal_pinch' },
      // 中段横断壁: プレイヤーの中央突破を阻止
      { walls: [{r:5,c:1,o:'h'}, {r:5,c:5,o:'h'}], name: 'mid_barrier_left' },
      { walls: [{r:5,c:5,o:'h'}, {r:5,c:9,o:'h'}], name: 'mid_barrier_right' },
      // 上部封鎖: AI側ゴール付近の防衛（プレイヤーをゴールから遠ざける）
      { walls: [{r:1,c:3,o:'h'}, {r:1,c:7,o:'h'}], name: 'top_barrier' },
      { walls: [{r:3,c:3,o:'h'}, {r:3,c:7,o:'h'}], name: 'upper_barrier' },
      // L字壁: 横＋縦で直角に曲がるバリア
      { walls: [{r:5,c:1,o:'h'}, {r:3,c:1,o:'v'}], name: 'L_left' },
      { walls: [{r:5,c:9,o:'h'}, {r:3,c:9,o:'v'}], name: 'L_right' },
      // S字壁: プレイヤーを蛇行させる
      { walls: [{r:3,c:3,o:'h'}, {r:7,c:7,o:'h'}], name: 'S_curve' },
      { walls: [{r:3,c:7,o:'h'}, {r:7,c:3,o:'h'}], name: 'S_curve_rev' },
    ];

    // 勝ちパターン: 状況に応じた戦略
    // 強制パターン: 通常評価を飛ばして即決すべき局面のみ
    // 「相手があと1歩でゴール」の緊急時だけ壁で止める
    // それ以外は全てscoreMoveNPly + scoreLearnedBestWallの統合評価に任せる
    function checkWinPatterns(ai, player, aiDist, playerDist) {
      // --- 緊急防壁: 相手がゴール1歩(距離2=現在地+ゴール) ---
      // 壁を置かないと次ターンで負ける
      if (playerDist === 2 && ai.walls > 0 && aiDist > 2) {
        const bestWall = findBestWall();
        if (bestWall) return { type: 'wall', ...bestWall, pattern: 'emergency_block' };
      }

      return null; // 通常評価へ
    }

    // 壁コンボの中で最も効果的なものを探す
    function findBestWallCombo(ai, player, playerDist) {
      const playerGoal = BOARD_SIZE - 1;
      let bestAction = null;
      let bestIncrease = 0;

      for (const combo of WALL_COMBOS) {
        for (const wallDef of combo.walls) {
          const wall = { cornerRow: wallDef.r, cornerCol: wallDef.c, orientation: wallDef.o };

          // この壁が配置可能かチェック
          if (!canPlaceWall(wall)) continue;

          // 配置した場合の効果を計算
          gameState.walls.push({ ...wall, owner: 2 });
          const newPlayerPath = findShortestPath(player.row, player.col, playerGoal);
          const newAiPath = findShortestPath(ai.row, ai.col, 0);
          gameState.walls.pop();

          if (!newPlayerPath || !newAiPath) continue;

          const increase = newPlayerPath.length - playerDist;
          const aiPenalty = newAiPath.length - (findShortestPath(ai.row, ai.col, 0) || []).length;

          // コンボの片方が既に盤上にあれば追加ボーナス
          const partner = combo.walls.find(w => w !== wallDef);
          const partnerPlaced = gameState.walls.some(
            w => w.cornerRow === partner.r && w.cornerCol === partner.c && w.orientation === partner.o
          );
          const comboBonus = partnerPlaced ? increase * 2 : 0;

          const netEffect = increase - aiPenalty + comboBonus;
          if (netEffect > bestIncrease && netEffect >= 2) {
            bestIncrease = netEffect;
            bestAction = { type: 'wall', cornerRow: wall.cornerRow, cornerCol: wall.cornerCol, orientation: wall.orientation, pattern: combo.name };
          }
        }
      }

      return bestAction;
    }
    let animating = false; // カードアニメーション中のロック

    // トースト通知システム
    function showToast(message, type = 'warn', duration = 2000) {
      const container = document.getElementById('toast-container');
      const toast = document.createElement('div');
      toast.className = 'toast ' + type;
      toast.textContent = message;
      container.appendChild(toast);
      setTimeout(() => {
        if (toast.parentNode) toast.remove();
      }, duration);
    }
    
    let gameState = {
      currentPlayer: 1,
      mode: 'move',
      turn: 1, // 現在のターン数
      players: {
        1: { row: 0, col: 3, walls: 6, emoji: '🐈', color: '#ffffff', cardsUsed: {}, totalCardsUsed: 0, frozen: false, frozenTurns: 0, hidden: false, sealedCards: {} },
        2: { row: 6, col: 3, walls: 6, emoji: '🐈‍⬛', color: '#333333', cardsUsed: {}, totalCardsUsed: 0, frozen: false, frozenTurns: 0, hidden: false, sealedCards: {} }
      },
      walls: [], // [{cornerRow, cornerCol, orientation}]
      gameOver: false,
      selectedCard: null,
      cardPhase: null,
      wallOrientation: 'h', // 'h' or 'v'
      previewWall: null,
      slideTargetWall: null, // スライド対象の壁
      copyingCard: false, // 複製カード使用中フラグ
      pendingMoveOk: false // 🟢 Move OK: カード使用後の移動許可フラグ
    };

    // 履歴スタック（一手戻る用）
    let historyStack = [];

    // 壁ドラッグ用の状態
    let wallDragState = {
      isDragging: false,
      currentCorner: null
    };

    const MAX_CARDS_PER_PLAYER = 3; // 各プレイヤーが使えるカードの枚数

    // 現在の状態を履歴に保存
    function saveHistory() {
      const snapshot = JSON.stringify({
        currentPlayer: gameState.currentPlayer,
        turn: gameState.turn,
        players: gameState.players,
        walls: gameState.walls,
        gameOver: gameState.gameOver
      });
      historyStack.push(snapshot);
      // 履歴は最大20手まで
      if (historyStack.length > 20) {
        historyStack.shift();
      }
    }

    // 一手戻る
    function undoMove() {
      if (historyStack.length === 0) {
        return;
      }
      
      // AIモードでAIのターン中は戻れない
      if (gameMode === 'ai' && gameState.currentPlayer === 2) {
        return;
      }
      
      const snapshot = JSON.parse(historyStack.pop());
      gameState.currentPlayer = snapshot.currentPlayer;
      gameState.turn = snapshot.turn;
      gameState.players = snapshot.players;
      gameState.walls = snapshot.walls;
      gameState.gameOver = snapshot.gameOver;
      gameState.selectedCard = null;
      gameState.cardPhase = null;
      gameState.previewWall = null;
      gameState.mode = 'move';
      
      initBoard();
      updateUI();
    }

    // モード選択画面
    function showModeSelect() {
      document.getElementById('mode-select').classList.remove('hidden');
      document.getElementById('ai-select').classList.add('hidden');
      document.getElementById('game-container').classList.add('hidden');
      document.getElementById('card-mode-select').classList.remove('show');
      document.getElementById('card-draft').classList.remove('show');
    }

    function showAISelect() {
      document.getElementById('mode-select').classList.add('hidden');
      document.getElementById('ai-select').classList.remove('hidden');
    }

    function startGame(mode, difficulty = 'easy') {
      gameMode = mode;
      aiDifficulty = difficulty;

      // キャラクター選択画面へ
      document.getElementById('mode-select').classList.add('hidden');
      document.getElementById('ai-select').classList.add('hidden');
      playerCharacters = { 1: null, 2: null };
      showCharacterSelect(1);
    }

    // キャラクター選択画面を表示
    function showCharacterSelect(playerNum) {
      const modal = document.getElementById('card-draft-modal');
      const isAI = gameMode === 'ai';
      const name = playerNum === 1 ? 'ちゃとら 🐈' : (isAI ? 'CPU' : 'くろねこ 🐈‍⬛');
      const otherName = playerNum === 1 ? (isAI ? 'CPU' : 'くろねこ') : 'ちゃとら';
      const otherChar = playerCharacters[playerNum === 1 ? 2 : 1];

      let html = `
        <div class="handoff-screen">
          <div class="handoff-cat">${playerNum === 1 ? '🐈' : '🐈‍⬛'}</div>
          <div class="handoff-msg">${name} のキャラをえらぼう！</div>
          ${playerNum === 2 && !isAI ? `<div class="handoff-secret">🙈 ${otherName} は みないでね！</div>` : ''}
          <div class="char-select-grid">
      `;

      CHARACTERS.forEach(char => {
        const isUsed = otherChar === char.id;
        html += `
          <div class="char-select-card${isUsed ? ' disabled' : ''}" ${isUsed ? '' : `onclick="selectCharacter(${playerNum}, '${char.id}')"`}>
            <div class="char-emoji">${char.emoji}</div>
            <div class="char-name">${char.name}</div>
            <div class="char-subtitle">${char.subtitle}</div>
            <div class="char-cards-preview">
              ${char.cards.map(cid => {
                const c = CHARACTER_CARDS.find(cc => cc.id === cid);
                const typeIcon = c.actionType === 'move_act' ? '🔵' : c.actionType === 'move_ok' ? '🟢' : c.actionType === 'turn_end' ? '🔴' : '🟣';
                return `<div class="char-card-mini">${typeIcon} ${c.icon} ${c.name}</div>`;
              }).join('')}
            </div>
            ${isUsed ? '<div class="char-used">あいてが せんたくずみ</div>' : ''}
          </div>
        `;
      });

      html += `</div>`;
      html += `<button class="card-mode-back" onclick="backFromCharSelect(${playerNum})">← もどる</button>`;
      html += `</div>`;

      modal.innerHTML = html;
      document.getElementById('card-draft').classList.add('show');
    }

    function selectCharacter(playerNum, charId) {
      playerCharacters[playerNum] = charId;
      const char = CHARACTERS.find(c => c.id === charId);
      playerHands[playerNum] = [...char.cards];

      if (playerNum === 1) {
        if (gameMode === 'ai') {
          // AIにランダムでキャラを割り当て（プレイヤーと被らない）
          const available = CHARACTERS.filter(c => c.id !== charId);
          const aiChar = available[Math.floor(Math.random() * available.length)];
          playerCharacters[2] = aiChar.id;
          playerHands[2] = [...aiChar.cards];
          document.getElementById('card-draft').classList.remove('show');
          launchGame();
        } else {
          // PvP → P2のキャラ選択へ（ハンドオフ画面を経由）
          showCharSelectHandoff(2);
        }
      } else {
        // P2完了 → ゲーム開始
        document.getElementById('card-draft').classList.remove('show');
        launchGame();
      }
    }

    function showCharSelectHandoff(playerNum) {
      const modal = document.getElementById('card-draft-modal');
      const name = playerNum === 1 ? 'ちゃとら 🐈' : 'くろねこ 🐈‍⬛';
      const otherName = playerNum === 1 ? 'くろねこ' : 'ちゃとら';
      modal.innerHTML = `
        <div class="handoff-screen">
          <div class="handoff-cat">${playerNum === 1 ? '🐈' : '🐈‍⬛'}</div>
          <div class="handoff-msg">${name} がキャラを えらぶばん！</div>
          <div class="handoff-secret">🙈 ${otherName} は みないでね！</div>
          <button class="handoff-btn" onclick="showCharacterSelect(${playerNum})">じゅんび OK！</button>
        </div>
      `;
    }

    function backFromCharSelect(playerNum) {
      document.getElementById('card-draft').classList.remove('show');
      if (playerNum === 1) {
        document.getElementById('mode-select').classList.remove('hidden');
      } else {
        showCharacterSelect(1);
      }
    }

    function launchGame() {
      document.getElementById('mode-select').classList.add('hidden');
      document.getElementById('ai-select').classList.add('hidden');
      document.getElementById('card-mode-select').classList.remove('show');
      document.getElementById('card-draft').classList.remove('show');
      document.getElementById('game-container').classList.remove('hidden');

      // プレイヤー名にキャラ情報を表示
      const char1 = CHARACTERS.find(c => c.id === playerCharacters[1]);
      const char2 = CHARACTERS.find(c => c.id === playerCharacters[2]);

      if (gameMode === 'ai') {
        document.getElementById('game-container').classList.add('ai-mode');
        let aiName = aiDifficulty === 'easy' ? 'こねこ' : aiDifficulty === 'medium' ? 'おとなねこ' : 'ボスねこ';
        if (aiDifficulty === 'hard' && aiLearningData.trained) {
          aiName = 'ボスねこ+';
        }
        document.querySelector('#player1-info .player-name').textContent =
          `${char1 ? char1.emoji : '🐈'} ${char1 ? char1.name : 'ちゃとら'} ⬇️`;
        document.querySelector('#player2-info .player-name').textContent =
          `⬆️ ${aiName} ${char2 ? char2.emoji : AI_FACES[aiDifficulty]}`;
      } else {
        document.getElementById('game-container').classList.remove('ai-mode');
        document.querySelector('#player1-info .player-name').textContent =
          `${char1 ? char1.emoji : '🐈'} ${char1 ? char1.name : 'ちゃとら'} ⬇️`;
        document.querySelector('#player2-info .player-name').textContent =
          `⬆️ ${char2 ? char2.name : 'くろねこ'} ${char2 ? char2.emoji : '🐈‍⬛'}`;
      }

      resetGame();
    }

    // AI思考表示
    // ============================================================
    // === カード配布システム（二人対戦用） ===
    // ============================================================

    function backFromCardMode() {
      document.getElementById('card-mode-select').classList.remove('show');
      document.getElementById('mode-select').classList.remove('hidden');
    }

    function startCardDraft(mode) {
      cardDistMode = mode;
      document.getElementById('card-mode-select').classList.remove('show');

      if (mode === 'random') {
        // ランダム配布
        const allIds = SKILL_CARDS.map(c => c.id);
        playerHands[1] = shuffleArray(allIds).slice(0, 3);
        playerHands[2] = shuffleArray(allIds).slice(0, 3);
        // P1に手札を見せる → P2に手札を見せる → ゲーム開始
        showRandomResult(1);
      } else {
        // 選んで戦う → P1の手渡し画面
        playerHands = { 1: [], 2: [] };
        showHandoff(1);
      }
    }

    function shuffleArray(arr) {
      const a = [...arr];
      for (let i = a.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [a[i], a[j]] = [a[j], a[i]];
      }
      return a;
    }

    // === 手渡し画面: 「相手は見ないで！」 ===
    function showHandoff(playerNum) {
      const modal = document.getElementById('card-draft-modal');
      const name = playerNum === 1 ? 'ちゃとら 🐈' : 'くろねこ 🐈‍⬛';
      const otherName = playerNum === 1 ? 'くろねこ' : 'ちゃとら';
      modal.innerHTML = `
        <div class="handoff-screen">
          <div class="handoff-cat">${playerNum === 1 ? '🐈' : '🐈‍⬛'}</div>
          <div class="handoff-msg">${name} がカードを えらぶばん！</div>
          <div class="handoff-secret">🙈 ${otherName} は みないでね！</div>
          <button class="handoff-btn" onclick="showDraftGrid(${playerNum})">じゅんび OK！</button>
        </div>
      `;
      document.getElementById('card-draft').classList.add('show');
    }

    // === ドラフト選択画面: 9枚から3枚選ぶ ===
    let draftSelected = [];
    let draftPlayerNum = 0;

    function showDraftGrid(playerNum) {
      draftSelected = [];
      draftPlayerNum = playerNum;
      renderDraftGrid();
    }

    function renderDraftGrid() {
      const modal = document.getElementById('card-draft-modal');
      const name = draftPlayerNum === 1 ? 'ちゃとら 🐈' : 'くろねこ 🐈‍⬛';
      const count = draftSelected.length;

      let html = `
        <h2>${name} のカードを えらぼう</h2>
        <div class="draft-warning">🙈 あいては みないでね！</div>
        <div class="draft-count">えらんだ数: <strong>${count}</strong> / 3</div>
        <div class="draft-grid">
      `;

      SKILL_CARDS.forEach((card, idx) => {
        const isSelected = draftSelected.includes(card.id);
        html += `
          <div class="draft-card${isSelected ? ' selected' : ''}" onclick="toggleDraftCard('${card.id}')">
            <div class="draft-num">${idx + 1}番</div>
            <div class="draft-icon">${card.icon}</div>
            <div class="draft-name">${card.name}</div>
            <div class="draft-desc">${card.desc}</div>
          </div>
        `;
      });

      html += `</div>`;
      html += `<button class="draft-confirm-btn${count === 3 ? ' ready' : ''}" onclick="confirmDraft()">これで けってい！</button>`;
      modal.innerHTML = html;
    }

    function toggleDraftCard(cardId) {
      const idx = draftSelected.indexOf(cardId);
      if (idx >= 0) {
        draftSelected.splice(idx, 1);
      } else if (draftSelected.length < 3) {
        draftSelected.push(cardId);
      }
      renderDraftGrid();
    }

    function confirmDraft() {
      if (draftSelected.length !== 3) return;
      playerHands[draftPlayerNum] = [...draftSelected];

      if (draftPlayerNum === 1) {
        // P1完了 → P2の手渡し画面へ
        showHandoff(2);
      } else {
        // P2完了 → ゲーム開始
        document.getElementById('card-draft').classList.remove('show');
        launchGame();
      }
    }

    // === ランダム配布結果表示 ===
    function showRandomResult(playerNum) {
      const modal = document.getElementById('card-draft-modal');
      const name = playerNum === 1 ? 'ちゃとら 🐈' : 'くろねこ 🐈‍⬛';
      const otherName = playerNum === 1 ? 'くろねこ' : 'ちゃとら';
      const hand = playerHands[playerNum];

      let cardsHtml = '<div class="random-cards-display">';
      hand.forEach((cardId, i) => {
        const card = SKILL_CARDS.find(c => c.id === cardId);
        cardsHtml += `
          <div class="random-card-item">
            <div class="draft-num">${i + 1}番</div>
            <div class="draft-icon">${card.icon}</div>
            <div class="draft-name">${card.name}</div>
          </div>
        `;
      });
      cardsHtml += '</div>';

      modal.innerHTML = `
        <div class="handoff-screen">
          <div class="handoff-cat">${playerNum === 1 ? '🐈' : '🐈‍⬛'}</div>
          <div class="handoff-msg">${name} のカード</div>
          <div class="handoff-secret">🙈 ${otherName} は みないでね！</div>
          ${cardsHtml}
          <button class="handoff-btn" onclick="nextRandomResult(${playerNum})">OK！</button>
        </div>
      `;
      document.getElementById('card-draft').classList.add('show');
    }

    function nextRandomResult(playerNum) {
      if (playerNum === 1) {
        showRandomResult(2);
      } else {
        document.getElementById('card-draft').classList.remove('show');
        launchGame();
      }
    }

    function showAIThinking() {
      const overlay = document.getElementById('ai-thinking');
      document.getElementById('ai-thinking-cat').textContent = AI_FACES[aiDifficulty];
      overlay.classList.remove('hidden');
    }

    function hideAIThinking() {
      document.getElementById('ai-thinking').classList.add('hidden');
    }

    // AI行動ロジック
    function aiTurn() {
      if (gameState.gameOver || gameState.currentPlayer !== 2) return;
      
      showAIThinking();
      
      const thinkTime = aiDifficulty === 'easy' ? 800 : aiDifficulty === 'medium' ? 1200 : 1500;
      
      setTimeout(() => {
        hideAIThinking();
        
        let action;
        switch (aiDifficulty) {
          case 'easy':
            action = aiEasyAction();
            break;
          case 'medium':
            action = aiMediumAction();
            break;
          case 'hard':
            action = aiLearningData.trained ? aiLearnedAction() : aiHardAction();
            break;
        }
        
        executeAIAction(action);
      }, thinkTime);
    }

    // 初級AI: ゴール方向に進む + たまに壁を置く（ミスあり）
    function aiEasyAction() {
      const ai = gameState.players[2];
      const aiPath = findShortestPath(ai.row, ai.col, 0);

      // 20%の確率でランダムな方向に移動（ミス）
      if (Math.random() < 0.2) {
        const moves = getValidMoves(2);
        if (moves.length > 0) {
          return { type: 'move', ...moves[Math.floor(Math.random() * moves.length)] };
        }
      }

      // 15%の確率で壁を置く（プリトレインデータから）
      if (ai.walls > 0 && Math.random() < 0.15) {
        const goodWalls = getLearnedWallPositions();
        if (goodWalls.length > 0 && goodWalls[0].score > 0) {
          const topWalls = goodWalls.slice(0, Math.min(5, goodWalls.length));
          return { type: 'wall', ...topWalls[Math.floor(Math.random() * topWalls.length)] };
        }
      }

      // 最短経路に沿って移動
      if (aiPath && aiPath.length > 1) {
        const nextStep = aiPath[1];
        const moves = getValidMoves(2);
        const bestMove = moves.find(m => m.row === nextStep.row && m.col === nextStep.col);
        if (bestMove) return { type: 'move', ...bestMove };
      }

      const moves = getValidMoves(2);
      const upMoves = moves.filter(m => m.row < ai.row);
      if (upMoves.length > 0) {
        return { type: 'move', ...upMoves[Math.floor(Math.random() * upMoves.length)] };
      }
      return moves.length > 0 ? { type: 'move', ...moves[Math.floor(Math.random() * moves.length)] } : null;
    }

    // 中級AI: 最短経路 + スコアリング壁配置 + 相手考慮移動（カードなし）
    function aiMediumAction() {
      const ai = gameState.players[2];
      const player = gameState.players[1];

      const aiPath = findShortestPath(ai.row, ai.col, 0);
      const playerPath = findShortestPath(player.row, player.col, BOARD_SIZE - 1);
      const aiDist = aiPath ? aiPath.length : 999;
      const playerDist = playerPath ? playerPath.length : 999;

      // ゴールに1歩なら即移動
      if (aiDist === 2 && aiPath) {
        const nextStep = aiPath[1];
        const moves = getValidMoves(2);
        const goal = moves.find(m => m.row === nextStep.row && m.col === nextStep.col);
        if (goal) return { type: 'move', ...goal };
      }

      // 壁配置の戦略判断
      if (ai.walls > 0) {
        const shouldWall = decideWallOrMove(aiDist, playerDist, ai.walls);
        if (shouldWall) {
          const bestWall = findBestWall();
          if (bestWall) return { type: 'wall', ...bestWall };
        }
      }

      // 最善移動（相手の経路も考慮）
      return aiBestMove(ai, aiPath);
    }

    // ============================================================
    // === ユーティリティ: レース勝敗判定・序盤判定・中央スコア ===
    // ============================================================

    // 正確なレース勝敗判定（手番考慮）
    // AI(player2)の手番かどうかを含めて、壁なし純粋レースの勝者を計算
    // 戻り値: 正=AI有利の差分ターン数, 負=相手有利, 0=互角
    function calcRaceAdvantage(aiDist, playerDist) {
      // findShortestPath の length はスタート含む → 残り移動数 = length - 1
      const aiMoves = aiDist - 1;       // AIがゴールまでに必要な移動回数
      const playerMoves = playerDist - 1; // 相手がゴールまでに必要な移動回数
      // AIは後手(player2) → 同ターン数なら相手が先にゴール
      // AI有利 = 相手の必要ターン - AIの必要ターン（正なら有利）
      return playerMoves - aiMoves;
    }

    // 序盤判定（最初3ターンは壁を温存して直進）
    function isOpeningPhase() {
      return gameState.turn <= 6; // 各プレイヤー3手ずつ = ターン6まで
    }

    // 中央スコア: 盤面中央に近いほどボーナス（壁で塞がれにくい）
    function centerScore(row, col) {
      const center = (BOARD_SIZE - 1) / 2; // 3.0 for 7x7
      const distFromCenter = Math.abs(col - center);
      return (center - distFromCenter) * 2; // 中央=6, 端=0
    }

    // 相手の壁残数による攻守バランス係数
    // 壁が多い相手 → 守備重視（堅牢な経路）、壁が少ない相手 → 攻撃重視（直進）
    function getDefenseWeight(playerWalls) {
      if (playerWalls === 0) return 0;   // 攻撃100%
      if (playerWalls <= 2) return 0.3;  // やや攻撃寄り
      if (playerWalls <= 4) return 0.6;  // バランス
      return 1.0;                         // 守備重視
    }

    // ============================================================
    // === ボスねこ（上級AI） ===
    // ============================================================
    function aiHardAction() {
      const ai = gameState.players[2];
      const player = gameState.players[1];

      const aiPath = findShortestPath(ai.row, ai.col, 0);
      const playerPath = findShortestPath(player.row, player.col, BOARD_SIZE - 1);
      const aiDist = aiPath ? aiPath.length : 999;
      const playerDist = playerPath ? playerPath.length : 999;

      // ===== 優先度1: ゴール1歩 → 即移動（勝ち確定） =====
      if (aiDist === 2 && aiPath) {
        const nextStep = aiPath[1];
        const moves = getValidMoves(2);
        const goal = moves.find(m => m.row === nextStep.row && m.col === nextStep.col);
        if (goal) return { type: 'move', ...goal };
      }

      // ===== 優先度2: カードで即ゴール or 決定的前進（勝負を決めるチャンス） =====
      const winCard = findWinningCard(ai, player, aiDist, playerDist);
      if (winCard) return winCard;

      // ===== 優先度3: 相手がゴール3マス以内 → 必ず壁で妨害 =====
      // playerDist: 2=1歩, 3=2歩, 4=3歩
      if (playerDist <= 4 && ai.walls > 0) {
        const bestWall = findBestWall();
        if (bestWall) return { type: 'wall', ...bestWall };
      }

      // ===== 優先度4: 負けそう → 壁 or カードで妨害 =====
      if (playerDist < aiDist) {
        // 壁で相手の経路を伸ばす（持続効果あり）
        if (ai.walls > 0) {
          const wallResult = scoreLearnedBestWall(ai, player, aiDist, playerDist);
          if (wallResult && wallResult.score > 0) {
            return { type: 'wall', ...wallResult.wall };
          }
        }
        // カードで妨害（swap/push）
        if (ai.totalCardsUsed < MAX_CARDS_PER_PLAYER) {
          const interferCard = findInterferenceCard(ai, player, aiDist, playerDist);
          if (interferCard) return interferCard;
        }
      }

      // ===== 優先度5: 大前提 — 最短経路を歩く（カードなし） =====
      return walkShortestPath(ai, aiPath, aiDist);
    }

    // カードでゴール到達 or 決定的前進が可能かチェック
    function findWinningCard(ai, player, aiDist, playerDist) {
      if (ai.totalCardsUsed >= MAX_CARDS_PER_PLAYER) return null;
      const usedCard = (id) => (ai.cardsUsed[id] || 0) >= 1;

      // dash: ゴール到達 or ゴール1歩に
      if (!usedCard('dash') && hasDashTargetFor(2)) {
        const dashTarget = getBestDashTarget(2);
        if (dashTarget) {
          const dashDist = (findShortestPath(dashTarget.row, dashTarget.col, 0) || []).length || 999;
          if (dashDist <= 2) return { type: 'card', cardId: 'dash' };
        }
      }

      // swap: 入れ替えでゴール到達 or ゴール1歩に
      if (!usedCard('swap') && gameState.turn >= 3) {
        const swapDist = Math.abs(ai.row - player.row) + Math.abs(ai.col - player.col);
        if (swapDist <= 2) {
          const swappedAiDist = (findShortestPath(player.row, player.col, 0) || []).length || 999;
          if (swappedAiDist <= 2) return { type: 'card', cardId: 'swap' };
        }
      }

      // jump: 飛び越えでゴール到達 or ゴール1歩に
      if (!usedCard('jump') && isAdjacentFor(2) && canJumpOverFor(2)) {
        const jumpTarget = getJumpTarget(2);
        if (jumpTarget && jumpTarget.row >= 0 && jumpTarget.row < BOARD_SIZE) {
          const jumpDist = (findShortestPath(jumpTarget.row, jumpTarget.col, 0) || []).length || 999;
          if (jumpDist <= 2) return { type: 'card', cardId: 'jump' };
        }
      }

      // push: 相手の位置に入ってゴール到達 or ゴール1歩に
      if (!usedCard('push') && isAdjacentFor(2) && canPushFor(2)) {
        const dr = player.row - ai.row, dc = player.col - ai.col;
        const pushDest = { row: player.row + dr, col: player.col + dc };
        if (pushDest.row >= 0 && pushDest.row < BOARD_SIZE && pushDest.col >= 0 && pushDest.col < BOARD_SIZE) {
          const aiNewDist = (findShortestPath(player.row, player.col, 0) || []).length || 999;
          if (aiNewDist <= 2) return { type: 'card', cardId: 'push' };
        }
      }

      return null;
    }

    // 妨害カード: 負けそうなとき相手を邪魔する
    function findInterferenceCard(ai, player, aiDist, playerDist) {
      if (ai.totalCardsUsed >= MAX_CARDS_PER_PLAYER) return null;
      const usedCard = (id) => (ai.cardsUsed[id] || 0) >= 1;

      // push: 相手をゴールから遠ざける
      if (!usedCard('push') && isAdjacentFor(2) && canPushFor(2)) {
        const dr = player.row - ai.row, dc = player.col - ai.col;
        const pushDest = { row: player.row + dr, col: player.col + dc };
        if (pushDest.row >= 0 && pushDest.row < BOARD_SIZE && pushDest.col >= 0 && pushDest.col < BOARD_SIZE) {
          const pushPlayerDist = (findShortestPath(pushDest.row, pushDest.col, BOARD_SIZE - 1) || []).length || 999;
          if (pushPlayerDist > playerDist) return { type: 'card', cardId: 'push' };
        }
      }

      // swap: 位置入れ替えで有利に
      if (!usedCard('swap') && gameState.turn >= 3) {
        const swapDist = Math.abs(ai.row - player.row) + Math.abs(ai.col - player.col);
        if (swapDist <= 2) {
          const swappedAiDist = (findShortestPath(player.row, player.col, 0) || []).length || 999;
          const swappedPlayerDist = (findShortestPath(ai.row, ai.col, BOARD_SIZE - 1) || []).length || 999;
          const myGain = aiDist - swappedAiDist;
          const opLoss = swappedPlayerDist - playerDist;
          if (myGain + opLoss >= 2) return { type: 'card', cardId: 'swap' };
        }
      }

      return null;
    }

    // 最短経路に沿って歩く（大前提の行動）
    function walkShortestPath(ai, aiPath, aiDist) {
      if (aiPath && aiPath.length > 1) {
        const nextStep = aiPath[1];
        const moves = getValidMoves(2);
        const pathMove = moves.find(m => m.row === nextStep.row && m.col === nextStep.col);
        if (pathMove) return { type: 'move', ...pathMove };
      }
      // 最短経路が通れない場合（相手が邪魔など）→ 最善前進手
      return selectBestAdvance(ai, gameState.players[1], aiPath, aiDist, 2);
    }

    // 最善前進手を選ぶ（経路堅牢性 + 中央制御込み）
    function selectBestAdvance(ai, player, aiPath, aiDist, depth) {
      const moves = getValidMoves(2);
      let bestAction = null;
      let bestScore = -Infinity;
      const defWeight = getDefenseWeight(player.walls);

      for (const m of moves) {
        let score = scoreMoveNPly(ai, player, m, depth);

        if (score > -5000) {
          // 経路堅牢性（相手の壁が多いほど重要）
          if (defWeight > 0) {
            const robustness = calcPathRobustness(m.row, m.col, 0);
            score += robustness * (4 + defWeight * 6);
          }

          // 中央制御（中央寄りの経路は壁で潰されにくい）
          score += centerScore(m.row, m.col) * defWeight;
        }

        if (score > bestScore) {
          bestScore = score;
          bestAction = { type: 'move', ...m, _score: score };
        }
      }
      return bestAction || aiBestMove(ai, aiPath);
    }

    // 経路堅牢性: この位置からゴール方向に進める方向がいくつあるか
    function calcPathRobustness(row, col, goalRow) {
      const bestPath = findShortestPath(row, col, goalRow);
      if (!bestPath || bestPath.length <= 2) return 3;

      const bestDist = bestPath.length;
      const directions = [[-1, 0], [1, 0], [0, -1], [0, 1]];
      let goodDirs = 0;

      for (const [dr, dc] of directions) {
        const nr = row + dr, nc = col + dc;
        if (nr >= 0 && nr < BOARD_SIZE && nc >= 0 && nc < BOARD_SIZE) {
          if (!isWallBetween(row, col, nr, nc)) {
            const altPath = findShortestPath(nr, nc, goalRow);
            if (altPath && altPath.length <= bestDist) {
              goodDirs++;
            }
          }
        }
      }
      return goodDirs;
    }

    // 壁を置くか移動するかの戦略判断（難易度別）
    function decideWallOrMove(aiDist, playerDist, wallsLeft) {
      if (wallsLeft <= 0) return false;

      if (aiDifficulty === 'medium') {
        // 中級: 相手がリードしている場合のみ壁（やや控えめ）
        if (playerDist <= 3) return true;
        if (playerDist < aiDist) return Math.random() < 0.6;
        if (playerDist === aiDist) return Math.random() < 0.3;
        return false;
      }

      // 上級・ボスねこ+: 確定的・戦略的に判断
      // 相手がゴール目前 → 確実に壁で妨害
      if (playerDist <= 3) return true;
      // 相手がリード → 壁配置
      if (playerDist < aiDist) return true;
      // 互角で壁に余裕がある → 壁配置
      if (playerDist === aiDist && wallsLeft >= 2) return true;
      // 自分が大きくリード → 移動優先
      if (aiDist < playerDist - 2) return false;
      // やや自分がリード → 壁に余裕があれば妨害
      return wallsLeft >= 3;
    }

    // AIの最善移動を選ぶ（難易度別評価）
    function aiBestMove(ai, aiPath) {
      const moves = getValidMoves(2);
      if (moves.length === 0) return null;

      const isHard = aiDifficulty === 'hard';
      const player = gameState.players[1];
      const playerGoal = BOARD_SIZE - 1;

      let bestMove = null;
      let bestScore = -Infinity;

      for (const m of moves) {
        const savedRow = ai.row, savedCol = ai.col;
        ai.row = m.row; ai.col = m.col;

        const myPath = findShortestPath(m.row, m.col, 0);
        const myDist = myPath ? myPath.length : 999;

        let score = -myDist * 10;

        // ゴール到達は最優先
        if (m.row === 0) score += 1000;

        // 中級以上: 相手の経路への影響も考慮
        if (aiDifficulty !== 'easy') {
          const opPath = findShortestPath(player.row, player.col, playerGoal);
          const opDist = opPath ? opPath.length : 999;
          score += opDist * 3;
        }

        // 上級・ボスねこ+: 1手先読み（相手の最善応答後の評価）
        if (isHard) {
          const opMoves = getValidMoves(1);
          let bestOpDist = Infinity;
          for (const om of opMoves) {
            const savedOpRow = player.row, savedOpCol = player.col;
            player.row = om.row; player.col = om.col;
            const opNextPath = findShortestPath(om.row, om.col, playerGoal);
            player.row = savedOpRow; player.col = savedOpCol;
            const d = opNextPath ? opNextPath.length : 999;
            if (d < bestOpDist) bestOpDist = d;
          }
          // 相手が最善手を打った後でもこちらが有利ならボーナス
          if (myDist <= bestOpDist) score += 8;
          else score -= 3;
        }

        ai.row = savedRow; ai.col = savedCol;

        if (score > bestScore) {
          bestScore = score;
          bestMove = m;
        }
      }

      return bestMove ? { type: 'move', ...bestMove } : { type: 'move', ...moves[0] };
    }

    // 学習済みAI（トレーニング後のボスねこ+）— 最強
    // 全アクション（移動・壁・カード）を統一スコアで比較し最善手を選ぶ
    // ============================================================
    // === ボスねこ+（最強AI）: MCTS + α-β + 予測壁 + 壁応手 ===
    // ============================================================
    function aiLearnedAction() {
      const ai = gameState.players[2];
      const player = gameState.players[1];

      const aiPath = findShortestPath(ai.row, ai.col, 0);
      const playerPath = findShortestPath(player.row, player.col, BOARD_SIZE - 1);
      const aiDist = aiPath ? aiPath.length : 999;
      const playerDist = playerPath ? playerPath.length : 999;

      // ゴールに1歩なら即移動
      if (aiDist === 2 && aiPath) {
        const nextStep = aiPath[1];
        const moves = getValidMoves(2);
        const goal = moves.find(m => m.row === nextStep.row && m.col === nextStep.col);
        if (goal) return { type: 'move', ...goal };
      }

      // 緊急防壁
      const patternAction = checkWinPatterns(ai, player, aiDist, playerDist);
      if (patternAction) return patternAction;

      // 序盤定石: 最初3手は最短経路直進
      if (isOpeningPhase() && aiPath && aiPath.length > 1) {
        const nextStep = aiPath[1];
        const moves = getValidMoves(2);
        const pathMove = moves.find(m => m.row === nextStep.row && m.col === nextStep.col);
        if (pathMove) return { type: 'move', ...pathMove };
      }

      const raceAdv = calcRaceAdvantage(aiDist, playerDist);

      // 純粋レース: 相手が何も妨害できない
      if (player.walls === 0 && player.totalCardsUsed >= MAX_CARDS_PER_PLAYER) {
        if (aiPath && aiPath.length > 1) {
          const nextStep = aiPath[1];
          const moves = getValidMoves(2);
          const pathMove = moves.find(m => m.row === nextStep.row && m.col === nextStep.col);
          if (pathMove) return { type: 'move', ...pathMove };
        }
      }

      // 相手壁0 → 全力前進
      if (player.walls === 0) {
        return selectBestAdvance(ai, player, aiPath, aiDist, 3);
      }

      // レースで大幅リード → 前進優先
      if (raceAdv >= 3) {
        return selectBestAdvance(ai, player, aiPath, aiDist, 3);
      }

      // === MCTS + α-β統合評価 ===
      // 全候補アクション（移動・壁・カード）をMCTSで評価
      const allActions = [];

      // 移動候補
      const moves = getValidMoves(2);
      for (const m of moves) {
        let score = scoreMoveNPly(ai, player, m, 3);
        const defWeight = getDefenseWeight(player.walls);
        if (score > -5000 && defWeight > 0) {
          score += calcPathRobustness(m.row, m.col, 0) * (4 + defWeight * 6);
          score += centerScore(m.row, m.col) * defWeight;
        }
        allActions.push({ action: { type: 'move', ...m }, score, kind: 'move' });
      }

      // 壁候補（予測壁込み）
      if (ai.walls > 0 && raceAdv < 2) {
        const wallResult = scorePredictiveWall(ai, player, aiDist, playerDist);
        if (wallResult && wallResult.score > 0) {
          allActions.push({ action: { type: 'wall', ...wallResult.wall }, score: wallResult.score + 100, kind: 'wall' });
        }
      }

      // カード候補
      const cardResult = scoreCardActions(ai, player, aiDist, playerDist);
      if (cardResult) {
        allActions.push({ action: cardResult.action, score: cardResult.score, kind: 'card' });
      }

      // MCTSで上位3候補をシミュレーション
      allActions.sort((a, b) => b.score - a.score);
      const topCandidates = allActions.slice(0, Math.min(3, allActions.length));

      if (topCandidates.length > 0) {
        let bestAction = topCandidates[0].action;
        let bestWinRate = -1;

        for (const cand of topCandidates) {
          const winRate = mctsSimulate(ai, player, cand.action, 60);
          if (winRate > bestWinRate) {
            bestWinRate = winRate;
            bestAction = cand.action;
          }
        }
        return bestAction;
      }

      return selectBestAdvance(ai, player, aiPath, aiDist, 3);
    }

    // ============================================================
    // === MCTS: モンテカルロ木探索 ===
    // ============================================================
    function mctsSimulate(ai, player, firstAction, numSims) {
      let wins = 0;
      const savedState = {
        aiRow: ai.row, aiCol: ai.col, aiWalls: ai.walls,
        playerRow: player.row, playerCol: player.col, playerWalls: player.walls,
        wallsSnapshot: gameState.walls.map(w => ({ ...w })),
        turn: gameState.turn
      };

      for (let i = 0; i < numSims; i++) {
        // 状態復元
        ai.row = savedState.aiRow; ai.col = savedState.aiCol; ai.walls = savedState.aiWalls;
        player.row = savedState.playerRow; player.col = savedState.playerCol; player.walls = savedState.playerWalls;
        gameState.walls = savedState.wallsSnapshot.map(w => ({ ...w }));
        gameState.turn = savedState.turn;

        // 最初の手を適用
        applyAction(firstAction, ai, player, 2);

        // 残りをランダムプレイアウト（最大20ターン）
        let currentPlayer = 1; // 次は相手の番
        for (let t = 0; t < 20; t++) {
          const cp = currentPlayer === 1 ? player : ai;
          const op = currentPlayer === 1 ? ai : player;
          const goalRow = currentPlayer === 1 ? BOARD_SIZE - 1 : 0;

          // ゴール判定
          if (cp.row === goalRow) break;

          // ランダムだが最短経路を70%の確率で選ぶ（賢いプレイアウト）
          const cpPath = findShortestPath(cp.row, cp.col, goalRow);
          const cpMoves = getValidMoves(currentPlayer);

          if (cpMoves.length > 0) {
            let chosen;
            if (cpPath && cpPath.length > 1 && Math.random() < 0.7) {
              const nextStep = cpPath[1];
              chosen = cpMoves.find(m => m.row === nextStep.row && m.col === nextStep.col);
            }
            if (!chosen) {
              chosen = cpMoves[Math.floor(Math.random() * cpMoves.length)];
            }
            cp.row = chosen.row;
            cp.col = chosen.col;
          }

          currentPlayer = currentPlayer === 1 ? 2 : 1;
        }

        // 勝敗判定
        const aiDistFinal = (findShortestPath(ai.row, ai.col, 0) || []).length || 999;
        const playerDistFinal = (findShortestPath(player.row, player.col, BOARD_SIZE - 1) || []).length || 999;
        if (ai.row === 0 || aiDistFinal < playerDistFinal) wins++;
        else if (aiDistFinal === playerDistFinal) wins += 0.5;
      }

      // 状態復元
      ai.row = savedState.aiRow; ai.col = savedState.aiCol; ai.walls = savedState.aiWalls;
      player.row = savedState.playerRow; player.col = savedState.playerCol; player.walls = savedState.playerWalls;
      gameState.walls = savedState.wallsSnapshot.map(w => ({ ...w }));
      gameState.turn = savedState.turn;

      return wins / numSims;
    }

    // MCTSヘルパー: アクションを適用
    function applyAction(action, ai, player, playerNum) {
      if (action.type === 'move') {
        if (playerNum === 2) { ai.row = action.row; ai.col = action.col; }
        else { player.row = action.row; player.col = action.col; }
      } else if (action.type === 'wall' && action.cornerRow !== undefined) {
        gameState.walls.push({ cornerRow: action.cornerRow, cornerCol: action.cornerCol, orientation: action.orientation, owner: playerNum });
        if (playerNum === 2) ai.walls--; else player.walls--;
      }
    }

    // ============================================================
    // === 予測壁: 相手の迂回先を先回りして塞ぐ ===
    // ============================================================
    function scorePredictiveWall(ai, player, aiDist, playerDist) {
      const walls = getValidWallPlacements();
      if (walls.length === 0) return null;

      const playerGoal = BOARD_SIZE - 1;
      const currentPlayerPath = findShortestPath(player.row, player.col, playerGoal);
      if (!currentPlayerPath) return null;

      // 移動の機会コスト
      const bestMoveScore = (() => {
        const moves = getValidMoves(2);
        let best = -Infinity;
        for (const m of moves) {
          if (m.row === 0) return 10000;
          const d = (findShortestPath(m.row, m.col, 0) || []).length || 999;
          const gain = aiDist - d;
          const s = gain * 40 + (BOARD_SIZE - d) * 5;
          if (s > best) best = s;
        }
        return best;
      })();

      if (bestMoveScore >= 10000) return null;

      // 相手の現在経路エッジセット
      const playerEdgeSet = new Set();
      for (let i = 0; i < currentPlayerPath.length - 1; i++) {
        const from = currentPlayerPath[i], to = currentPlayerPath[i + 1];
        playerEdgeSet.add(`${from.row * 2 + (to.row - from.row)},${from.col * 2 + (to.col - from.col)}`);
      }

      let bestWall = null;
      let bestScore = -Infinity;

      // 1次評価
      const candidates = [];
      for (const wall of walls) {
        gameState.walls.push({ ...wall, owner: 2 });
        const newPlayerPath = findShortestPath(player.row, player.col, playerGoal);
        const newAiPath = findShortestPath(ai.row, ai.col, 0);
        gameState.walls.pop();

        if (!newPlayerPath || !newAiPath) continue;

        const playerIncrease = newPlayerPath.length - playerDist;
        const aiIncrease = newAiPath.length - aiDist;
        if (playerIncrease <= 0) continue;

        let isDirectHit = false;
        if (playerEdgeSet.size > 0) {
          const edges = getBlockedEdges(wall);
          for (const e of edges) {
            if (playerEdgeSet.has(`${e.row},${e.col}`)) { isDirectHit = true; break; }
          }
        }

        const delayValue = playerIncrease * (isDirectHit ? 15 : 8);
        const selfPenalty = aiIncrease * 10;
        let score = delayValue - selfPenalty - bestMoveScore;

        // 予測壁ボーナス: 壁Aを置いた後の相手の迂回経路に対して壁Bが刺さるか
        if (ai.walls >= 2) {
          gameState.walls.push({ ...wall, owner: 2 });
          const reroutedPath = findShortestPath(player.row, player.col, playerGoal);

          if (reroutedPath) {
            // 迂回経路のエッジを取得
            const rerouteEdges = new Set();
            for (let i = 0; i < reroutedPath.length - 1; i++) {
              const from = reroutedPath[i], to = reroutedPath[i + 1];
              rerouteEdges.add(`${from.row * 2 + (to.row - from.row)},${from.col * 2 + (to.col - from.col)}`);
            }

            // 迂回経路を塞げる壁Bがあるか探す
            const walls2 = getValidWallPlacements();
            let bestComboExtra = 0;
            for (const w2 of walls2) {
              const edges2 = getBlockedEdges(w2);
              let hitsReroute = false;
              for (const e of edges2) {
                if (rerouteEdges.has(`${e.row},${e.col}`)) { hitsReroute = true; break; }
              }
              if (!hitsReroute) continue;

              gameState.walls.push({ ...w2, owner: 2 });
              const comboPath = findShortestPath(player.row, player.col, playerGoal);
              const comboAi = findShortestPath(ai.row, ai.col, 0);
              gameState.walls.pop();
              if (!comboPath || !comboAi) continue;

              const totalIncrease = comboPath.length - playerDist;
              const extra = totalIncrease - playerIncrease;
              if (extra > bestComboExtra) bestComboExtra = extra;
            }
            // 予測壁ボーナス: 迂回先をさらに+N歩伸ばせる
            if (bestComboExtra >= 2) score += bestComboExtra * 12;
          }
          gameState.walls.pop();
        }

        // 壁の応手: 相手がこの壁に対して壁を置き返してきた場合の影響
        // （相手が壁で対抗 → 相手の壁を消費させる効果もある）
        if (player.walls > 0) {
          score += 3; // 壁を使わせる効果（相手の壁資源を削る）
        }

        // 学習データ
        const key = `${wall.cornerRow},${wall.cornerCol},${wall.orientation}`;
        const learnScore = aiLearningData.goodWallPositions[key] || 0;
        if (learnScore > 0) score += Math.min(learnScore / 300, 5);

        if (ai.walls <= 2 && playerIncrease < 2) score -= 20;

        candidates.push({ wall, score });
      }

      for (const cand of candidates) {
        if (cand.score > bestScore) {
          bestScore = cand.score;
          bestWall = cand.wall;
        }
      }

      if (!bestWall || bestScore <= 0) return null;
      return { wall: bestWall, score: bestScore };
    }

    // N手先読み移動スコア（ボスねこ: depth=2, ボスねこ+: depth=3）
    // 核心: 最短経路前進 + テンポ差 + 相手カード脅威
    function scoreMoveNPly(ai, player, move, depth) {
      const playerGoal = BOARD_SIZE - 1;
      const savedAiRow = ai.row, savedAiCol = ai.col;

      // ゴール到達は最高スコア
      if (move.row === 0) return 10000;

      // === 現在の距離（移動前） ===
      const currentDist = (findShortestPath(ai.row, ai.col, 0) || []).length || 999;

      // === 移動後の自分の距離 ===
      ai.row = move.row; ai.col = move.col;
      const newDist = (findShortestPath(move.row, move.col, 0) || []).length || 999;
      const advanceGain = currentDist - newDist;

      // === 相手の最善応答を先読み ===
      const opMoves = getValidMoves(1);
      let bestOpDist = 999;

      for (const om of opMoves) {
        const savedOpRow = player.row, savedOpCol = player.col;
        player.row = om.row; player.col = om.col;

        if (om.row === playerGoal) {
          player.row = savedOpRow; player.col = savedOpCol;
          ai.row = savedAiRow; ai.col = savedAiCol;
          return -5000;
        }

        const opDist = (findShortestPath(om.row, om.col, playerGoal) || []).length || 999;

        if (depth >= 3) {
          // 3手先: 相手応答後 → AIの2手目まで読む
          const aiMoves2 = getValidMoves(2);
          let bestAi2Dist = 999;
          for (const am of aiMoves2) {
            if (am.row === 0) { bestAi2Dist = 1; break; }
            const savedAi2Row = ai.row, savedAi2Col = ai.col;
            ai.row = am.row; ai.col = am.col;
            const d = (findShortestPath(am.row, am.col, 0) || []).length || 999;
            ai.row = savedAi2Row; ai.col = savedAi2Col;
            if (d < bestAi2Dist) bestAi2Dist = d;
          }
        }

        if (opDist < bestOpDist) bestOpDist = opDist;
        player.row = savedOpRow; player.col = savedOpCol;
      }

      ai.row = savedAiRow; ai.col = savedAiCol;

      // === 相手の未使用カード脅威 ===
      const opUsed = (id) => (player.cardsUsed[id] || 0) >= 1;
      const opCardsLeft = player.totalCardsUsed < MAX_CARDS_PER_PLAYER;
      let cardThreat = 0;

      if (opCardsLeft) {
        // dash脅威: 相手が通常移動+1歩分追加で前進する可能性
        if (!opUsed('dash')) {
          bestOpDist = Math.max(2, bestOpDist - 1);
        }

        // swap脅威: 移動後にマンハッタン距離≤2なら入れ替えられる危険
        if (!opUsed('swap') && gameState.turn >= 2) {
          const distToOp = Math.abs(move.row - player.row) + Math.abs(move.col - player.col);
          if (distToOp <= 2) {
            // swap後: AIがplayer位置(相手ゴール近く)、playerがAI位置(AIゴール近く)
            const swappedAiDist = (findShortestPath(player.row, player.col, 0) || []).length || 999;
            const swapLoss = swappedAiDist - newDist; // 正=不利になる
            if (swapLoss > 0) {
              cardThreat += swapLoss * 12; // swap食らうと大損 → 近づきすぎを抑制
            }
          }
        }

        // push脅威: 隣接していると押し戻される危険
        if (!opUsed('push')) {
          const distToOp = Math.abs(move.row - player.row) + Math.abs(move.col - player.col);
          if (distToOp === 1) {
            // 相手方向に押される → ゴールから遠ざかる
            const dr = move.row - player.row;
            const pushDest = move.row + dr;
            if (pushDest >= 0 && pushDest < BOARD_SIZE) {
              const pushedDist = (findShortestPath(pushDest, move.col, 0) || []).length || 999;
              const pushLoss = pushedDist - newDist;
              if (pushLoss > 0) cardThreat += pushLoss * 8;
            }
          }
        }

        // freeze脅威: 相手が使うと1ターンロス → 実質テンポ-1
        if (!opUsed('freeze')) {
          cardThreat += 4; // 軽微だが考慮
        }
      }

      // === スコアリング ===
      const advanceBonus = advanceGain * 40;
      const distScore = (BOARD_SIZE - newDist) * 5;
      const tempo = bestOpDist - newDist;
      const tempoScore = tempo * 8;

      return advanceBonus + distScore + tempoScore - cardThreat;
    }

    // 壁のスコアリング + 2壁コンボ先読み
    // 壁を置く = 1ターン移動を犠牲にして相手の経路を伸ばす
    function scoreLearnedBestWall(ai, player, aiDist, playerDist) {
      const walls = getValidWallPlacements();
      if (walls.length === 0) return null;

      const playerGoal = BOARD_SIZE - 1;
      const currentPlayerPath = findShortestPath(player.row, player.col, playerGoal);

      // 相手の現在経路のエッジセット（直撃判定用）
      const playerEdgeSet = new Set();
      if (currentPlayerPath) {
        for (let i = 0; i < currentPlayerPath.length - 1; i++) {
          const from = currentPlayerPath[i], to = currentPlayerPath[i + 1];
          const edgeRow = from.row * 2 + (to.row - from.row);
          const edgeCol = from.col * 2 + (to.col - from.col);
          playerEdgeSet.add(`${edgeRow},${edgeCol}`);
        }
      }

      // 移動の機会コスト
      const bestMoveScore = (() => {
        const moves = getValidMoves(2);
        let best = -Infinity;
        for (const m of moves) {
          if (m.row === 0) return 10000;
          const d = (findShortestPath(m.row, m.col, 0) || []).length || 999;
          const gain = aiDist - d;
          const s = gain * 40 + (BOARD_SIZE - d) * 5;
          if (s > best) best = s;
        }
        return best;
      })();

      if (bestMoveScore >= 10000) return null;

      let bestWall = null;
      let bestScore = -Infinity;

      // === 1次評価: 全壁を評価してトップN候補を残す ===
      const candidates = [];
      for (const wall of walls) {
        gameState.walls.push({ ...wall, owner: 2 });
        const newPlayerPath = findShortestPath(player.row, player.col, playerGoal);
        const newAiPath = findShortestPath(ai.row, ai.col, 0);
        gameState.walls.pop();

        if (!newPlayerPath || !newAiPath) continue;

        const playerIncrease = newPlayerPath.length - playerDist;
        const aiIncrease = newAiPath.length - aiDist;
        if (playerIncrease <= 0) continue;

        // 直撃判定
        let isDirectHit = false;
        if (playerEdgeSet.size > 0) {
          const edges = getBlockedEdges(wall);
          for (const e of edges) {
            if (playerEdgeSet.has(`${e.row},${e.col}`)) { isDirectHit = true; break; }
          }
        }

        const delayValue = playerIncrease * (isDirectHit ? 15 : 8);
        const selfPenalty = aiIncrease * 10;
        let score = delayValue - selfPenalty - bestMoveScore;

        // 学習データ
        const key = `${wall.cornerRow},${wall.cornerCol},${wall.orientation}`;
        const learnScore = aiLearningData.goodWallPositions[key] || 0;
        if (learnScore > 0) score += Math.min(learnScore / 300, 5);

        if (ai.walls <= 2 && playerIncrease < 2) score -= 20;

        candidates.push({ wall, score, playerIncrease, newPlayerDist: newPlayerPath.length });
      }

      // === 2壁コンボ先読み（壁が2つ以上あるとき、トップ8候補に対して） ===
      if (ai.walls >= 2 && candidates.length > 0) {
        // 1次スコア上位8候補を対象
        candidates.sort((a, b) => b.score - a.score);
        const topN = candidates.slice(0, 8);

        for (const cand of topN) {
          // 壁Aを仮配置
          gameState.walls.push({ ...cand.wall, owner: 2 });

          // 壁Aの後、最も効果的な壁Bを探す
          const walls2 = getValidWallPlacements();
          let bestComboIncrease = 0;
          for (const w2 of walls2) {
            gameState.walls.push({ ...w2, owner: 2 });
            const comboPath = findShortestPath(player.row, player.col, playerGoal);
            const comboAiPath = findShortestPath(ai.row, ai.col, 0);
            gameState.walls.pop();
            if (!comboPath || !comboAiPath) continue;
            const totalIncrease = comboPath.length - playerDist;
            if (totalIncrease > bestComboIncrease) bestComboIncrease = totalIncrease;
          }
          gameState.walls.pop();

          // コンボ効果: 壁B追加で大きく伸びるなら壁Aの価値が高い
          const comboBonus = bestComboIncrease - cand.playerIncrease;
          if (comboBonus >= 2) {
            cand.score += comboBonus * 10; // コンボで+2歩以上なら大ボーナス
          }
        }
      }

      // 最高スコアの壁を選ぶ
      for (const cand of candidates) {
        if (cand.score > bestScore) {
          bestScore = cand.score;
          bestWall = cand.wall;
        }
      }

      if (!bestWall || bestScore <= 0) return null;
      return { wall: bestWall, score: bestScore };
    }

    // カード使用評価: 前進に直結するカードのみ使用
    // カードを使う = 1ターン消費 → 通常移動より明確に良い場合だけ
    function scoreCardActions(ai, player, aiDist, playerDist) {
      if (ai.totalCardsUsed >= MAX_CARDS_PER_PLAYER) return null;
      const usedCard = (id) => (ai.cardsUsed[id] || 0) >= 1;
      const playerGoal = BOARD_SIZE - 1;
      let bestCard = null;
      let bestScore = -Infinity;

      // dash: 2マス移動 → 通常移動より1歩多く前進できる
      // 通常移動1歩=40点、dash2歩=80点 → 差が40点あるので強い
      if (!usedCard('dash') && hasDashTargetFor(2)) {
        const dashTarget = getBestDashTarget(2);
        if (dashTarget) {
          const dashDist = (findShortestPath(dashTarget.row, dashTarget.col, 0) || []).length || 999;
          const gain = aiDist - dashDist;
          if (gain >= 2) {
            // 2歩以上前進できる場合のみ使用（1歩なら通常移動と同じ）
            const score = gain * 40 + (BOARD_SIZE - dashDist) * 5;
            if (score > bestScore) { bestScore = score; bestCard = { type: 'card', cardId: 'dash' }; }
          }
        }
      }

      // swap: 位置入れ替え → 自分が大幅に前進できるなら使う
      if (!usedCard('swap') && gameState.turn >= 3) {
        const swapDist = Math.abs(ai.row - player.row) + Math.abs(ai.col - player.col);
        if (swapDist <= 2) {
          const swappedAiDist = (findShortestPath(player.row, player.col, 0) || []).length || 999;
          const myGain = aiDist - swappedAiDist;
          if (myGain >= 2) {
            // 自分が2歩以上前進できるなら使う（前進度×40で移動と同スケール比較）
            const score = myGain * 40 + (BOARD_SIZE - swappedAiDist) * 5;
            if (score > bestScore) { bestScore = score; bestCard = { type: 'card', cardId: 'swap' }; }
          }
        }
      }

      // jump: 飛び越え → 前進できるなら使う
      if (!usedCard('jump') && isAdjacentFor(2) && canJumpOverFor(2)) {
        const jumpTarget = getJumpTarget(2);
        if (jumpTarget && jumpTarget.row >= 0 && jumpTarget.row < BOARD_SIZE) {
          const jumpDist = (findShortestPath(jumpTarget.row, jumpTarget.col, 0) || []).length || 999;
          const gain = aiDist - jumpDist;
          if (gain >= 2) {
            const score = gain * 40 + (BOARD_SIZE - jumpDist) * 5;
            if (score > bestScore) { bestScore = score; bestCard = { type: 'card', cardId: 'jump' }; }
          }
        }
      }

      // push: 相手を押す + 自分がその位置に入る → 前進できるなら使う
      if (!usedCard('push') && isAdjacentFor(2) && canPushFor(2)) {
        const dr = player.row - ai.row, dc = player.col - ai.col;
        const pushDest = { row: player.row + dr, col: player.col + dc };
        if (pushDest.row >= 0 && pushDest.row < BOARD_SIZE && pushDest.col >= 0 && pushDest.col < BOARD_SIZE) {
          // pushするとAIが相手の位置に移動
          const aiNewDist = (findShortestPath(player.row, player.col, 0) || []).length || 999;
          const myGain = aiDist - aiNewDist;
          if (myGain >= 1) {
            // 前進 + 相手を押し戻す効果
            const pushPlayerDist = (findShortestPath(pushDest.row, pushDest.col, playerGoal) || []).length || 999;
            const opLoss = pushPlayerDist - playerDist;
            const score = myGain * 40 + Math.max(0, opLoss) * 8 + (BOARD_SIZE - aiNewDist) * 5;
            if (score > bestScore) { bestScore = score; bestCard = { type: 'card', cardId: 'push' }; }
          }
        }
      }

      // freeze, wallplus: 使わない（1ターン消費して前進しないカードは弱い）

      if (!bestCard) return null;
      return { action: bestCard, score: bestScore };
    }

    // 学習した良い壁位置を取得（プレイヤーが下の白マスへ行くのをブロック）
    function getLearnedWallPositions() {
      const validWalls = getValidWallPlacements();
      const player = gameState.players[1]; // ちゃとら
      const playerGoal = BOARD_SIZE - 1; // プレイヤーのゴールは下(row=6)
      
      // 現在のプレイヤーの最短経路
      const playerPath = findShortestPath(player.row, player.col, playerGoal);
      const currentDist = playerPath ? playerPath.length : 999;
      
      const scoredWalls = validWalls.map(wall => {
        const key = `${wall.cornerRow},${wall.cornerCol},${wall.orientation}`;
        let score = aiLearningData.goodWallPositions[key] || 0;
        
        // プレイヤーの下への経路をブロックできるかチェック
        gameState.walls.push({ ...wall, owner: 2 });
        const newPath = findShortestPath(player.row, player.col, playerGoal);
        gameState.walls.pop();
        
        // 経路が完全に塞がる壁は除外（安全策）
        if (!newPath) return { ...wall, score: -1 };

        const increase = newPath.length - currentDist;
        score += increase * 2000; // ブロック効果を大きく評価

        return { ...wall, score };
      });
      
      // スコア順にソート
      scoredWalls.sort((a, b) => b.score - a.score);
      return scoredWalls.filter(w => w.score > 0);
    }

    // トレーニング開始
    function startTraining() {
      trainingMode = true;
      trainingGames = 0;
      aiLearningData = {
        goodWallPositions: {},
        goodMovePatterns: {},
        trained: false,
        totalGames: 0,
        wins: { 1: 0, 2: 0 }
      };
      
      document.getElementById('training-overlay').classList.remove('hidden');
      document.getElementById('training-bar').style.width = '0%';
      document.getElementById('training-text').textContent = '0 / 100 戦';
      document.getElementById('training-stats').textContent = '';
      
      // 非同期でトレーニング実行
      setTimeout(runTrainingGame, 100);
    }

    // トレーニング用のゲームを1回実行
    function runTrainingGame() {
      if (trainingGames >= trainingTotal) {
        finishTraining();
        return;
      }
      
      // ゲーム状態をリセット
      resetTrainingGame();
      currentGameMoves = [];
      
      // 高速でゲームをシミュレート
      simulateGame();
    }

    // トレーニング用ゲームリセット
    function resetTrainingGame() {
      gameState = {
        currentPlayer: 1,
        mode: 'move',
        turn: 1,
        players: {
          1: { row: 0, col: 3, walls: 6, emoji: '🐈', color: '#ffffff', cardsUsed: {}, totalCardsUsed: 0, frozen: false },
          2: { row: 6, col: 3, walls: 6, emoji: '🐈‍⬛', color: '#333333', cardsUsed: {}, totalCardsUsed: 0, frozen: false }
        },
        walls: [],
        gameOver: false,
        selectedCard: null,
        cardPhase: null,
        wallOrientation: 'h',
        previewWall: null,
        slideTargetWall: null,
        copyingCard: false
      };
    }

    // ゲームをシミュレート（高速）
    function simulateGame() {
      let maxTurns = 200; // 無限ループ防止
      
      while (!gameState.gameOver && maxTurns > 0) {
        maxTurns--;
        
        // 両方のプレイヤーがボスねこAIとして行動
        const action = aiHardAction();
        
        if (action) {
          // 行動を記録
          currentGameMoves.push({
            player: gameState.currentPlayer,
            action: action,
            turn: gameState.turn
          });
          
          // 行動を実行（描画なし）
          executeTrainingAction(action);
        } else {
          // 行動できない場合はパス
          gameState.currentPlayer = gameState.currentPlayer === 1 ? 2 : 1;
          gameState.turn++;
        }
        
        // 勝利チェック
        checkTrainingWin();
      }
      
      // 次のゲームへ
      trainingGames++;
      updateTrainingProgress();
      
      // 少し待って次のゲーム
      setTimeout(runTrainingGame, 10);
    }

    // トレーニング用の行動実行（描画なし）
    function executeTrainingAction(action) {
      const currentPlayer = gameState.currentPlayer;
      
      if (action.type === 'move') {
        gameState.players[currentPlayer].row = action.row;
        gameState.players[currentPlayer].col = action.col;
      } else if (action.type === 'wall') {
        const wall = {
          cornerRow: action.cornerRow,
          cornerCol: action.cornerCol,
          orientation: action.orientation,
          owner: currentPlayer
        };
        gameState.walls.push(wall);
        gameState.players[currentPlayer].walls--;
      }
      
      gameState.currentPlayer = currentPlayer === 1 ? 2 : 1;
      gameState.turn++;
    }

    // トレーニング用の勝利チェック
    function checkTrainingWin() {
      const p1 = gameState.players[1];
      const p2 = gameState.players[2];
      
      if (p1.row === BOARD_SIZE - 1) {
        recordWin(1);
        gameState.gameOver = true;
      } else if (p2.row === 0) {
        recordWin(2);
        gameState.gameOver = true;
      }
    }

    // 勝利を記録して学習
    function recordWin(winner) {
      aiLearningData.wins[winner]++;
      aiLearningData.totalGames++;
      
      // 勝った側の壁配置を学習
      const winnerMoves = currentGameMoves.filter(m => m.player === winner && m.action.type === 'wall');
      winnerMoves.forEach(m => {
        const key = `${m.action.cornerRow},${m.action.cornerCol},${m.action.orientation}`;
        aiLearningData.goodWallPositions[key] = (aiLearningData.goodWallPositions[key] || 0) + 1;
      });
    }

    // トレーニング進捗を更新
    function updateTrainingProgress() {
      const percent = (trainingGames / trainingTotal) * 100;
      document.getElementById('training-bar').style.width = percent + '%';
      document.getElementById('training-text').textContent = `${trainingGames} / ${trainingTotal} 戦`;
      
      const stats = `プレイヤー1勝利: ${aiLearningData.wins[1]} / プレイヤー2勝利: ${aiLearningData.wins[2]}`;
      document.getElementById('training-stats').textContent = stats;
    }

    // トレーニング完了
    function finishTraining() {
      trainingMode = false;
      aiLearningData.trained = true;
      
      document.getElementById('training-overlay').classList.add('hidden');
      
      // ボスねこの説明を更新
      document.getElementById('boss-name').textContent = 'ボスねこ+';
      document.getElementById('boss-desc').textContent = `${trainingTotal}戦の けいけんを つんだ！`;
      document.getElementById('train-status').textContent = `✓ 学習完了！（${aiLearningData.wins[2]}勝）`;
      
      showToast(`トレーニング完了！ ボスねこは${aiLearningData.wins[2]}勝しました`, 'success', 3000);
    }

    // AIがカード使用を検討する（ボスねこ系のみ使用）
    function aiConsiderCard() {
      const ai = gameState.players[2];
      const player = gameState.players[1];
      const aiHand = playerHands[2];
      if (!aiHand) return null;

      const maxCards = aiHand.length;
      if (ai.totalCardsUsed >= maxCards) return null;

      const aiPath = findShortestPath(ai.row, ai.col, 0);
      const playerPath = findShortestPath(player.row, player.col, BOARD_SIZE - 1);
      const aiDist = aiPath ? aiPath.length : 999;
      const playerDist = playerPath ? playerPath.length : 999;
      const usedCard = (id) => (ai.cardsUsed[id] || 0) >= 1;
      const hasCard = (id) => aiHand.includes(id) && !usedCard(id) && !((ai.sealedCards[id] || 0) > 0);
      const dist = Math.abs(ai.row - player.row) + Math.abs(ai.col - player.col);
      const adjacent = isAdjacentFor(2);

      // === 移動妨害系カード（高優先度）===
      // 金縛り / 感電ショック
      if (hasCard('ghost_kanashibari') && playerDist <= 4) {
        return { type: 'card', cardId: 'ghost_kanashibari' };
      }
      if (hasCard('electric_shock') && adjacent && playerDist <= 5) {
        return { type: 'card', cardId: 'electric_shock' };
      }

      // === 移動系カード（ゴールに近づける）===
      // 電光石火: 2マス自由移動
      if (hasCard('electric_raiden') && aiDist <= 5) {
        return { type: 'card', cardId: 'electric_raiden' };
      }
      // ムササビの術: 相手を飛び越え
      if (hasCard('ninja_musasabi') && adjacent && canJumpOverFor(2)) {
        const jt = getJumpTarget(2);
        if (jt && jt.row < ai.row) return { type: 'card', cardId: 'ninja_musasabi' };
      }
      // 忍び足: 斜め移動
      if (hasCard('ninja_shinobi') && aiDist <= 4) {
        return { type: 'card', cardId: 'ninja_shinobi' };
      }
      // ゴーストウォーク: 壁すり抜け
      if (hasCard('ghost_walk') && aiDist <= 4) {
        return { type: 'card', cardId: 'ghost_walk' };
      }
      // ずらかる: 横2マス
      if (hasCard('thief_zurakaru') && aiDist <= 5) {
        return { type: 'card', cardId: 'thief_zurakaru' };
      }
      // テレポート
      if (hasCard('witch_teleport') && aiDist <= 4) {
        return { type: 'card', cardId: 'witch_teleport' };
      }

      // === 相手操作系カード ===
      // 押し出し
      if (hasCard('sumo_oshidashi') && adjacent && canPushFor(2)) {
        return { type: 'card', cardId: 'sumo_oshidashi' };
      }
      // 上手投げ
      if (hasCard('sumo_uwatenage') && adjacent) {
        return { type: 'card', cardId: 'sumo_uwatenage' };
      }
      // 憑依 / リモート操作
      if (hasCard('ghost_hyoui') && dist <= 2) {
        return { type: 'card', cardId: 'ghost_hyoui' };
      }
      if (hasCard('prof_remote') && dist <= 2) {
        return { type: 'card', cardId: 'prof_remote' };
      }
      // 超電導マグネット
      if (hasCard('electric_magnet') && dist <= 2 && dist > 0) {
        return { type: 'card', cardId: 'electric_magnet' };
      }

      // === 壁操作系カード ===
      if (hasCard('prof_wallmachine') && ai.walls <= 1) {
        return { type: 'card', cardId: 'prof_wallmachine' };
      }
      if (hasCard('thief_itadaki') && player.walls > 0) {
        return { type: 'card', cardId: 'thief_itadaki' };
      }
      if (hasCard('construction_blast') && gameState.walls.length > 0 && aiDist > playerDist) {
        return { type: 'card', cardId: 'construction_blast' };
      }

      // === 封印系 ===
      if (hasCard('witch_seal') && playerDist <= 5) {
        return { type: 'card', cardId: 'witch_seal' };
      }

      // === 隠れる / コピー ===
      if (hasCard('thief_kakureru') && playerDist <= 3) {
        return { type: 'card', cardId: 'thief_kakureru' };
      }
      if (hasCard('prof_copy')) {
        return { type: 'card', cardId: 'prof_copy' };
      }

      // 壁スライド系（状況に応じて）
      if (hasCard('sumo_teppou') && gameState.walls.length > 0) {
        return { type: 'card', cardId: 'sumo_teppou' };
      }
      if (hasCard('construction_move') && gameState.walls.length > 0) {
        return { type: 'card', cardId: 'construction_move' };
      }
      if (hasCard('construction_rotate') && gameState.walls.length > 0) {
        return { type: 'card', cardId: 'construction_rotate' };
      }

      return null;
    }

    // AI用: ダッシュの最適な移動先を返す
    function getBestDashTarget(playerNum) {
      const p = gameState.players[playerNum];
      const o = gameState.players[playerNum === 1 ? 2 : 1];
      const goalRow = playerNum === 1 ? BOARD_SIZE - 1 : 0;
      const directions = [[-1, 0], [1, 0], [0, -1], [0, 1]];
      let bestTarget = null;
      let bestDist = Infinity;
      for (const [dr, dc] of directions) {
        const mr = p.row + dr, mc = p.col + dc;
        const tr = p.row + dr * 2, tc = p.col + dc * 2;
        if (tr >= 0 && tr < BOARD_SIZE && tc >= 0 && tc < BOARD_SIZE) {
          if (!isWallBetween(p.row, p.col, mr, mc) &&
              !isWallBetween(mr, mc, tr, tc) &&
              !(tr === o.row && tc === o.col) &&
              !(mr === o.row && mc === o.col)) {
            const path = findShortestPath(tr, tc, goalRow);
            const dist = path ? path.length : 999;
            if (dist < bestDist) {
              bestDist = dist;
              bestTarget = { row: tr, col: tc };
            }
          }
        }
      }
      return bestTarget;
    }

    // AI用のカード条件チェック関数群
    function isAdjacentFor(playerNum) {
      const p = gameState.players[playerNum];
      const o = gameState.players[playerNum === 1 ? 2 : 1];
      const dr = Math.abs(p.row - o.row);
      const dc = Math.abs(p.col - o.col);
      return (dr === 1 && dc === 0) || (dr === 0 && dc === 1);
    }

    function canJumpOverFor(playerNum) {
      const p = gameState.players[playerNum];
      const o = gameState.players[playerNum === 1 ? 2 : 1];
      const dr = o.row - p.row;
      const dc = o.col - p.col;
      const lr = o.row + dr;
      const lc = o.col + dc;
      if (lr < 0 || lr >= BOARD_SIZE || lc < 0 || lc >= BOARD_SIZE) return false;
      return !isWallBetween(p.row, p.col, o.row, o.col) &&
             !isWallBetween(o.row, o.col, lr, lc);
    }

    function getJumpTarget(playerNum) {
      const p = gameState.players[playerNum];
      const o = gameState.players[playerNum === 1 ? 2 : 1];
      const dr = o.row - p.row;
      const dc = o.col - p.col;
      return { row: o.row + dr, col: o.col + dc };
    }

    function canPushFor(playerNum) {
      const p = gameState.players[playerNum];
      const o = gameState.players[playerNum === 1 ? 2 : 1];
      const dr = o.row - p.row;
      const dc = o.col - p.col;
      const pr = o.row + dr;
      const pc = o.col + dc;
      if (pr < 0 || pr >= BOARD_SIZE || pc < 0 || pc >= BOARD_SIZE) return false;
      return !isWallBetween(p.row, p.col, o.row, o.col) &&
             !isWallBetween(o.row, o.col, pr, pc);
    }

    function hasDashTargetFor(playerNum) {
      const p = gameState.players[playerNum];
      const o = gameState.players[playerNum === 1 ? 2 : 1];
      const directions = [[-1, 0], [1, 0], [0, -1], [0, 1]];
      for (const [dr, dc] of directions) {
        const mr = p.row + dr;
        const mc = p.col + dc;
        const tr = p.row + dr * 2;
        const tc = p.col + dc * 2;
        if (tr >= 0 && tr < BOARD_SIZE && tc >= 0 && tc < BOARD_SIZE) {
          if (!isWallBetween(p.row, p.col, mr, mc) &&
              !isWallBetween(mr, mc, tr, tc) &&
              !(tr === o.row && tc === o.col) &&
              !(mr === o.row && mc === o.col)) {
            return true;
          }
        }
      }
      return false;
    }

    // AIの行動を実行
    function executeAIAction(action) {
      if (!action) {
        switchPlayer();
        return;
      }

      if (action.type === 'card') {
        const cid = action.cardId;
        // AIの手札にあるか＆ゲームルールで使用可能か最終チェック
        const aiHand = playerHands[2];
        if (!aiHand || !aiHand.includes(cid) || !canUseCard(cid)) {
          gameState.selectedCard = null;
          // カードが無効なら通常移動にフォールバック
          const moves = getValidMoves(2);
          if (moves.length > 0) {
            const ai = gameState.players[2];
            ai.row = moves[0].row;
            ai.col = moves[0].col;
            renderCats(2);
            checkWin();
          }
          if (!gameState.gameOver) switchPlayer();
          return;
        }
        saveHistory();
        gameState.selectedCard = cid;

        // 即時実行カード
        const immediateCards = ['thief_kakureru', 'thief_itadaki', 'ghost_kanashibari', 'electric_shock', 'prof_wallmachine', 'prof_copy'];
        if (immediateCards.includes(cid)) {
          executeImmediateCard(cid);
          return;
        }

        // ターゲット選択が必要なカード → AI自動選択
        aiExecuteTargetCard(cid);
        return;
      }

      if (action.type === 'move') {
        const ai = gameState.players[2];
        ai.row = action.row;
        ai.col = action.col;
        renderCats(2);
        checkWin();
        if (!gameState.gameOver) {
          switchPlayer();
        }
      } else if (action.type === 'wall') {
        const wall = {
          cornerRow: action.cornerRow,
          cornerCol: action.cornerCol,
          orientation: action.orientation,
          owner: 2
        };
        gameState.walls.push(wall);
        gameState.players[2].walls--;
        renderWalls();
        updateUI();
        switchPlayer();
      }
    }

    // AI用のダッシュ実行
    function aiExecuteDash() {
      const ai = gameState.players[2];
      const other = gameState.players[1];
      const directions = [[-1, 0], [1, 0], [0, -1], [0, 1]];
      let bestTarget = null;
      let bestDist = Infinity;

      for (const [dr, dc] of directions) {
        const mr = ai.row + dr;
        const mc = ai.col + dc;
        const tr = ai.row + dr * 2;
        const tc = ai.col + dc * 2;
        if (tr >= 0 && tr < BOARD_SIZE && tc >= 0 && tc < BOARD_SIZE) {
          if (!isWallBetween(ai.row, ai.col, mr, mc) &&
              !isWallBetween(mr, mc, tr, tc) &&
              !(tr === other.row && tc === other.col) &&
              !(mr === other.row && mc === other.col)) {
            const path = findShortestPath(tr, tc, 0);
            const dist = path ? path.length : 999;
            if (dist < bestDist) {
              bestDist = dist;
              bestTarget = { row: tr, col: tc };
            }
          }
        }
      }

      if (bestTarget) {
        const fromR = ai.row, fromC = ai.col;
        showCardEffect('dash', () => {
          animateDash(fromR, fromC, bestTarget.row, bestTarget.col, 2, () => {
            ai.row = bestTarget.row;
            ai.col = bestTarget.col;
            useCard();
            renderCats(2);
            checkWin();
            if (!gameState.gameOver) switchPlayer();
          });
        });
      } else {
        gameState.selectedCard = null;
        switchPlayer();
      }
    }

    // AI用のジャンプ実行
    function aiExecuteJump() {
      const ai = gameState.players[2];
      const target = getJumpTarget(2);
      const fromR = ai.row, fromC = ai.col;

      showCardEffect('jump', () => {
        animateJump(fromR, fromC, target.row, target.col, 2, () => {
          ai.row = target.row;
          ai.col = target.col;
          useCard();
          renderCats(2);
          checkWin();
          if (!gameState.gameOver) switchPlayer();
        });
      });
    }

    // AI用のプッシュ実行
    function aiExecutePush() {
      const ai = gameState.players[2];
      const player = gameState.players[1];
      const dr = player.row - ai.row;
      const dc = player.col - ai.col;
      const pushTarget = { row: player.row + dr, col: player.col + dc };
      const aiTarget = { row: player.row, col: player.col };
      const pushDir = [dr, dc];

      showCardEffect('push', () => {
        animatePush(2, pushDir, () => {
          player.row = pushTarget.row;
          player.col = pushTarget.col;
          ai.row = aiTarget.row;
          ai.col = aiTarget.col;
          useCard();
          renderCats(2);
          checkWin();
          if (!gameState.gameOver) switchPlayer();
        });
      });
    }

    // AI用: 新キャラカードのターゲット自動選択＆実行
    function aiExecuteTargetCard(cardId) {
      const ai = gameState.players[2];
      const player = gameState.players[1];

      const doMove = (row, col) => {
        showCardEffect(cardId, () => {
          ai.row = row;
          ai.col = col;
          useCard();
          renderCats(2);
          checkWin();
          if (!gameState.gameOver) afterCardAction(cardId);
        });
      };

      const doMoveOther = (row, col) => {
        showCardEffect(cardId, () => {
          player.row = row;
          player.col = col;
          useCard();
          renderCats();
          checkWin();
          if (!gameState.gameOver) afterCardAction(cardId);
        });
      };

      const fallback = () => { gameState.selectedCard = null; switchPlayer(); };

      // ゴール方向に最も近づけるマスを選ぶヘルパー
      const bestMoveToGoal = (candidates) => {
        let best = null, bestDist = Infinity;
        for (const {row, col} of candidates) {
          const p = findShortestPath(row, col, 0);
          const d = p ? p.length : 999;
          if (d < bestDist) { bestDist = d; best = {row, col}; }
        }
        return best;
      };

      switch (cardId) {
        case 'ninja_shinobi': {
          const cands = [];
          for (const [dr,dc] of [[-1,-1],[-1,1],[1,-1],[1,1]]) {
            const nr = ai.row+dr, nc = ai.col+dc;
            if (nr<0||nr>=BOARD_SIZE||nc<0||nc>=BOARD_SIZE) continue;
            if (nr===player.row && nc===player.col) continue;
            if (!isWallBetween(ai.row, ai.col, ai.row+dr, ai.col) &&
                !isWallBetween(ai.row+dr, ai.col, nr, nc)) {
              cands.push({row:nr, col:nc});
            }
          }
          const t = bestMoveToGoal(cands);
          if (t) doMove(t.row, t.col); else fallback();
          break;
        }
        case 'ninja_musasabi': {
          const jt = getJumpTarget(2);
          if (jt) doMove(jt.row, jt.col); else fallback();
          break;
        }
        case 'thief_zurakaru': {
          const cands = [];
          for (const dc of [-2, 2]) {
            const nc = ai.col + dc;
            if (nc >= 0 && nc < BOARD_SIZE && !(ai.row === player.row && nc === player.col)) {
              cands.push({row: ai.row, col: nc});
            }
          }
          const t = bestMoveToGoal(cands);
          if (t) doMove(t.row, t.col); else fallback();
          break;
        }
        case 'sumo_oshidashi': {
          const dr = player.row - ai.row;
          const dc = player.col - ai.col;
          const pushRow = player.row + dr;
          const pushCol = player.col + dc;
          if (pushRow>=0 && pushRow<BOARD_SIZE && pushCol>=0 && pushCol<BOARD_SIZE &&
              !isWallBetween(player.row, player.col, pushRow, pushCol)) {
            showCardEffect(cardId, () => {
              player.row = pushRow;
              player.col = pushCol;
              useCard();
              renderCats();
              checkWin();
              if (!gameState.gameOver) afterCardAction(cardId);
            });
          } else fallback();
          break;
        }
        case 'sumo_uwatenage': {
          const dr = player.row - ai.row;
          const dc = player.col - ai.col;
          let throwRow = ai.row - dr;
          let throwCol = ai.col - dc;
          throwRow = Math.max(0, Math.min(BOARD_SIZE-1, throwRow));
          throwCol = Math.max(0, Math.min(BOARD_SIZE-1, throwCol));
          if (throwRow === ai.row && throwCol === ai.col) { fallback(); break; }
          doMoveOther(throwRow, throwCol);
          break;
        }
        case 'witch_teleport': {
          const cands = [];
          for (let c = 0; c < BOARD_SIZE; c++) {
            if (c !== ai.col) cands.push({row: ai.row, col: c});
          }
          const t = bestMoveToGoal(cands);
          if (t) {
            showCardEffect(cardId, () => {
              if (t.row === player.row && t.col === player.col) {
                player.row = ai.row; player.col = ai.col;
              }
              ai.row = t.row; ai.col = t.col;
              useCard();
              renderCats(2);
              checkWin();
              if (!gameState.gameOver) afterCardAction(cardId);
            });
          } else fallback();
          break;
        }
        case 'witch_seal': {
          // 相手の未使用カードからランダムに封印
          const otherHand = playerHands[1];
          if (otherHand) {
            const sealable = otherHand.filter(cid =>
              !((player.cardsUsed[cid]||0)>=1) && !((player.sealedCards[cid]||0)>0)
            );
            if (sealable.length > 0) {
              const target = sealable[Math.floor(Math.random()*sealable.length)];
              showCardEffect(cardId, () => {
                player.sealedCards[target] = 2;
                useCard();
                afterCardAction(cardId);
              });
            } else fallback();
          } else fallback();
          break;
        }
        case 'ghost_walk': {
          // 壁を無視して隣接マスの最善を選ぶ
          const cands = [];
          for (const [dr,dc] of [[-1,0],[1,0],[0,-1],[0,1]]) {
            const nr = ai.row+dr, nc = ai.col+dc;
            if (nr>=0 && nr<BOARD_SIZE && nc>=0 && nc<BOARD_SIZE && !(nr===player.row && nc===player.col)) {
              cands.push({row:nr, col:nc});
            }
          }
          const t = bestMoveToGoal(cands);
          if (t) doMove(t.row, t.col); else fallback();
          break;
        }
        case 'ghost_hyoui':
        case 'prof_remote': {
          // 相手をゴールから遠ざける方向に動かす
          const cands = [];
          for (const [dr,dc] of [[-1,0],[1,0],[0,-1],[0,1]]) {
            const nr = player.row+dr, nc = player.col+dc;
            if (nr>=0 && nr<BOARD_SIZE && nc>=0 && nc<BOARD_SIZE &&
                !(nr===ai.row && nc===ai.col) && !isWallBetween(player.row, player.col, nr, nc)) {
              cands.push({row:nr, col:nc});
            }
          }
          // 相手がゴール(row=BOARD_SIZE-1)から最も遠ざかるマスを選ぶ
          let best = null, bestScore = -Infinity;
          for (const c of cands) {
            const p = findShortestPath(c.row, c.col, BOARD_SIZE-1);
            const d = p ? p.length : 999;
            if (d > bestScore) { bestScore = d; best = c; }
          }
          if (best) doMoveOther(best.row, best.col); else fallback();
          break;
        }
        case 'electric_raiden': {
          // 2歩以内の最善マス
          const cands = [];
          for (let r = 0; r < BOARD_SIZE; r++) {
            for (let c = 0; c < BOARD_SIZE; c++) {
              if (r===ai.row && c===ai.col) continue;
              if (r===player.row && c===player.col) continue;
              const d = Math.abs(r-ai.row) + Math.abs(c-ai.col);
              if (d <= 2 && canReachInSteps(ai.row, ai.col, r, c, 2, player)) {
                cands.push({row:r, col:c});
              }
            }
          }
          const t = bestMoveToGoal(cands);
          if (t) doMove(t.row, t.col); else fallback();
          break;
        }
        case 'electric_magnet': {
          // 自分の隣にプレイヤーを引き寄せ（ゴール方向から遠い側）
          const cands = [];
          for (const [dr,dc] of [[-1,0],[1,0],[0,-1],[0,1]]) {
            const nr = ai.row+dr, nc = ai.col+dc;
            if (nr>=0 && nr<BOARD_SIZE && nc>=0 && nc<BOARD_SIZE && !(nr===player.row && nc===player.col)) {
              cands.push({row:nr, col:nc});
            }
          }
          let best = null, bestScore = -Infinity;
          for (const c of cands) {
            const p = findShortestPath(c.row, c.col, BOARD_SIZE-1);
            const d = p ? p.length : 999;
            if (d > bestScore) { bestScore = d; best = c; }
          }
          if (best) doMoveOther(best.row, best.col); else fallback();
          break;
        }
        // 壁操作系: AIは壁カードを簡易的に処理
        case 'sumo_teppou':
        case 'construction_move':
        case 'construction_rotate':
        case 'construction_blast': {
          if (gameState.walls.length > 0) {
            // プレイヤーの経路上にある壁を操作（簡易: 最初の壁をターゲット）
            const targetWall = gameState.walls[0];
            if (cardId === 'construction_blast' || cardId === 'walldestroy') {
              showCardEffect(cardId, () => {
                const idx = gameState.walls.findIndex(w =>
                  w.cornerRow === targetWall.cornerRow && w.cornerCol === targetWall.cornerCol
                );
                if (idx !== -1) gameState.walls.splice(idx, 1);
                useCard();
                renderWalls();
                updateUI();
                afterCardAction(cardId);
              });
            } else {
              // スライド/回転はスキップ（AI壁操作は難しいので移動にフォールバック）
              fallback();
            }
          } else fallback();
          break;
        }
        default:
          fallback();
      }
    }

    // 有効な移動先を取得
    function getValidMoves(playerNum) {
      const player = gameState.players[playerNum];
      const other = gameState.players[playerNum === 1 ? 2 : 1];
      const moves = [];
      const directions = [[-1, 0], [1, 0], [0, -1], [0, 1]];
      
      for (const [dr, dc] of directions) {
        const nr = player.row + dr;
        const nc = player.col + dc;
        
        if (nr >= 0 && nr < BOARD_SIZE && nc >= 0 && nc < BOARD_SIZE) {
          if (!isWallBetween(player.row, player.col, nr, nc)) {
            if (!(nr === other.row && nc === other.col)) {
              moves.push({ row: nr, col: nc });
            }
          }
        }
      }
      
      return moves;
    }

    // 有効な壁配置を取得
    function getValidWallPlacements() {
      const walls = [];
      
      for (let r = 1; r < GRID_SIZE; r += 2) {
        for (let c = 1; c < GRID_SIZE; c += 2) {
          for (const orientation of ['h', 'v']) {
            const wall = { cornerRow: r, cornerCol: c, orientation };
            if (canPlaceWall(wall)) {
              walls.push(wall);
            }
          }
        }
      }
      
      return walls;
    }

    // 最良の壁を探す（全候補をスコアリング・難易度別）
    function findBestWall() {
      const ai = gameState.players[2];
      const player = gameState.players[1];
      const walls = getValidWallPlacements();
      if (walls.length === 0) return null;

      const playerPath = findShortestPath(player.row, player.col, BOARD_SIZE - 1);
      const aiPath = findShortestPath(ai.row, ai.col, 0);
      const currentPlayerDist = playerPath ? playerPath.length : 999;
      const currentAiDist = aiPath ? aiPath.length : 999;

      // 上級以上: 相手の現在経路のエッジセットを構築（直撃ボーナス用）
      const playerEdgeSet = new Set();
      if (playerPath && aiDifficulty === 'hard') {
        for (let i = 0; i < playerPath.length - 1; i++) {
          const from = playerPath[i], to = playerPath[i + 1];
          const edgeRow = from.row * 2 + (to.row - from.row);
          const edgeCol = from.col * 2 + (to.col - from.col);
          playerEdgeSet.add(`${edgeRow},${edgeCol}`);
        }
      }

      let bestWall = null;
      let bestScore = -Infinity;

      for (const wall of walls) {
        gameState.walls.push({ ...wall, owner: 2 });

        const newPlayerPath = findShortestPath(player.row, player.col, BOARD_SIZE - 1);
        const newAiPath = findShortestPath(ai.row, ai.col, 0);
        const newPlayerDist = newPlayerPath ? newPlayerPath.length : 999;
        const newAiDist = newAiPath ? newAiPath.length : 999;

        gameState.walls.pop();

        // 経路完全遮断は避ける（どちらかのパスがなくなる壁は除外）
        if (!newPlayerPath || !newAiPath) continue;

        const playerIncrease = newPlayerDist - currentPlayerDist;
        const aiIncrease = newAiDist - currentAiDist;
        let score = playerIncrease * 3 - aiIncrease * 2;

        // 上級以上: 相手の現在の最短経路を直接塞ぐ壁にボーナス
        if (aiDifficulty === 'hard' && playerEdgeSet.size > 0) {
          const edges = getBlockedEdges(wall);
          for (const e of edges) {
            if (playerEdgeSet.has(`${e.row},${e.col}`)) {
              score += playerIncrease * 2;
              break;
            }
          }
        }

        // ボスねこ+: 学習データボーナス
        if (aiLearningData.trained) {
          const key = `${wall.cornerRow},${wall.cornerCol},${wall.orientation}`;
          const learnScore = aiLearningData.goodWallPositions[key] || 0;
          if (learnScore > 0) {
            score += Math.min(learnScore / 500, 5);
          }
        }

        if (score > bestScore) {
          bestScore = score;
          bestWall = wall;
        }
      }

      // 効果が薄い壁は置かない
      if (bestScore <= 0) return null;
      return bestWall;
    }

    // 最短経路を探す（BFS）
    function findShortestPath(startRow, startCol, goalRow) {
      const queue = [[{ row: startRow, col: startCol }]];
      const visited = new Set([`${startRow},${startCol}`]);
      
      while (queue.length > 0) {
        const path = queue.shift();
        const current = path[path.length - 1];
        
        if (current.row === goalRow) {
          return path;
        }
        
        const directions = [[-1, 0], [1, 0], [0, -1], [0, 1]];
        for (const [dr, dc] of directions) {
          const nr = current.row + dr;
          const nc = current.col + dc;
          const key = `${nr},${nc}`;
          
          if (nr >= 0 && nr < BOARD_SIZE && nc >= 0 && nc < BOARD_SIZE && !visited.has(key)) {
            if (!isWallBetween(current.row, current.col, nr, nc)) {
              visited.add(key);
              queue.push([...path, { row: nr, col: nc }]);
            }
          }
        }
      }
      
      return null;
    }

    function initBoard() {
      const board = document.getElementById('board');
      board.innerHTML = '';
      board.style.gridTemplateColumns = `repeat(${GRID_SIZE}, auto)`;
      board.style.gridTemplateRows = `repeat(${GRID_SIZE}, auto)`;

      for (let row = 0; row < GRID_SIZE; row++) {
        for (let col = 0; col < GRID_SIZE; col++) {
          const cell = document.createElement('div');
          cell.className = 'cell';
          cell.dataset.row = row;
          cell.dataset.col = col;

          const isFloorRow = row % 2 === 0;
          const isFloorCol = col % 2 === 0;

          if (isFloorRow && isFloorCol) {
            // 床マス
            cell.classList.add('floor');
            const boardRow = row / 2;
            const boardCol = col / 2;
            
            if (boardRow === 0) {
              cell.classList.add('goal2');
            } else if (boardRow === BOARD_SIZE - 1) {
              cell.classList.add('goal1');
            }
            
            // タッチとクリック両方に対応（即座に反応）
            cell.addEventListener('touchstart', (e) => {
              e.preventDefault();
              handleFloorClick(boardRow, boardCol);
            }, { passive: false });
            cell.onclick = () => handleFloorClick(boardRow, boardCol);
          } else if (isFloorRow && !isFloorCol) {
            // 縦壁スロット
            cell.classList.add('wall-slot', 'vertical');
          } else if (!isFloorRow && isFloorCol) {
            // 横壁スロット
            cell.classList.add('wall-slot', 'horizontal');
          } else {
            // コーナー（壁の配置起点）
            cell.classList.add('wall-slot', 'corner');
            cell.dataset.cornerRow = row;
            cell.dataset.cornerCol = col;
          }

          board.appendChild(cell);
        }
      }

      renderCats();
      renderWalls();
      updateUI();
      highlightValidMoves();
      
      // ボード全体のイベント（座標ベースで最寄りコーナーを検出）
      const boardEl = document.getElementById('board');
      
      // 既存のイベントリスナーを除去するためclone
      const newBoard = boardEl.cloneNode(true);
      boardEl.parentNode.replaceChild(newBoard, boardEl);
      
      // 床マスのイベントを再登録
      newBoard.querySelectorAll('.cell.floor').forEach(cell => {
        const boardRow = parseInt(cell.dataset.row) / 2;
        const boardCol = parseInt(cell.dataset.col) / 2;
        cell.addEventListener('touchstart', (e) => {
          e.preventDefault();
          handleFloorClick(boardRow, boardCol);
        }, { passive: false });
        cell.onclick = () => handleFloorClick(boardRow, boardCol);
      });
      
      // ボード全体でタッチ/クリック → 最寄りコーナー検出
      newBoard.addEventListener('touchstart', (e) => {
        const result = findNearestCorner(e.touches[0], newBoard);
        if (result) {
          e.preventDefault();
          handleWallDragStart(result.row, result.col);
        }
      }, { passive: false });
      
      newBoard.addEventListener('mousedown', (e) => {
        if (e.target.classList.contains('floor') || e.target.classList.contains('cat')) return;
        const result = findNearestCorner(e, newBoard);
        if (result) {
          e.preventDefault();
          handleWallDragStart(result.row, result.col);
        }
      });
      
      newBoard.addEventListener('touchmove', handleWallDragMove, { passive: false });
      newBoard.addEventListener('mousemove', handleWallDragMove);
      newBoard.addEventListener('touchend', handleWallDragEnd);
      newBoard.addEventListener('mouseup', handleWallDragEnd);
      newBoard.addEventListener('mouseleave', handleWallDragEnd);
    }

    // タッチ/クリック座標から最も近いコーナーを見つける
    function findNearestCorner(pointer, boardEl) {
      const rect = boardEl.getBoundingClientRect();
      const x = pointer.clientX - rect.left;
      const y = pointer.clientY - rect.top;
      
      // 各セルの実際のサイズを計算
      const cellW = rect.width / GRID_SIZE;
      const cellH = rect.height / GRID_SIZE;
      
      // グリッド上の連続座標
      const gridX = x / cellW;
      const gridY = y / cellH;
      
      // 最寄りのコーナー（奇数行・奇数列）を探索
      let bestCorner = null;
      let bestDist = Infinity;
      
      // 周辺の4つのコーナー候補をチェック
      const baseR = Math.floor(gridY / 2) * 2 + 1;
      const baseC = Math.floor(gridX / 2) * 2 + 1;
      
      for (let dr = -2; dr <= 2; dr += 2) {
        for (let dc = -2; dc <= 2; dc += 2) {
          const cr = baseR + dr;
          const cc = baseC + dc;
          
          if (cr <= 0 || cr >= GRID_SIZE - 1) continue;
          if (cc <= 0 || cc >= GRID_SIZE - 1) continue;
          
          // コーナーの中心座標
          const cornerCenterX = (cc + 0.5) * cellW;
          const cornerCenterY = (cr + 0.5) * cellH;
          
          const dist = Math.sqrt(
            Math.pow(x - cornerCenterX, 2) + 
            Math.pow(y - cornerCenterY, 2)
          );
          
          // タップがコーナーの近く（セルサイズの0.8倍以内）にある場合のみ
          const threshold = Math.max(cellW, cellH) * 0.9;
          
          if (dist < threshold && dist < bestDist) {
            bestDist = dist;
            bestCorner = { row: cr, col: cc };
          }
        }
      }
      
      return bestCorner;
    }

    let lastCatPositions = { 1: null, 2: null };

    function renderCats(movedPlayer) {
      document.querySelectorAll('.cat').forEach(el => el.remove());

      for (let p = 1; p <= 2; p++) {
        const player = gameState.players[p];
        const gridRow = player.row * 2;
        const gridCol = player.col * 2;
        const cell = document.querySelector(`.cell[data-row="${gridRow}"][data-col="${gridCol}"]`);

        if (cell) {
          const cat = document.createElement('span');
          cat.className = 'cat ' + (p === 1 ? 'cat-down' : 'cat-up');
          cat.textContent = player.emoji;
          cat.style.color = player.color;
          if (p === 1) {
            cat.style.textShadow = '0 0 3px #fff, 0 0 5px #fff';
          } else {
            cat.style.textShadow = '0 0 3px #000, 0 0 5px #000';
          }
          // 移動時のバウンスアニメーション
          if (p === movedPlayer) {
            cat.classList.add('moving');
            setTimeout(() => cat.classList.remove('moving'), 350);
          }
          cell.appendChild(cat);
        }
        lastCatPositions[p] = { row: player.row, col: player.col };
      }
    }

    function renderWalls() {
      // 壁表示をクリア
      document.querySelectorAll('.cell.wall-slot').forEach(cell => {
        cell.classList.remove('has-wall', 'wall-player1', 'wall-player2', 'highlight-wall', 'preview', 'preview-player1', 'preview-player2', 'preview-active', 'preview-invalid', 'invalid');
      });

      // 既存の壁を表示
      gameState.walls.forEach(wall => {
        const cells = getWallCells(wall);
        cells.forEach(({row, col}) => {
          const cell = document.querySelector(`.cell[data-row="${row}"][data-col="${col}"]`);
          if (cell) {
            cell.classList.add('has-wall');
            cell.classList.add(`wall-player${wall.owner}`);
          }
        });
      });

      // プレビュー表示
      if (gameState.previewWall) {
        const isInvalid = gameState.previewWall.invalid;
        const cells = getWallCells(gameState.previewWall);
        cells.forEach(({row, col}) => {
          const cell = document.querySelector(`.cell[data-row="${row}"][data-col="${col}"]`);
          if (cell && !cell.classList.contains('has-wall')) {
            cell.classList.add('preview');
            if (isInvalid) {
              cell.classList.add('preview-invalid');
            } else {
              cell.classList.add(`preview-player${gameState.currentPlayer}`);
            }
          }
        });
        
        // 交差点を膨らませる
        const cornerCell = document.querySelector(`.cell[data-row="${gameState.previewWall.cornerRow}"][data-col="${gameState.previewWall.cornerCol}"]`);
        if (cornerCell) {
          cornerCell.classList.add('preview-active');
          if (isInvalid) {
            cornerCell.classList.add('invalid');
          }
        }
      }
    }

    // 壁が占めるセル（コーナー + 2つの壁スロット）
    function getWallCells(wall) {
      const {cornerRow, cornerCol, orientation} = wall;
      const cells = [{row: cornerRow, col: cornerCol}];
      
      if (orientation === 'h') {
        // 横壁: コーナーの左右の横スロット
        cells.push({row: cornerRow, col: cornerCol - 1});
        cells.push({row: cornerRow, col: cornerCol + 1});
      } else {
        // 縦壁: コーナーの上下の縦スロット
        cells.push({row: cornerRow - 1, col: cornerCol});
        cells.push({row: cornerRow + 1, col: cornerCol});
      }
      
      return cells;
    }

    // 壁がブロックするエッジ（移動を妨げる位置）
    function getBlockedEdges(wall) {
      const {cornerRow, cornerCol, orientation} = wall;
      const edges = [];
      
      if (orientation === 'h') {
        // 横壁は縦移動をブロック
        // コーナーの左右にある2つのエッジ
        edges.push({row: cornerRow, col: cornerCol - 1});
        edges.push({row: cornerRow, col: cornerCol + 1});
      } else {
        // 縦壁は横移動をブロック
        // コーナーの上下にある2つのエッジ
        edges.push({row: cornerRow - 1, col: cornerCol});
        edges.push({row: cornerRow + 1, col: cornerCol});
      }
      
      return edges;
    }

    function isWallBetween(r1, c1, r2, c2) {
      const edgeRow = r1 * 2 + (r2 - r1);
      const edgeCol = c1 * 2 + (c2 - c1);
      
      return gameState.walls.some(wall => {
        const edges = getBlockedEdges(wall);
        return edges.some(e => e.row === edgeRow && e.col === edgeCol);
      });
    }

    function handleFloorClick(boardRow, boardCol) {
      if (gameState.gameOver || animating) return;

      // カードフェーズの処理
      if (gameState.cardPhase) {
        handleCardFloorClick(boardRow, boardCol);
        return;
      }

      if (gameState.mode !== 'move') return;

      const player = gameState.players[gameState.currentPlayer];
      
      // 足止め中は移動不可
      if (player.frozen) {
        showToast('足止めされていて移動できないよ！壁かカードを使おう', 'warn');
        return;
      }
      const dr = Math.abs(boardRow - player.row);
      const dc = Math.abs(boardCol - player.col);

      if ((dr === 1 && dc === 0) || (dr === 0 && dc === 1)) {
        if (!isWallBetween(player.row, player.col, boardRow, boardCol)) {
          const otherPlayer = gameState.currentPlayer === 1 ? 2 : 1;
          const other = gameState.players[otherPlayer];
          
          if (boardRow === other.row && boardCol === other.col) {
            return;
          }

          // 履歴を保存
          saveHistory();

          player.row = boardRow;
          player.col = boardCol;

          renderCats(gameState.currentPlayer);
          checkWin();

          if (!gameState.gameOver) {
            // Move OK後の移動ならターン終了
            if (gameState.pendingMoveOk) {
              gameState.pendingMoveOk = false;
            }
            switchPlayer();
          }
        }
      }
    }

    // 壁ドラッグ開始
    function handleWallDragStart(cornerRow, cornerCol) {
      if (gameState.gameOver) return;
      if (gameState.cardPhase) {
        handleCardWallClick(cornerRow, cornerCol);
        return;
      }
      if (gameState.mode !== 'wall') return;
      
      const player = gameState.players[gameState.currentPlayer];
      if (player.walls <= 0) return;
      
      // 有効なコーナーかチェック
      if (cornerRow % 2 === 0 || cornerCol % 2 === 0) return;
      if (cornerRow <= 0 || cornerRow >= GRID_SIZE - 1) return;
      if (cornerCol <= 0 || cornerCol >= GRID_SIZE - 1) return;
      
      // プレビュー中の壁と同じコーナーをタップ → 確定
      if (gameState.previewWall && 
          gameState.previewWall.cornerRow === cornerRow && 
          gameState.previewWall.cornerCol === cornerCol &&
          !gameState.previewWall.invalid) {
        if (canPlaceWall(gameState.previewWall)) {
          saveHistory();
          placeWall(gameState.previewWall);
          player.walls--;
          gameState.previewWall = null;
          updateUI();
          switchPlayer();
          return;
        }
      }
      
      // 新しいコーナーをタップ → プレビュー表示（ドラッグ開始）
      wallDragState.isDragging = true;
      wallDragState.currentCorner = { row: cornerRow, col: cornerCol };
      
      updateWallPreview(cornerRow, cornerCol);
    }

    // 壁ドラッグ中（移動）
    function handleWallDragMove(e) {
      if (!wallDragState.isDragging) return;
      if (gameState.mode !== 'wall') return;
      
      e.preventDefault();
      
      const pointer = e.touches ? e.touches[0] : e;
      const boardEl = e.currentTarget;
      const result = findNearestCorner(pointer, boardEl);
      
      if (!result) return;
      
      // 現在のコーナーと違う場合のみ更新
      if (!wallDragState.currentCorner || 
          wallDragState.currentCorner.row !== result.row || 
          wallDragState.currentCorner.col !== result.col) {
        wallDragState.currentCorner = { row: result.row, col: result.col };
        updateWallPreview(result.row, result.col);
      }
    }

    // 壁ドラッグ終了
    function handleWallDragEnd() {
      if (!wallDragState.isDragging) return;
      
      wallDragState.isDragging = false;
      wallDragState.currentCorner = null;
      
      // ドラッグ終了時は確定せず、プレビューを維持
      // 無効な場所だった場合のみプレビューをクリア
      if (gameState.previewWall && gameState.previewWall.invalid) {
        gameState.previewWall = null;
        renderWalls();
        highlightValidMoves();
      }
    }

    // 壁プレビューを更新
    function updateWallPreview(cornerRow, cornerCol) {
      const orientation = gameState.wallOrientation || 'h';
      let newWall = { cornerRow, cornerCol, orientation };
      
      if (canPlaceWall(newWall)) {
        gameState.previewWall = newWall;
      } else {
        // 反対の向きを試す
        const altOrientation = orientation === 'h' ? 'v' : 'h';
        newWall.orientation = altOrientation;
        if (canPlaceWall(newWall)) {
          gameState.previewWall = newWall;
        } else {
          // 置けない場所
          gameState.previewWall = { cornerRow, cornerCol, orientation, invalid: true };
        }
      }
      
      renderWalls();
    }

    function handleCornerClick(cornerRow, cornerCol) {
      if (gameState.gameOver) return;

      // カードフェーズの壁操作
      if (gameState.cardPhase) {
        handleCardWallClick(cornerRow, cornerCol);
        return;
      }

      if (gameState.mode !== 'wall') return;

      const player = gameState.players[gameState.currentPlayer];
      if (player.walls <= 0) return;

      // コーナーは奇数行・奇数列のみ有効
      if (cornerRow % 2 === 0 || cornerCol % 2 === 0) return;
      
      // 端すぎるコーナーは無効
      if (cornerRow <= 0 || cornerRow >= GRID_SIZE - 1) return;
      if (cornerCol <= 0 || cornerCol >= GRID_SIZE - 1) return;

      // プレビュー中の壁と同じコーナーをクリック → 確定
      if (gameState.previewWall && 
          gameState.previewWall.cornerRow === cornerRow && 
          gameState.previewWall.cornerCol === cornerCol) {
        if (canPlaceWall(gameState.previewWall)) {
          // 履歴を保存
          saveHistory();
          placeWall(gameState.previewWall);
          player.walls--;
          gameState.previewWall = null;
          updateUI();
          switchPlayer();
          return;
        } else {
          showToast('ここには壁を置けないよ！', 'warn');
          gameState.previewWall = null;
          renderWalls();
          return;
        }
      }
      
      // 新しいコーナーをクリック → プレビュー表示
      const orientation = gameState.wallOrientation || 'h';
      const newWall = {cornerRow, cornerCol, orientation};
      
      if (canPlaceWall(newWall)) {
        gameState.previewWall = newWall;
      } else {
        // 現在の向きで置けない場合、反対の向きを試す
        const altOrientation = orientation === 'h' ? 'v' : 'h';
        newWall.orientation = altOrientation;
        if (canPlaceWall(newWall)) {
          gameState.previewWall = newWall;
        } else {
          showToast('ここには壁を置けないよ！', 'warn');
          return;
        }
      }
      
      renderWalls();
    }
    
    function canPlaceWall(wall) {
      // 既存の壁と重なっていないか
      const newCells = getWallCells(wall);
      for (const existingWall of gameState.walls) {
        const existingCells = getWallCells(existingWall);
        for (const nc of newCells) {
          for (const ec of existingCells) {
            if (nc.row === ec.row && nc.col === ec.col) {
              return false;
            }
          }
        }
      }
      
      // 一時的に壁を追加して経路をチェック
      gameState.walls.push(wall);
      const canReach = canBothPlayersReachGoal();
      gameState.walls.pop();
      
      return canReach;
    }

    function placeWall(wall) {
      gameState.walls.push({...wall, owner: gameState.currentPlayer});
      renderWalls();
    }

    function canBothPlayersReachGoal() {
      return canReachGoal(1) && canReachGoal(2);
    }

    function canReachGoal(playerNum) {
      const player = gameState.players[playerNum];
      const goalRow = playerNum === 1 ? BOARD_SIZE - 1 : 0;
      
      const visited = new Set();
      const queue = [[player.row, player.col]];
      visited.add(`${player.row},${player.col}`);

      const directions = [[-1, 0], [1, 0], [0, -1], [0, 1]];

      while (queue.length > 0) {
        const [r, c] = queue.shift();
        
        if (r === goalRow) return true;

        for (const [dr, dc] of directions) {
          const nr = r + dr;
          const nc = c + dc;
          
          if (nr >= 0 && nr < BOARD_SIZE && nc >= 0 && nc < BOARD_SIZE) {
            const key = `${nr},${nc}`;
            if (!visited.has(key) && !isWallBetween(r, c, nr, nc)) {
              visited.add(key);
              queue.push([nr, nc]);
            }
          }
        }
      }

      return false;
    }

    function handleCardFloorClick(boardRow, boardCol) {
      const cardId = gameState.selectedCard;
      const player = gameState.players[gameState.currentPlayer];
      const otherNum = gameState.currentPlayer === 1 ? 2 : 1;
      const other = gameState.players[otherNum];
      const pNum = gameState.currentPlayer;

      // === にんじゃねこ: 忍び足（斜め1マス移動） ===
      if (cardId === 'ninja_shinobi') {
        const dr = boardRow - player.row;
        const dc = boardCol - player.col;
        if (Math.abs(dr) === 1 && Math.abs(dc) === 1) {
          if (boardRow >= 0 && boardRow < BOARD_SIZE && boardCol >= 0 && boardCol < BOARD_SIZE) {
            if (!(boardRow === other.row && boardCol === other.col)) {
              // 斜め壁チェック: 縦→横 の経路で壁がないか
              if (!isWallBetween(player.row, player.col, player.row + dr, player.col) &&
                  !isWallBetween(player.row + dr, player.col, boardRow, boardCol)) {
                saveHistory();
                showCardEffect(cardId, () => {
                  player.row = boardRow;
                  player.col = boardCol;
                  useCard();
                  renderCats(pNum);
                  checkWin();
                  if (!gameState.gameOver) afterCardAction(cardId);
                });
                return;
              }
            }
          }
        }
      }

      // === にんじゃねこ: ムササビの術（相手を飛び越える） ===
      if (cardId === 'ninja_musasabi') {
        const dr = boardRow - player.row;
        const dc = boardCol - player.col;
        if ((Math.abs(dr) === 2 && dc === 0) || (dr === 0 && Math.abs(dc) === 2)) {
          const midRow = player.row + dr / 2;
          const midCol = player.col + dc / 2;
          if (midRow === other.row && midCol === other.col) {
            if (!isWallBetween(player.row, player.col, midRow, midCol) &&
                !isWallBetween(midRow, midCol, boardRow, boardCol)) {
              saveHistory();
              showCardEffect(cardId, () => {
                player.row = boardRow;
                player.col = boardCol;
                useCard();
                renderCats(pNum);
                checkWin();
                if (!gameState.gameOver) afterCardAction(cardId);
              });
              return;
            }
          }
        }
      }

      // === どろぼうねこ: ずらかる（横2マス移動、壁無視） ===
      if (cardId === 'thief_zurakaru') {
        const dr = boardRow - player.row;
        const dc = boardCol - player.col;
        if (dr === 0 && Math.abs(dc) === 2) {
          if (boardCol >= 0 && boardCol < BOARD_SIZE) {
            if (!(boardRow === other.row && boardCol === other.col)) {
              saveHistory();
              showCardEffect(cardId, () => {
                player.row = boardRow;
                player.col = boardCol;
                useCard();
                renderCats(pNum);
                checkWin();
                if (!gameState.gameOver) afterCardAction(cardId);
              });
              return;
            }
          }
        }
      }

      // === すもうねこ: 押し出し（隣の相手を1マス押す） ===
      if (cardId === 'sumo_oshidashi') {
        if (boardRow === other.row && boardCol === other.col) {
          const dr = other.row - player.row;
          const dc = other.col - player.col;
          if ((Math.abs(dr) === 1 && dc === 0) || (dr === 0 && Math.abs(dc) === 1)) {
            const pushRow = other.row + dr;
            const pushCol = other.col + dc;
            if (pushRow >= 0 && pushRow < BOARD_SIZE && pushCol >= 0 && pushCol < BOARD_SIZE) {
              if (!isWallBetween(player.row, player.col, other.row, other.col) &&
                  !isWallBetween(other.row, other.col, pushRow, pushCol)) {
                saveHistory();
                showCardEffect(cardId, () => {
                  other.row = pushRow;
                  other.col = pushCol;
                  useCard();
                  renderCats();
                  checkWin();
                  if (!gameState.gameOver) afterCardAction(cardId);
                });
                return;
              }
            }
          }
        }
      }

      // === すもうねこ: 上手投げ（隣の相手を反対側に2マス投げる） ===
      if (cardId === 'sumo_uwatenage') {
        if (boardRow === other.row && boardCol === other.col) {
          const dr = other.row - player.row;
          const dc = other.col - player.col;
          if ((Math.abs(dr) === 1 && dc === 0) || (dr === 0 && Math.abs(dc) === 1)) {
            // 反対側に2マス = player側に2マス
            let throwRow = player.row - dr;
            let throwCol = player.col - dc;
            // 範囲外ならクリップ
            throwRow = Math.max(0, Math.min(BOARD_SIZE - 1, throwRow));
            throwCol = Math.max(0, Math.min(BOARD_SIZE - 1, throwCol));
            // 投げ先にプレイヤーがいたら1マス手前に
            if (throwRow === player.row && throwCol === player.col) {
              throwRow = player.row - dr;
              throwCol = player.col - dc;
              if (throwRow < 0 || throwRow >= BOARD_SIZE || throwCol < 0 || throwCol >= BOARD_SIZE) return;
            }
            saveHistory();
            showCardEffect(cardId, () => {
              other.row = throwRow;
              other.col = throwCol;
              useCard();
              renderCats();
              checkWin();
              if (!gameState.gameOver) afterCardAction(cardId);
            });
            return;
          }
        }
      }

      // === まじょねこ: テレポート（同じ行の好きなマスへ移動、壁無視、入れ替わり） ===
      if (cardId === 'witch_teleport') {
        if (boardRow === player.row && boardCol !== player.col) {
          saveHistory();
          showCardEffect(cardId, () => {
            if (boardRow === other.row && boardCol === other.col) {
              // 入れ替わり
              other.row = player.row;
              other.col = player.col;
            }
            player.row = boardRow;
            player.col = boardCol;
            useCard();
            renderCats(pNum);
            checkWin();
            if (!gameState.gameOver) afterCardAction(cardId);
          });
          return;
        }
      }

      // === ゆうれいねこ: ゴーストウォーク（壁を1枚すり抜けて隣に移動） ===
      if (cardId === 'ghost_walk') {
        const dr = boardRow - player.row;
        const dc = boardCol - player.col;
        if ((Math.abs(dr) === 1 && dc === 0) || (dr === 0 && Math.abs(dc) === 1)) {
          if (boardRow >= 0 && boardRow < BOARD_SIZE && boardCol >= 0 && boardCol < BOARD_SIZE) {
            if (!(boardRow === other.row && boardCol === other.col)) {
              // 壁があってもなくても移動可能（ゴーストウォーク）
              saveHistory();
              showCardEffect(cardId, () => {
                player.row = boardRow;
                player.col = boardCol;
                useCard();
                renderCats(pNum);
                checkWin();
                if (!gameState.gameOver) afterCardAction(cardId);
              });
              return;
            }
          }
        }
      }

      // === ゆうれいねこ: 憑依（相手を1マス自由に動かす） ===
      // === はかせねこ: リモート操作（2マス以内の相手を1マス動かす） ===
      if (cardId === 'ghost_hyoui' || cardId === 'prof_remote') {
        // クリック先が相手の隣接マスか（相手を移動させる先）
        const dr = boardRow - other.row;
        const dc = boardCol - other.col;
        if ((Math.abs(dr) === 1 && dc === 0) || (dr === 0 && Math.abs(dc) === 1)) {
          if (boardRow >= 0 && boardRow < BOARD_SIZE && boardCol >= 0 && boardCol < BOARD_SIZE) {
            if (!(boardRow === player.row && boardCol === player.col)) {
              if (!isWallBetween(other.row, other.col, boardRow, boardCol)) {
                saveHistory();
                showCardEffect(cardId, () => {
                  other.row = boardRow;
                  other.col = boardCol;
                  useCard();
                  renderCats();
                  checkWin();
                  if (!gameState.gameOver) afterCardAction(cardId);
                });
                return;
              }
            }
          }
        }
      }

      // === でんきねこ: 電光石火（自由に2マス移動、壁は越えられない） ===
      if (cardId === 'electric_raiden') {
        // 2マス以内で壁なしパスが存在するか
        const dist = Math.abs(boardRow - player.row) + Math.abs(boardCol - player.col);
        if (dist >= 1 && dist <= 2 && !(boardRow === other.row && boardCol === other.col)) {
          // パス検証: BFSで2歩以内に到達可能か
          const canReach = canReachInSteps(player.row, player.col, boardRow, boardCol, 2, other);
          if (canReach) {
            saveHistory();
            showCardEffect(cardId, () => {
              player.row = boardRow;
              player.col = boardCol;
              useCard();
              renderCats(pNum);
              checkWin();
              if (!gameState.gameOver) afterCardAction(cardId);
            });
            return;
          }
        }
      }

      // === でんきねこ: 超電導マグネット（2マス以内の相手を隣に引き寄せる） ===
      if (cardId === 'electric_magnet') {
        // ターゲットはプレイヤーの隣接マス
        const dr = boardRow - player.row;
        const dc = boardCol - player.col;
        if ((Math.abs(dr) === 1 && dc === 0) || (dr === 0 && Math.abs(dc) === 1)) {
          const distToOther = Math.abs(player.row - other.row) + Math.abs(player.col - other.col);
          if (distToOther <= 2 && distToOther > 0) {
            // 相手を引き寄せ先に移動（壁チェックは相手→引き寄せ先）
            if (!isWallBetween(other.row, other.col, boardRow, boardCol) || distToOther === 1) {
              saveHistory();
              showCardEffect(cardId, () => {
                other.row = boardRow;
                other.col = boardCol;
                useCard();
                renderCats();
                checkWin();
                if (!gameState.gameOver) afterCardAction(cardId);
              });
              return;
            }
          }
        }
      }
    }

    // 電光石火用: BFSで指定歩数以内に到達可能か
    function canReachInSteps(fromRow, fromCol, toRow, toCol, maxSteps, other) {
      const queue = [{ row: fromRow, col: fromCol, steps: 0 }];
      const visited = new Set();
      visited.add(`${fromRow},${fromCol}`);
      while (queue.length > 0) {
        const { row, col, steps } = queue.shift();
        if (row === toRow && col === toCol) return true;
        if (steps >= maxSteps) continue;
        for (const [dr, dc] of [[-1,0],[1,0],[0,-1],[0,1]]) {
          const nr = row + dr, nc = col + dc;
          if (nr < 0 || nr >= BOARD_SIZE || nc < 0 || nc >= BOARD_SIZE) continue;
          if (visited.has(`${nr},${nc}`)) continue;
          if (nr === other.row && nc === other.col) continue;
          if (isWallBetween(row, col, nr, nc)) continue;
          visited.add(`${nr},${nc}`);
          queue.push({ row: nr, col: nc, steps: steps + 1 });
        }
      }
      return false;
    }

    function handleCardWallClick(cornerRow, cornerCol) {
      const cardId = gameState.selectedCard;

      // スライド先選択フェーズ: タップした交差点が移動先
      if (gameState.cardPhase === 'slideSelectDest') {
        executeSlideToCorner(cornerRow, cornerCol);
        return;
      }

      // 壁を特定
      const wallIndex = gameState.walls.findIndex(w =>
        w.cornerRow === cornerRow && w.cornerCol === cornerCol
      );

      if (wallIndex === -1) return;
      const wall = gameState.walls[wallIndex];

      // スライド系カード: sumo_teppou, construction_move
      if (cardId === 'sumo_teppou' || cardId === 'construction_move' || cardId === 'slide') {
        gameState.slideTargetWall = wall;
        gameState.slideCardId = cardId;
        // 移動可能な交差点をハイライトして選択させる
        gameState.cardPhase = 'slideSelectDest';
        highlightSlideDestinations(wall);
      }
      // 回転系カード: construction_rotate, rotate
      else if (cardId === 'construction_rotate' || cardId === 'rotate') {
        const newOrientation = wall.orientation === 'h' ? 'v' : 'h';
        const newWall = {
          cornerRow: wall.cornerRow,
          cornerCol: wall.cornerCol,
          orientation: newOrientation,
          owner: wall.owner
        };

        gameState.walls.splice(wallIndex, 1);

        if (canPlaceWall(newWall)) {
          const savedWall = {...wall};
          showCardEffect(cardId, () => {
            animateRotate(savedWall, () => {
              gameState.walls.push(newWall);
              useCard();
              renderWalls();
              afterCardAction(cardId);
            });
          });
          return;
        }

        gameState.walls.push(wall);
        showToast('回転できないよ！', 'warn');
      }
      // 壁破壊系カード: construction_blast, walldestroy
      else if (cardId === 'construction_blast' || cardId === 'walldestroy') {
        const savedWall = {...wall};
        showCardEffect(cardId, () => {
          animateWallDestroy(savedWall, () => {
            const idx = gameState.walls.findIndex(w =>
              w.cornerRow === savedWall.cornerRow && w.cornerCol === savedWall.cornerCol
            );
            if (idx !== -1) gameState.walls.splice(idx, 1);
            useCard();
            renderWalls();
            updateUI();
            afterCardAction(cardId);
          });
        });
      }
      // 壁回収(旧): recover
      else if (cardId === 'recover') {
        if (wall.owner !== gameState.currentPlayer) {
          showToast('自分がおいた壁しか回収できないよ！', 'warn');
          return;
        }
        const savedWall = {...wall};
        showCardEffect(cardId, () => {
          animateRecover(savedWall, () => {
            const idx = gameState.walls.findIndex(w =>
              w.cornerRow === savedWall.cornerRow && w.cornerCol === savedWall.cornerCol
            );
            if (idx !== -1) gameState.walls.splice(idx, 1);
            gameState.players[gameState.currentPlayer].walls++;
            useCard();
            renderWalls();
            updateUI();
            switchPlayer();
          });
        });
      }
    }

    function openSlideModal() {
      const wall = gameState.slideTargetWall;
      if (!wall) return;

      // 各方向にスライドできるかチェックしてボタンを有効/無効に
      const directions = {
        'up': [-2, 0],
        'down': [2, 0],
        'left': [0, -2],
        'right': [0, 2]
      };

      for (const [dir, [dr, dc]] of Object.entries(directions)) {
        const btn = document.getElementById(`dir-${dir}`);
        const canSlide = checkSlideDirection(wall, dr, dc);
        btn.disabled = !canSlide;
      }

      const slideModal = document.getElementById('slide-modal');
      slideModal.classList.toggle('rotated-for-p1', gameState.currentPlayer === 1 && gameMode !== 'ai');
      slideModal.classList.add('show');
    }

    function closeSlideModal() {
      const slideModal = document.getElementById('slide-modal');
      slideModal.classList.remove('show', 'rotated-for-p1');
      gameState.slideTargetWall = null;
    }

    // スライド可能な交差点をハイライトする
    function highlightSlideDestinations(wall) {
      clearHighlights();
      const directions = [[-2, 0], [2, 0], [0, -2], [0, 2]];
      let hasAny = false;
      for (const [dr, dc] of directions) {
        if (checkSlideDirection(wall, dr, dc)) {
          const destRow = wall.cornerRow + dr;
          const destCol = wall.cornerCol + dc;
          const cell = document.querySelector(`.cell[data-row="${destRow}"][data-col="${destCol}"]`);
          if (cell) {
            cell.classList.add('highlight-wall');
            hasAny = true;
          }
        }
      }
      // 元の壁位置も表示（選択中であることを示す）
      const origCell = document.querySelector(`.cell[data-row="${wall.cornerRow}"][data-col="${wall.cornerCol}"]`);
      if (origCell) origCell.classList.add('highlight-corner');

      if (!hasAny) {
        showToast('この壁はスライドできないよ！', 'warn');
        gameState.cardPhase = 'selectTarget';
        gameState.slideTargetWall = null;
        highlightCardTargets(gameState.selectedCard);
      }
    }

    // ハイライトされた交差点をタップしてスライド実行
    function executeSlideToCorner(cornerRow, cornerCol) {
      const wall = gameState.slideTargetWall;
      if (!wall) return;

      // タップした位置が有効なスライド先か判定
      const dr = cornerRow - wall.cornerRow;
      const dc = cornerCol - wall.cornerCol;
      if (!checkSlideDirection(wall, dr, dc)) {
        // 無効な位置 → 元の壁選択に戻す
        showToast('そこにはスライドできないよ！', 'warn');
        return;
      }

      const newWall = {
        cornerRow: cornerRow,
        cornerCol: cornerCol,
        orientation: wall.orientation,
        owner: wall.owner
      };

      const wallIndex = gameState.walls.findIndex(w =>
        w.cornerRow === wall.cornerRow && w.cornerCol === wall.cornerCol
      );
      if (wallIndex === -1) return;

      gameState.walls.splice(wallIndex, 1);

      if (canPlaceWall(newWall)) {
        const savedOldWall = {...wall};
        const savedNewWall = {...newWall};
        const slideCardId = gameState.slideCardId || 'slide';
        showCardEffect(slideCardId, () => {
          animateSlide(savedOldWall, savedNewWall, () => {
            gameState.walls.push(newWall);
            useCard();
            renderWalls();
            afterCardAction(slideCardId);
          });
        });
      } else {
        gameState.walls.push(wall);
        showToast('そこにはスライドできないよ！', 'warn');
      }
    }

    function checkSlideDirection(wall, dr, dc) {
      // ふすまスライド: 壁の向きに沿った方向のみ移動可能
      // 横壁(h)は左右のみ、縦壁(v)は上下のみ
      if (wall.orientation === 'h' && dr !== 0) return false; // 横壁は上下不可
      if (wall.orientation === 'v' && dc !== 0) return false; // 縦壁は左右不可
      
      const newWall = {
        cornerRow: wall.cornerRow + dr,
        cornerCol: wall.cornerCol + dc,
        orientation: wall.orientation
      };

      // 範囲チェック
      if (newWall.cornerRow <= 0 || newWall.cornerRow >= GRID_SIZE - 1) return false;
      if (newWall.cornerCol <= 0 || newWall.cornerCol >= GRID_SIZE - 1) return false;

      // 移動先のセルに別の壁があるかチェック
      // スライド先の「新しく占有するセル」に壁があったら不可
      const oldCells = getWallCells(wall);
      const newCells = getWallCells(newWall);
      
      // 新しいセルのうち、元のセルと重ならない部分をチェック
      const oldSet = new Set(oldCells.map(c => `${c.row},${c.col}`));
      
      for (const nc of newCells) {
        if (oldSet.has(`${nc.row},${nc.col}`)) continue; // 元と重なる部分はOK
        
        // この位置に別の壁があるか？
        for (const otherWall of gameState.walls) {
          if (otherWall.cornerRow === wall.cornerRow && otherWall.cornerCol === wall.cornerCol) continue; // 自分自身はスキップ
          const otherCells = getWallCells(otherWall);
          for (const oc of otherCells) {
            if (oc.row === nc.row && oc.col === nc.col) return false; // 壁がある→スライド不可
          }
        }
      }

      // 一時的に元の壁を削除してパス確認
      const wallIndex = gameState.walls.findIndex(w => 
        w.cornerRow === wall.cornerRow && w.cornerCol === wall.cornerCol
      );
      if (wallIndex === -1) return false;

      const originalWall = gameState.walls.splice(wallIndex, 1)[0];
      const canPlace = canPlaceWall(newWall);
      gameState.walls.push(originalWall);

      return canPlace;
    }

    function slideWall(direction) {
      const wall = gameState.slideTargetWall;
      if (!wall) return;

      const directions = {
        'up': [-2, 0],
        'down': [2, 0],
        'left': [0, -2],
        'right': [0, 2]
      };

      const [dr, dc] = directions[direction];
      const newWall = {
        cornerRow: wall.cornerRow + dr,
        cornerCol: wall.cornerCol + dc,
        orientation: wall.orientation,
        owner: wall.owner
      };

      const wallIndex = gameState.walls.findIndex(w => 
        w.cornerRow === wall.cornerRow && w.cornerCol === wall.cornerCol
      );
      if (wallIndex === -1) return;

      gameState.walls.splice(wallIndex, 1);

      if (canPlaceWall(newWall)) {
        const savedOldWall = {...wall};
        const savedNewWall = {...newWall};
        const slideCardId = gameState.slideCardId || 'slide';
        closeSlideModal();
        showCardEffect(slideCardId, () => {
          animateSlide(savedOldWall, savedNewWall, () => {
            gameState.walls.push(newWall);
            useCard();
            renderWalls();
            afterCardAction(slideCardId);
          });
        });
      } else {
        gameState.walls.push(wall);
        showToast('そっちにはスライドできないよ！', 'warn');
      }
    }

    function checkWin() {
      const currentPlayer = gameState.players[gameState.currentPlayer];

      // ちゃとらがピンクマス（7行目のどれか）にとまると勝ち
      if (gameState.currentPlayer === 1 && currentPlayer.row === BOARD_SIZE - 1) {
        showWinner(1);
      }
      // くろねこがみどりマス（1行目のどれか）にとまると勝ち
      if (gameState.currentPlayer === 2 && currentPlayer.row === 0) {
        showWinner(2);
      }
    }

    function showWinner(playerNum) {
      gameState.gameOver = true;
      const overlay = document.getElementById('winner-overlay');
      const winnerCat = document.getElementById('winner-cat');
      const winnerText = document.getElementById('winner-text');
      
      const player = gameState.players[playerNum];
      winnerCat.textContent = playerNum === 2 && gameMode === 'ai' ? AI_FACES[aiDifficulty] : player.emoji;
      winnerCat.style.color = player.color;
      
      if (playerNum === 1) {
        winnerText.textContent = gameMode === 'ai' ? 'やったね！きみの かち！🎉' : 'ちゃとら の かち！🎉';
      } else {
        if (gameMode === 'ai') {
          const aiName = aiDifficulty === 'easy' ? 'こねこ' : aiDifficulty === 'medium' ? 'おとなねこ' : 'ボスねこ';
          winnerText.textContent = `${aiName} に まけちゃった...😿`;
        } else {
          winnerText.textContent = 'くろねこ の かち！🎉';
        }
      }
      
      overlay.classList.add('show');
      spawnConfetti();
    }

    function spawnConfetti() {
      const container = document.getElementById('confetti-container');
      container.innerHTML = '';
      const colors = ['#ff6b6b', '#ffd93d', '#6bff6b', '#6bb5ff', '#ff6bda', '#ffb347', '#a855f7', '#00f2fe'];
      const emojis = ['🐾', '⭐', '✨', '🎉', '💫', '🌟'];
      
      for (let i = 0; i < 60; i++) {
        const conf = document.createElement('div');
        const isEmoji = Math.random() < 0.3;
        
        if (isEmoji) {
          conf.textContent = emojis[Math.floor(Math.random() * emojis.length)];
          conf.style.fontSize = (8 + Math.random() * 14) + 'px';
          conf.style.width = 'auto';
          conf.style.height = 'auto';
        } else {
          conf.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
          conf.style.width = (6 + Math.random() * 10) + 'px';
          conf.style.height = (6 + Math.random() * 10) + 'px';
          conf.style.borderRadius = Math.random() > 0.5 ? '50%' : '2px';
        }
        
        conf.className = 'confetti';
        conf.style.left = Math.random() * 100 + '%';
        conf.style.animationDuration = (2 + Math.random() * 3) + 's';
        conf.style.animationDelay = Math.random() * 2 + 's';
        container.appendChild(conf);
      }
    }

    function backToTitle() {
      document.getElementById('winner-overlay').classList.remove('show');
      showModeSelect();
    }

    function switchPlayer() {
      const prevPlayer = gameState.currentPlayer;

      // 足止め状態の処理（frozenTurnsをデクリメント）
      const pp = gameState.players[prevPlayer];
      if (pp.frozenTurns > 0) {
        pp.frozenTurns--;
        if (pp.frozenTurns <= 0) {
          pp.frozen = false;
        }
      }

      // hidden状態をクリア（自分のターン終了時）
      pp.hidden = false;

      // Move OKフラグリセット
      gameState.pendingMoveOk = false;

      gameState.currentPlayer = gameState.currentPlayer === 1 ? 2 : 1;
      gameState.turn++;
      gameState.selectedCard = null;
      gameState.cardPhase = null;
      gameState.previewWall = null;
      setMode('move');

      // 次のプレイヤーの封印カウントダウン
      const nextPlayer = gameState.players[gameState.currentPlayer];
      for (const cid of Object.keys(nextPlayer.sealedCards)) {
        if (nextPlayer.sealedCards[cid] > 0) {
          nextPlayer.sealedCards[cid]--;
          if (nextPlayer.sealedCards[cid] <= 0) {
            delete nextPlayer.sealedCards[cid];
          }
        }
      }

      updateUI();
      highlightValidMoves();

      // AIモードでくろねこのターンならAI行動
      if (gameMode === 'ai' && gameState.currentPlayer === 2 && !gameState.gameOver) {
        aiTurn();
      }
    }

    function setMode(mode) {
      if (gameState.cardPhase) {
        gameState.selectedCard = null;
        gameState.cardPhase = null;
        clearHighlights();
      }
      
      gameState.previewWall = null;
      gameState.mode = mode;
      
      // ちゃとら用ボタン
      document.getElementById('btn-move').classList.toggle('active', mode === 'move');
      document.getElementById('btn-wall-h').classList.toggle('active', mode === 'wall' && gameState.wallOrientation === 'h');
      document.getElementById('btn-wall-v').classList.toggle('active', mode === 'wall' && gameState.wallOrientation === 'v');
      document.getElementById('btn-card').classList.remove('active');
      
      // くろねこ用ボタン
      document.getElementById('btn-move2').classList.toggle('active', mode === 'move');
      document.getElementById('btn-wall-h2').classList.toggle('active', mode === 'wall' && gameState.wallOrientation === 'h');
      document.getElementById('btn-wall-v2').classList.toggle('active', mode === 'wall' && gameState.wallOrientation === 'v');
      document.getElementById('btn-card2').classList.remove('active');
      
      const modeText = mode === 'move' ? 'ねこをうごかす' : 'かべをおく';
      document.getElementById('mode-indicator').textContent = `モード: ${modeText}`;
      document.getElementById('mode-indicator2').textContent = `モード: ${modeText}`;
      
      renderWalls();
      highlightValidMoves();
    }

    function setWallMode(orientation) {
      gameState.wallOrientation = orientation;
      setMode('wall');
    }

    function openCardModal() {
      const player = gameState.players[gameState.currentPlayer];
      const hand = playerHands[gameState.currentPlayer];

      // 手札があればそのカードのみ、なければ全カード
      const availableCards = hand
        ? SKILL_CARDS.filter(c => hand.includes(c.id))
        : SKILL_CARDS;

      // 手札制限チェック（表示はするが全カード使用不可になる）
      const maxCards = hand ? hand.length : MAX_CARDS_PER_PLAYER;
      const allUsed = player.totalCardsUsed >= maxCards;

      const grid = document.getElementById('card-grid');
      grid.innerHTML = '';

      availableCards.forEach(card => {
        const cardEl = document.createElement('div');
        cardEl.className = 'skill-card';

        const usedCount = player.cardsUsed[card.id] || 0;
        const canUseMore = usedCount < card.limit;
        const isSealed = (player.sealedCards[card.id] || 0) > 0;
        const canUse = !allUsed && canUseMore && !isSealed && canUseCard(card.id);

        // カウンターカードは自分のターンには使えない（受動発動のみ）
        const isCounter = card.actionType === 'counter';
        const finalCanUse = canUse && !isCounter;

        if (!finalCanUse) {
          cardEl.classList.add('disabled');
          if (!allUsed && canUseMore && !isSealed && !isCounter) cardEl.classList.add('available');
        }

        const typeIcon = card.actionType === 'move_act' ? '🔵' : card.actionType === 'move_ok' ? '🟢' : card.actionType === 'turn_end' ? '🔴' : '🟣';
        const imgSrc = CARD_IMAGES[card.id];
        let statusText = '';
        if (allUsed || !canUseMore) {
          statusText = '<div class="card-name" style="margin-top:4px;font-size:0.75em;color:#c0392b;">使用済み</div>';
        } else if (isSealed) {
          statusText = `<div class="card-name" style="margin-top:4px;font-size:0.7em;color:#9b59b6;">🤐 封印中（${player.sealedCards[card.id]}ターン）</div>`;
        } else if (isCounter) {
          statusText = '<div class="card-name" style="margin-top:4px;font-size:0.7em;color:#3498db;">🟣 カウンター（自動発動）</div>';
        } else if (!canUse) {
          statusText = '<div class="card-name" style="margin-top:4px;font-size:0.7em;color:#7f8c8d;">条件を みたしていない</div>';
        }

        if (imgSrc) {
          cardEl.innerHTML = `
            <img src="${imgSrc}" class="card-image" alt="${card.name}" style="width:100%;height:auto;border-radius:10px;display:block;">
            ${statusText}
          `;
        } else {
          cardEl.innerHTML = `
            <div class="card-icon">${card.icon}</div>
            <div class="card-name">${typeIcon} ${card.name}</div>
            <div class="card-desc">${card.desc}</div>
            ${statusText}
          `;
        }

        if (finalCanUse) {
          cardEl.onclick = () => selectCard(card.id);
        }

        grid.appendChild(cardEl);
      });

      // スクロールヒント（カードが多い場合）
      const existingHint = grid.parentNode.querySelector('.scroll-hint');
      if (existingHint) existingHint.remove();
      if (availableCards.length > 6) {
        const hint = document.createElement('div');
        hint.className = 'scroll-hint';
        hint.textContent = '↓ スクロールして もっとみる';
        grid.parentNode.insertBefore(hint, grid.nextSibling);
      }

      const cardModal = document.getElementById('card-modal');
      cardModal.classList.toggle('rotated-for-p1', gameState.currentPlayer === 1 && gameMode !== 'ai');
      cardModal.classList.add('show');
    }

    function closeCardModal() {
      const cardModal = document.getElementById('card-modal');
      cardModal.classList.remove('show', 'rotated-for-p1');
    }

    function canUseCard(cardId) {
      const player = gameState.players[gameState.currentPlayer];
      const otherNum = gameState.currentPlayer === 1 ? 2 : 1;
      const other = gameState.players[otherNum];
      const dist = Math.abs(player.row - other.row) + Math.abs(player.col - other.col);

      switch (cardId) {
        // --- にんじゃねこ ---
        case 'ninja_shinobi': {
          // 斜め方向に移動可能なマスがあるか
          const diags = [[-1,-1],[-1,1],[1,-1],[1,1]];
          return diags.some(([dr,dc]) => {
            const nr = player.row+dr, nc = player.col+dc;
            if (nr<0||nr>=BOARD_SIZE||nc<0||nc>=BOARD_SIZE) return false;
            if (nr===other.row && nc===other.col) return false;
            // 斜め移動の壁判定: 縦横両方の壁がないこと
            return !isWallBetween(player.row, player.col, player.row+dr, player.col) &&
                   !isWallBetween(player.row+dr, player.col, nr, nc);
          });
        }
        case 'ninja_musasabi':
          return isAdjacentToOther() && canJumpOver();
        case 'ninja_kawarimi':
          return true; // カウンター（常に持てる）

        // --- どろぼうねこ ---
        case 'thief_zurakaru': {
          // 横方向に2マス移動可能か（壁は無視）
          const col = player.col;
          return (col + 2 < BOARD_SIZE && !(player.row === other.row && col+2 === other.col)) ||
                 (col - 2 >= 0 && !(player.row === other.row && col-2 === other.col));
        }
        case 'thief_kakureru':
          return true;
        case 'thief_itadaki':
          return other.walls > 0;

        // --- すもうねこ ---
        case 'sumo_oshidashi':
          return isAdjacentToOther() && canPushOther();
        case 'sumo_uwatenage':
          return isAdjacentToOther();
        case 'sumo_teppou':
          return gameState.walls.length > 0;

        // --- まじょねこ ---
        case 'witch_teleport':
          return true; // 同じ行の他マスに移動可能
        case 'witch_seal': {
          // 相手に未使用・未封印のカードがあるか
          const otherHand = playerHands[otherNum];
          if (!otherHand) return false;
          return otherHand.some(cid => {
            const used = (other.cardsUsed[cid] || 0) >= 1;
            const sealed = (other.sealedCards[cid] || 0) > 0;
            return !used && !sealed;
          });
        }
        case 'witch_shield':
          return true; // カウンター

        // --- ゆうれいねこ ---
        case 'ghost_walk':
          return true; // 壁がなくても移動先選択で判定
        case 'ghost_kanashibari':
          return true;
        case 'ghost_hyoui':
          return true; // 相手を操る

        // --- でんきねこ ---
        case 'electric_raiden':
          return true; // 2マス自由移動
        case 'electric_shock':
          return isAdjacentToOther();
        case 'electric_magnet':
          return dist <= 2 && dist > 0;

        // --- はかせねこ ---
        case 'prof_wallmachine':
          return true;
        case 'prof_copy': {
          const otherHandC = playerHands[otherNum];
          if (!otherHandC) return false;
          return otherHandC.some(cid => {
            const c = CHARACTER_CARDS.find(cc => cc.id === cid);
            return c && c.actionType !== 'counter' && !((other.cardsUsed[cid] || 0) >= 1);
          });
        }
        case 'prof_remote':
          return dist <= 2 && dist > 0;

        // --- こうじねこ ---
        case 'construction_move':
          return gameState.walls.length > 0;
        case 'construction_rotate':
          return gameState.walls.length > 0;
        case 'construction_blast':
          return gameState.walls.length > 0;

        default:
          return true;
      }
    }

    function hasDashTarget() {
      const player = gameState.players[gameState.currentPlayer];
      const other = gameState.players[gameState.currentPlayer === 1 ? 2 : 1];
      const directions = [[-1, 0], [1, 0], [0, -1], [0, 1]];

      for (const [dr, dc] of directions) {
        const midRow = player.row + dr;
        const midCol = player.col + dc;
        const targetRow = player.row + dr * 2;
        const targetCol = player.col + dc * 2;

        if (targetRow >= 0 && targetRow < BOARD_SIZE && targetCol >= 0 && targetCol < BOARD_SIZE) {
          if (!isWallBetween(player.row, player.col, midRow, midCol) &&
              !isWallBetween(midRow, midCol, targetRow, targetCol)) {
            if (!(midRow === other.row && midCol === other.col) &&
                !(targetRow === other.row && targetCol === other.col)) {
              return true;
            }
          }
        }
      }
      return false;
    }

    function isAdjacentToOther() {
      const player = gameState.players[gameState.currentPlayer];
      const other = gameState.players[gameState.currentPlayer === 1 ? 2 : 1];
      const dr = Math.abs(player.row - other.row);
      const dc = Math.abs(player.col - other.col);
      return (dr === 1 && dc === 0) || (dr === 0 && dc === 1);
    }

    function canJumpOver() {
      const player = gameState.players[gameState.currentPlayer];
      const other = gameState.players[gameState.currentPlayer === 1 ? 2 : 1];
      
      const dr = other.row - player.row;
      const dc = other.col - player.col;
      const landRow = other.row + dr;
      const landCol = other.col + dc;

      if (landRow < 0 || landRow >= BOARD_SIZE || landCol < 0 || landCol >= BOARD_SIZE) {
        return false;
      }

      return !isWallBetween(player.row, player.col, other.row, other.col) &&
             !isWallBetween(other.row, other.col, landRow, landCol);
    }

    function canPushOther() {
      const player = gameState.players[gameState.currentPlayer];
      const other = gameState.players[gameState.currentPlayer === 1 ? 2 : 1];
      
      const dr = other.row - player.row;
      const dc = other.col - player.col;
      const pushRow = other.row + dr;
      const pushCol = other.col + dc;

      if (pushRow < 0 || pushRow >= BOARD_SIZE || pushCol < 0 || pushCol >= BOARD_SIZE) {
        return false;
      }

      return !isWallBetween(player.row, player.col, other.row, other.col) &&
             !isWallBetween(other.row, other.col, pushRow, pushCol);
    }

    function selectCard(cardId) {
      closeCardModal();
      gameState.selectedCard = cardId;

      // ちゃとら用ボタン
      document.getElementById('btn-move').classList.remove('active');
      document.getElementById('btn-wall-h').classList.remove('active');
      document.getElementById('btn-wall-v').classList.remove('active');
      document.getElementById('btn-card').classList.add('active');

      // くろねこ用ボタン
      document.getElementById('btn-move2').classList.remove('active');
      document.getElementById('btn-wall-h2').classList.remove('active');
      document.getElementById('btn-wall-v2').classList.remove('active');
      document.getElementById('btn-card2').classList.add('active');

      const card = SKILL_CARDS.find(c => c.id === cardId);
      document.getElementById('mode-indicator').textContent = `カード: ${card.icon} ${card.name}`;
      document.getElementById('mode-indicator2').textContent = `カード: ${card.icon} ${card.name}`;

      // 即時実行カード（ターゲット不要）
      const immediateCards = [
        'thief_kakureru', 'thief_itadaki',
        'ghost_kanashibari',
        'electric_shock',
        'prof_wallmachine', 'prof_copy'
      ];
      if (immediateCards.includes(cardId)) {
        executeImmediateCard(cardId);
        return;
      }

      // 封印カード → 相手カード選択モーダル
      if (cardId === 'witch_seal') {
        showSealModal();
        return;
      }

      gameState.cardPhase = 'selectTarget';
      highlightCardTargets(cardId);
    }

    // カード使用後のアクションタイプに応じた処理
    function afterCardAction(cardId) {
      const card = CHARACTER_CARDS.find(c => c.id === cardId);
      if (!card) { switchPlayer(); return; }

      // コピーカード使用時はコピー元のactionTypeに従う（ただしprof_copy自体のturn_endは無視）
      const effectiveActionType = gameState.copyingCard ? (card.actionType || 'turn_end') : card.actionType;

      if (effectiveActionType === 'move_ok') {
        // 🟢 Move OK: 使用後に1マス移動可能
        gameState.pendingMoveOk = true;
        gameState.selectedCard = null;
        gameState.cardPhase = null;
        setMode('move');
        highlightValidMoves();

        // AI時は自動で最善移動
        if (gameMode === 'ai' && gameState.currentPlayer === 2) {
          const moves = getValidMoves(2);
          if (moves.length > 0) {
            const ai = gameState.players[2];
            let best = moves[0], bestDist = Infinity;
            for (const m of moves) {
              const p = findShortestPath(m.row, m.col, 0);
              const d = p ? p.length : 999;
              if (d < bestDist) { bestDist = d; best = m; }
            }
            ai.row = best.row;
            ai.col = best.col;
            renderCats(2);
            checkWin();
          }
          gameState.pendingMoveOk = false;
          if (!gameState.gameOver) switchPlayer();
        } else {
          showToast('🟢 カード使用後、1マス移動できるよ！', 'info', 2000);
        }
      } else {
        // 🔴 Turn End / 🔵 Move Act: ターン終了
        if (!gameState.gameOver) switchPlayer();
      }
    }

    // 即時実行カード（ターゲット選択不要）
    function executeImmediateCard(cardId) {
      const player = gameState.players[gameState.currentPlayer];
      const otherNum = gameState.currentPlayer === 1 ? 2 : 1;
      const other = gameState.players[otherNum];

      switch (cardId) {
        case 'thief_kakureru':
          showCardEffect(cardId, () => {
            player.hidden = true;
            useCard();
            showToast('🃏 次のターン、カード効果を受けないよ！', 'info');
            afterCardAction(cardId);
          });
          break;
        case 'thief_itadaki':
          showCardEffect(cardId, () => {
            if (other.walls > 0) {
              other.walls--;
              player.walls++;
              showToast('🧱 壁を1枚いただいた！', 'info');
            }
            useCard();
            updateUI();
            afterCardAction(cardId);
          });
          break;
        case 'ghost_kanashibari':
          showCardEffect(cardId, () => {
            other.frozen = true;
            other.frozenTurns = 1;
            useCard();
            showToast('🥶 金縛り！相手は次のターン移動できない！', 'info');
            afterCardAction(cardId);
          });
          break;
        case 'electric_shock':
          showCardEffect(cardId, () => {
            other.frozen = true;
            other.frozenTurns = 2;
            useCard();
            showToast('⚡ 感電！相手は2ターン移動できない！', 'info');
            afterCardAction(cardId);
          });
          break;
        case 'prof_wallmachine':
          showCardEffect(cardId, () => {
            player.walls += 2;
            useCard();
            updateUI();
            showToast('🧪 壁が2枚増えた！', 'info');
            afterCardAction(cardId);
          });
          break;
        case 'prof_copy':
          executeCopy();
          break;
      }
    }

    // 封印カード: 相手のカード選択モーダル
    function showSealModal() {
      const otherNum = gameState.currentPlayer === 1 ? 2 : 1;
      const other = gameState.players[otherNum];
      const otherHand = playerHands[otherNum];
      if (!otherHand) return;

      const sealableCards = CHARACTER_CARDS.filter(c =>
        otherHand.includes(c.id) && !((other.cardsUsed[c.id] || 0) >= 1) && !((other.sealedCards[c.id] || 0) > 0)
      );

      const grid = document.getElementById('copy-grid');
      grid.innerHTML = '';

      sealableCards.forEach(card => {
        const cardEl = document.createElement('div');
        cardEl.className = 'skill-card';
        cardEl.innerHTML = `
          <div class="card-icon">${card.icon}</div>
          <div class="card-name">${card.name}</div>
          <div class="card-desc">${card.desc}</div>
        `;
        cardEl.onclick = () => {
          closeCopyModal();
          showCardEffect('witch_seal', () => {
            other.sealedCards[card.id] = 2;
            useCard();
            showToast(`🤐 ${card.name} を2ターン封印した！`, 'info');
            afterCardAction('witch_seal');
          });
        };
        grid.appendChild(cardEl);
      });

      const copyModal = document.getElementById('copy-modal');
      document.querySelector('#copy-modal .card-modal h2').textContent = '🤐 封印するカードをえらぶ';
      copyModal.classList.toggle('rotated-for-p1', gameState.currentPlayer === 1 && gameMode !== 'ai');
      copyModal.classList.add('show');
    }

    function executeSwap() {
      const p1 = gameState.players[1];
      const p2 = gameState.players[2];

      showCardEffect('swap', () => {
        animateSwap(() => {
          [p1.row, p2.row] = [p2.row, p1.row];
          [p1.col, p2.col] = [p2.col, p1.col];
          useCard();
          renderCats();
          checkWin();
          if (!gameState.gameOver) switchPlayer();
        });
      });
    }

    function executeFreeze() {
      const otherNum = gameState.currentPlayer === 1 ? 2 : 1;
      
      showCardEffect('freeze', () => {
        animateFreeze(otherNum, () => {
          gameState.players[otherNum].frozen = true;
          useCard();
          if (!gameState.gameOver) switchPlayer();
        });
      });
    }

    function executeWallPlus() {
      const currentP = gameState.currentPlayer;

      showCardEffect('wallplus', () => {
        animateWallPlus(currentP, () => {
          gameState.players[currentP].walls += 2;
          useCard();
          if (!gameState.gameOver) switchPlayer();
        });
      });
    }

    function executeCopy() {
      const otherNum = gameState.currentPlayer === 1 ? 2 : 1;
      const other = gameState.players[otherNum];
      const otherHand = playerHands[otherNum];

      // 相手の未使用カードを取得（コピー系・カウンター系はコピー不可）
      let opponentCards;
      if (otherHand) {
        opponentCards = CHARACTER_CARDS.filter(c =>
          c.actionType !== 'counter' && c.id !== 'prof_copy' && otherHand.includes(c.id) && !((other.cardsUsed[c.id] || 0) >= c.limit)
        );
      } else {
        opponentCards = CHARACTER_CARDS.filter(c =>
          c.actionType !== 'counter' && c.id !== 'prof_copy' && !((other.cardsUsed[c.id] || 0) >= c.limit)
        );
      }

      if (opponentCards.length === 0) {
        showToast('コピーできるカードがないよ！', 'warn');
        return;
      }

      document.querySelector('#copy-modal .card-modal h2').textContent = '📠 相手のカードをコピー！';
      showCopyModal(opponentCards);
    }

    function showCopyModal(cards) {
      const grid = document.getElementById('copy-grid');
      grid.innerHTML = '';

      cards.forEach(card => {
        const cardEl = document.createElement('div');
        cardEl.className = 'skill-card';
        const canUse = canUseCard(card.id);
        if (!canUse) cardEl.classList.add('disabled');

        const imgSrc = CARD_IMAGES[card.id];
        if (imgSrc) {
          cardEl.innerHTML = `
            <img src="${imgSrc}" class="card-image" alt="${card.name}" style="width:100%;height:auto;border-radius:10px;display:block;">
          `;
        } else {
          cardEl.innerHTML = `
            <div class="card-icon">${card.icon}</div>
            <div class="card-name">${card.name}</div>
            <div class="card-desc">${card.desc}</div>
          `;
        }

        if (canUse) {
          cardEl.onclick = () => executeCopiedCard(card.id);
        }

        grid.appendChild(cardEl);
      });

      const copyModal = document.getElementById('copy-modal');
      copyModal.classList.toggle('rotated-for-p1', gameState.currentPlayer === 1 && gameMode !== 'ai');
      copyModal.classList.add('show');
    }

    function closeCopyModal() {
      const copyModal = document.getElementById('copy-modal');
      copyModal.classList.remove('show', 'rotated-for-p1');
    }

    function executeCopiedCard(cardId) {
      closeCopyModal();
      gameState.copyingCard = true;
      gameState.selectedCard = cardId;

      const card = SKILL_CARDS.find(c => c.id === cardId);
      document.getElementById('mode-indicator').textContent = `複製: ${card.icon} ${card.name}`;
      document.getElementById('mode-indicator2').textContent = `複製: ${card.icon} ${card.name}`;

      // 即時実行カード
      const immediateCards = [
        'thief_kakureru', 'thief_itadaki',
        'ghost_kanashibari',
        'electric_shock',
        'prof_wallmachine'
      ];
      if (immediateCards.includes(cardId)) {
        executeImmediateCard(cardId);
        return;
      }

      // 封印カード
      if (cardId === 'witch_seal') {
        showSealModal();
        return;
      }

      // ターゲット選択カード
      gameState.cardPhase = 'selectTarget';
      highlightCardTargets(cardId);
    }

    function useCard(skipEffect) {
      const player = gameState.players[gameState.currentPlayer];
      let cardId = gameState.selectedCard;

      // 複製カードの場合、'prof_copy'(or旧'copy')を消費する
      if (gameState.copyingCard) {
        cardId = playerHands[gameState.currentPlayer]?.includes('prof_copy') ? 'prof_copy' : 'copy';
        gameState.copyingCard = false;
      }

      animating = false; // アニメーションロック解除

      player.cardsUsed[cardId] = (player.cardsUsed[cardId] || 0) + 1;
      player.totalCardsUsed++;
      gameState.selectedCard = null;
      gameState.cardPhase = null;
      clearHighlights();
      updateUI();
    }

    function showCardEffect(cardId, onComplete) {
      const card = SKILL_CARDS.find(c => c.id === cardId);
      if (!card) { if (onComplete) onComplete(); return; }
      
      animating = true;
      
      const overlay = document.getElementById('card-effect');
      const cardEl = document.getElementById('card-effect-card');
      const imgEl = document.getElementById('card-effect-image');
      const activateEl = document.getElementById('card-effect-activate');
      
      const imgSrc = CARD_IMAGES[cardId];
      if (imgSrc) {
        imgEl.src = imgSrc;
        imgEl.alt = card.name;
        imgEl.style.display = '';
      } else {
        imgEl.style.display = 'none';
      }

      activateEl.textContent = `${card.icon} ${card.name} 発動！`;
      
      cardEl.querySelectorAll('.card-spark').forEach(el => el.remove());
      
      const sparkColors = ['#FFD700', '#FF6B6B', '#4ECDC4', '#A855F7', '#FF9F43', '#FFFFFF'];
      for (let i = 0; i < 16; i++) {
        const spark = document.createElement('div');
        spark.className = 'card-spark';
        const angle = (i / 16) * Math.PI * 2;
        const dist = 80 + Math.random() * 80;
        spark.style.cssText = `
          top: 50%; left: 50%;
          background: ${sparkColors[i % sparkColors.length]};
          box-shadow: 0 0 6px ${sparkColors[i % sparkColors.length]};
          --sx: ${Math.cos(angle) * dist}px;
          --sy: ${Math.sin(angle) * dist}px;
          animation-delay: ${0.35 + Math.random() * 0.2}s;
          width: ${4 + Math.random() * 5}px;
          height: ${4 + Math.random() * 5}px;
        `;
        cardEl.appendChild(spark);
      }
      
      overlay.classList.remove('show');
      void overlay.offsetWidth;
      overlay.classList.add('show');
      
      setTimeout(() => {
        overlay.classList.remove('show');
        if (onComplete) onComplete();
      }, 1800);
    }

    // ========== 盤面アニメーション共通ユーティリティ ==========
    
    function getCellCenter(boardRow, boardCol) {
      const cell = document.querySelector(`.cell[data-row="${boardRow * 2}"][data-col="${boardCol * 2}"]`);
      if (!cell) return null;
      const rect = cell.getBoundingClientRect();
      return { x: rect.left + rect.width / 2, y: rect.top + rect.height / 2, rect };
    }
    
    function createFlyingCat(playerNum) {
      const p = gameState.players[playerNum];
      const pos = getCellCenter(p.row, p.col);
      if (!pos) return null;
      const el = document.createElement('div');
      el.textContent = p.emoji;
      el.style.cssText = `
        position: fixed; z-index: 500; pointer-events: none;
        font-size: min(7vw, 26px);
        left: ${pos.x - 15}px; top: ${pos.y - 15}px;
        text-shadow: ${playerNum === 1 ? '0 0 3px #fff, 0 0 5px #fff' : '0 0 3px #000, 0 0 5px #000'};
      `;
      document.body.appendChild(el);
      return { el, startX: pos.x - 15, startY: pos.y - 15 };
    }
    
    function createBoardEffect(boardRow, boardCol, emoji, color, duration) {
      const pos = getCellCenter(boardRow, boardCol);
      if (!pos) return;
      const el = document.createElement('div');
      el.textContent = emoji;
      el.style.cssText = `
        position: fixed; z-index: 400; pointer-events: none;
        font-size: 2rem;
        left: ${pos.x - 16}px; top: ${pos.y - 16}px;
        filter: drop-shadow(0 0 8px ${color});
        transition: all ${duration}ms ease-out;
      `;
      document.body.appendChild(el);
      requestAnimationFrame(() => {
        el.style.transform = 'scale(2) translateY(-20px)';
        el.style.opacity = '0';
      });
      setTimeout(() => el.remove(), duration);
    }

    // ========== 各カードのアニメーション ==========
    
    // 🔄 いれかえ: 2匹が弧を描いて入れ替わる
    function animateSwap(onComplete) {
      const p1 = gameState.players[1];
      const p2 = gameState.players[2];
      const fly1 = createFlyingCat(1);
      const fly2 = createFlyingCat(2);
      
      if (!fly1 || !fly2) { onComplete(); return; }
      
      document.querySelectorAll('.cat').forEach(el => el.style.opacity = '0');
      
      const pos1 = getCellCenter(p1.row, p1.col);
      const pos2 = getCellCenter(p2.row, p2.col);
      const dx = pos2.x - pos1.x;
      const dy = pos2.y - pos1.y;
      const duration = 600;
      const start = performance.now();
      
      function step(now) {
        const t = Math.min((now - start) / duration, 1);
        const ease = t < 0.5 ? 4*t*t*t : 1 - Math.pow(-2*t+2, 3)/2;
        const arc = Math.sin(t * Math.PI) * 25;
        const s = 1 + Math.sin(t * Math.PI) * 0.3;
        const glow = 8 + Math.sin(t * Math.PI) * 15;
        
        fly1.el.style.transform = `translate(${dx*ease}px, ${dy*ease - arc}px) scale(${s})`;
        fly2.el.style.transform = `translate(${-dx*ease}px, ${-dy*ease + arc}px) scale(${s})`;
        fly1.el.style.filter = `drop-shadow(0 0 ${glow}px rgba(168,85,247,0.9))`;
        fly2.el.style.filter = `drop-shadow(0 0 ${glow}px rgba(168,85,247,0.9))`;
        
        if (t < 1) requestAnimationFrame(step);
        else { fly1.el.remove(); fly2.el.remove(); onComplete(); }
      }
      requestAnimationFrame(step);
    }
    
    // ⏩ 2マスダッシュ: 猫がシュッと高速移動（残像付き）
    function animateDash(fromRow, fromCol, toRow, toCol, playerNum, onComplete) {
      const p = gameState.players[playerNum];
      const from = getCellCenter(fromRow, fromCol);
      const to = getCellCenter(toRow, toCol);
      if (!from || !to) { onComplete(); return; }
      
      document.querySelectorAll('.cat').forEach(el => el.style.opacity = '0');
      
      const fly = document.createElement('div');
      fly.textContent = p.emoji;
      fly.style.cssText = `
        position: fixed; z-index: 500; pointer-events: none;
        font-size: min(7vw, 26px); left: ${from.x - 15}px; top: ${from.y - 15}px;
        text-shadow: ${playerNum === 1 ? '0 0 3px #fff, 0 0 5px #fff' : '0 0 3px #000, 0 0 5px #000'};
      `;
      document.body.appendChild(fly);
      
      // 残像を3つ生成
      const afterimages = [];
      for (let i = 0; i < 3; i++) {
        const ai = document.createElement('div');
        ai.textContent = p.emoji;
        ai.style.cssText = fly.style.cssText + `opacity: ${0.3 - i*0.1}; filter: blur(${2+i}px);`;
        document.body.appendChild(ai);
        afterimages.push(ai);
      }
      
      const dx = to.x - from.x;
      const dy = to.y - from.y;
      const duration = 350;
      const start = performance.now();
      
      function step(now) {
        const t = Math.min((now - start) / duration, 1);
        const ease = 1 - Math.pow(1 - t, 3);
        
        fly.style.transform = `translate(${dx*ease}px, ${dy*ease}px)`;
        fly.style.filter = `drop-shadow(0 0 ${12 - t*10}px rgba(59,130,246,0.8))`;
        
        afterimages.forEach((ai, i) => {
          const delay = (i + 1) * 0.15;
          const at = Math.max(0, Math.min((t - delay) / (1 - delay), 1));
          const ae = 1 - Math.pow(1 - at, 3);
          ai.style.transform = `translate(${dx*ae}px, ${dy*ae}px)`;
          ai.style.opacity = `${(0.3 - i*0.1) * (1 - t)}`;
        });
        
        if (t < 1) requestAnimationFrame(step);
        else {
          fly.remove();
          afterimages.forEach(ai => ai.remove());
          onComplete();
        }
      }
      requestAnimationFrame(step);
    }
    
    // 🦘 ジャンプ: 猫がぴょーんと弧を描いて飛び越える
    function animateJump(fromRow, fromCol, toRow, toCol, playerNum, onComplete) {
      const p = gameState.players[playerNum];
      const from = getCellCenter(fromRow, fromCol);
      const to = getCellCenter(toRow, toCol);
      if (!from || !to) { onComplete(); return; }
      
      document.querySelectorAll('.cat').forEach(el => el.style.opacity = '0');
      
      const fly = document.createElement('div');
      fly.textContent = p.emoji;
      fly.style.cssText = `
        position: fixed; z-index: 500; pointer-events: none;
        font-size: min(7vw, 26px); left: ${from.x - 15}px; top: ${from.y - 15}px;
        text-shadow: ${playerNum === 1 ? '0 0 3px #fff, 0 0 5px #fff' : '0 0 3px #000, 0 0 5px #000'};
      `;
      document.body.appendChild(fly);
      
      const dx = to.x - from.x;
      const dy = to.y - from.y;
      const duration = 500;
      const jumpHeight = 50;
      const start = performance.now();
      
      function step(now) {
        const t = Math.min((now - start) / duration, 1);
        const ease = t < 0.5 ? 2*t*t : 1-Math.pow(-2*t+2,2)/2;
        const arc = -Math.sin(t * Math.PI) * jumpHeight;
        const rot = Math.sin(t * Math.PI) * 15;
        
        fly.style.transform = `translate(${dx*ease}px, ${dy*ease + arc}px) rotate(${rot}deg) scale(${1 + Math.sin(t*Math.PI)*0.2})`;
        fly.style.filter = `drop-shadow(0 0 ${10 + Math.sin(t*Math.PI)*10}px rgba(34,197,94,0.8))`;
        
        if (t < 1) requestAnimationFrame(step);
        else { fly.remove(); onComplete(); }
      }
      requestAnimationFrame(step);
    }
    
    // 💨 おしだし: 猫が突進して相手を押す
    function animatePush(pusherNum, pushDir, onComplete) {
      const pusher = gameState.players[pusherNum];
      const pushed = gameState.players[pusherNum === 1 ? 2 : 1];
      const fly1 = createFlyingCat(pusherNum);
      const fly2 = createFlyingCat(pusherNum === 1 ? 2 : 1);
      if (!fly1 || !fly2) { onComplete(); return; }
      
      document.querySelectorAll('.cat').forEach(el => el.style.opacity = '0');
      
      const cellSize = getCellCenter(0, 1).x - getCellCenter(0, 0).x;
      const dx = pushDir[1] * cellSize;
      const dy = pushDir[0] * cellSize;
      const duration = 400;
      const start = performance.now();
      
      function step(now) {
        const t = Math.min((now - start) / duration, 1);
        
        // 突進する猫: 最初ためてから急加速
        const t1 = t < 0.3 ? 0 : Math.pow((t - 0.3) / 0.7, 2);
        fly1.el.style.transform = `translate(${dx*t1}px, ${dy*t1}px)`;
        
        // 押される猫: 衝突後に移動
        const t2 = t < 0.4 ? 0 : Math.min(((t - 0.4) / 0.6), 1);
        const e2 = 1 - Math.pow(1 - t2, 3);
        fly2.el.style.transform = `translate(${dx*e2}px, ${dy*e2}px)`;
        
        if (t > 0.35 && t < 0.5) {
          fly1.el.style.filter = 'drop-shadow(0 0 15px rgba(255,100,50,0.9))';
          fly2.el.style.filter = 'drop-shadow(0 0 15px rgba(255,100,50,0.9))';
        } else {
          fly1.el.style.filter = '';
          fly2.el.style.filter = '';
        }
        
        if (t < 1) requestAnimationFrame(step);
        else { fly1.el.remove(); fly2.el.remove(); onComplete(); }
      }
      requestAnimationFrame(step);
    }
    
    // ➡️ スライド: 壁がスルッと移動する
    function animateSlide(oldWall, newWall, onComplete) {
      const oldCells = getWallCells(oldWall);
      const newCells = getWallCells(newWall);
      if (oldCells.length === 0 || newCells.length === 0) { onComplete(); return; }
      
      const oldEl = document.querySelector(`.cell[data-row="${oldCells[0].row}"][data-col="${oldCells[0].col}"]`);
      const newEl = document.querySelector(`.cell[data-row="${newCells[0].row}"][data-col="${newCells[0].col}"]`);
      if (!oldEl || !newEl) { onComplete(); return; }
      
      const or = oldEl.getBoundingClientRect();
      const nr = newEl.getBoundingClientRect();
      
      // 一時的なフライング壁ブロック
      const flyWall = document.createElement('div');
      flyWall.style.cssText = `
        position: fixed; z-index: 400; pointer-events: none;
        left: ${or.left}px; top: ${or.top}px;
        width: ${or.width}px; height: ${or.height}px;
        background: #8B6914; border-radius: 3px;
        box-shadow: 0 0 10px rgba(59,130,246,0.6);
        transition: none;
      `;
      document.body.appendChild(flyWall);
      
      const dx = nr.left - or.left;
      const dy = nr.top - or.top;
      const duration = 350;
      const start = performance.now();
      
      function step(now) {
        const t = Math.min((now - start) / duration, 1);
        const ease = 1 - Math.pow(1 - t, 3);
        flyWall.style.transform = `translate(${dx*ease}px, ${dy*ease}px)`;
        flyWall.style.boxShadow = `0 0 ${15 - t*10}px rgba(59,130,246,${0.8 - t*0.6})`;
        
        if (t < 1) requestAnimationFrame(step);
        else { flyWall.remove(); onComplete(); }
      }
      requestAnimationFrame(step);
    }
    
    // 🔃 回転: 壁がクルッと回る
    function animateRotate(wall, onComplete) {
      const cells = getWallCells(wall);
      if (cells.length === 0) { onComplete(); return; }
      
      const el = document.querySelector(`.cell[data-row="${wall.cornerRow}"][data-col="${wall.cornerCol}"]`);
      if (!el) { onComplete(); return; }
      const rect = el.getBoundingClientRect();
      
      const flyWall = document.createElement('div');
      flyWall.textContent = '🧱';
      flyWall.style.cssText = `
        position: fixed; z-index: 400; pointer-events: none;
        left: ${rect.left + rect.width/2 - 15}px; top: ${rect.top + rect.height/2 - 15}px;
        font-size: 1.8rem;
        filter: drop-shadow(0 0 8px rgba(234,179,8,0.8));
      `;
      document.body.appendChild(flyWall);
      
      const duration = 400;
      const start = performance.now();
      
      function step(now) {
        const t = Math.min((now - start) / duration, 1);
        const ease = 1 - Math.pow(1 - t, 3);
        flyWall.style.transform = `rotate(${90 * ease}deg) scale(${1 + Math.sin(t*Math.PI)*0.3})`;
        
        if (t < 1) requestAnimationFrame(step);
        else { flyWall.remove(); onComplete(); }
      }
      requestAnimationFrame(step);
    }
    
    // ♻️ 回収: 壁が手元に吸い込まれる
    function animateRecover(wall, onComplete) {
      const cells = getWallCells(wall);
      if (cells.length === 0) { onComplete(); return; }
      
      // 壁セルの位置からアニメーション
      cells.forEach(({row, col}, i) => {
        const cellEl = document.querySelector(`.cell[data-row="${row}"][data-col="${col}"]`);
        if (!cellEl) return;
        const rect = cellEl.getBoundingClientRect();
        
        const particle = document.createElement('div');
        particle.textContent = '✨';
        particle.style.cssText = `
          position: fixed; z-index: 400; pointer-events: none;
          left: ${rect.left + rect.width/2 - 10}px; top: ${rect.top + rect.height/2 - 10}px;
          font-size: 1.2rem;
          transition: all 500ms cubic-bezier(0.4, 0, 0.2, 1);
        `;
        document.body.appendChild(particle);
        
        setTimeout(() => {
          particle.style.transform = 'scale(0) translateY(80px)';
          particle.style.opacity = '0';
        }, 50);
        setTimeout(() => particle.remove(), 600);
      });
      
      setTimeout(onComplete, 500);
    }
    
    // 💥 壁破壊: 壁が砕け散るエフェクト
    function animateWallDestroy(wall, onComplete) {
      const cells = getWallCells(wall);
      if (cells.length === 0) { onComplete(); return; }

      cells.forEach(({row, col}, i) => {
        const cellEl = document.querySelector(`.cell[data-row="${row}"][data-col="${col}"]`);
        if (!cellEl) return;
        const rect = cellEl.getBoundingClientRect();

        const emojis = ['💥', '💫', '🔥'];
        emojis.forEach((emoji, j) => {
          const particle = document.createElement('div');
          particle.textContent = emoji;
          const angle = ((i * emojis.length + j) / (cells.length * emojis.length)) * Math.PI * 2;
          const dist = 30 + Math.random() * 40;
          particle.style.cssText = `
            position: fixed; z-index: 400; pointer-events: none;
            left: ${rect.left + rect.width/2 - 10}px; top: ${rect.top + rect.height/2 - 10}px;
            font-size: 1.2rem; opacity: 1;
            transition: all 500ms cubic-bezier(0.4, 0, 0.2, 1);
          `;
          document.body.appendChild(particle);

          setTimeout(() => {
            particle.style.transform = `translate(${Math.cos(angle)*dist}px, ${Math.sin(angle)*dist}px) scale(0)`;
            particle.style.opacity = '0';
          }, 50);
          setTimeout(() => particle.remove(), 600);
        });
      });

      setTimeout(onComplete, 500);
    }

    // 🐾 足止め: 相手に鎖エフェクト
    function animateFreeze(targetNum, onComplete) {
      const target = gameState.players[targetNum];
      const pos = getCellCenter(target.row, target.col);
      if (!pos) { onComplete(); return; }
      
      const emojis = ['⛓️', '🐾', '❄️'];
      emojis.forEach((emoji, i) => {
        const el = document.createElement('div');
        el.textContent = emoji;
        el.style.cssText = `
          position: fixed; z-index: 400; pointer-events: none;
          left: ${pos.x - 15 + (i-1)*20}px; top: ${pos.y - 15}px;
          font-size: 1.5rem; opacity: 0;
          transition: all 400ms ease-out;
        `;
        document.body.appendChild(el);
        
        setTimeout(() => {
          el.style.opacity = '1';
          el.style.transform = `translateY(-${15 + i*10}px) scale(1.3)`;
        }, i * 120);
        setTimeout(() => {
          el.style.opacity = '0';
          el.style.transform = `translateY(-${30 + i*10}px) scale(0.5)`;
        }, 600 + i * 80);
        setTimeout(() => el.remove(), 1000);
      });
      
      setTimeout(onComplete, 700);
    }
    
    // 🧱 壁プラス: 壁が増えるエフェクト
    function animateWallPlus(playerNum, onComplete) {
      const player = gameState.players[playerNum];
      const pos = getCellCenter(player.row, player.col);
      if (!pos) { onComplete(); return; }
      
      for (let i = 0; i < 3; i++) {
        const el = document.createElement('div');
        el.textContent = '🧱';
        el.style.cssText = `
          position: fixed; z-index: 400; pointer-events: none;
          left: ${pos.x - 15}px; top: ${pos.y + 20}px;
          font-size: 1.4rem; opacity: 0;
          transition: all 400ms cubic-bezier(0.34, 1.56, 0.64, 1);
        `;
        document.body.appendChild(el);
        
        setTimeout(() => {
          el.style.opacity = '1';
          el.style.transform = `translateY(-${30 + i*25}px) rotate(${-10 + i*10}deg)`;
        }, i * 150);
        setTimeout(() => {
          el.style.opacity = '0';
          el.style.transform = `translateY(-${50 + i*25}px) scale(0.3)`;
        }, 500 + i * 100);
        setTimeout(() => el.remove(), 900);
      }
      
      setTimeout(onComplete, 700);
    }

    function highlightCardTargets(cardId) {
      clearHighlights();

      const player = gameState.players[gameState.currentPlayer];
      const other = gameState.players[gameState.currentPlayer === 1 ? 2 : 1];

      // === 忍び足: 斜め1マス ===
      if (cardId === 'ninja_shinobi') {
        for (const [dr, dc] of [[-1,-1],[-1,1],[1,-1],[1,1]]) {
          const nr = player.row+dr, nc = player.col+dc;
          if (nr<0||nr>=BOARD_SIZE||nc<0||nc>=BOARD_SIZE) continue;
          if (nr===other.row && nc===other.col) continue;
          if (!isWallBetween(player.row, player.col, player.row+dr, player.col) &&
              !isWallBetween(player.row+dr, player.col, nr, nc)) {
            highlightFloor(nr, nc, 'highlight-special');
          }
        }
      }
      // === ムササビの術: 相手を飛び越え ===
      else if (cardId === 'ninja_musasabi') {
        const dr = other.row - player.row;
        const dc = other.col - player.col;
        if ((Math.abs(dr)+Math.abs(dc)) === 1) {
          const landRow = other.row + dr;
          const landCol = other.col + dc;
          if (landRow>=0 && landRow<BOARD_SIZE && landCol>=0 && landCol<BOARD_SIZE) {
            if (!isWallBetween(player.row, player.col, other.row, other.col) &&
                !isWallBetween(other.row, other.col, landRow, landCol)) {
              highlightFloor(landRow, landCol, 'highlight-special');
            }
          }
        }
      }
      // === ずらかる: 横2マス ===
      else if (cardId === 'thief_zurakaru') {
        for (const dc of [-2, 2]) {
          const nc = player.col + dc;
          if (nc >= 0 && nc < BOARD_SIZE && !(player.row === other.row && nc === other.col)) {
            highlightFloor(player.row, nc, 'highlight-special');
          }
        }
      }
      // === 押し出し: 相手をハイライト ===
      else if (cardId === 'sumo_oshidashi') {
        if (isAdjacentToOther()) highlightFloor(other.row, other.col, 'highlight-special');
      }
      // === 上手投げ: 相手をハイライト ===
      else if (cardId === 'sumo_uwatenage') {
        if (isAdjacentToOther()) highlightFloor(other.row, other.col, 'highlight-special');
      }
      // === テレポート: 同じ行の全マス ===
      else if (cardId === 'witch_teleport') {
        for (let c = 0; c < BOARD_SIZE; c++) {
          if (c !== player.col) highlightFloor(player.row, c, 'highlight-special');
        }
      }
      // === ゴーストウォーク: 隣接4方向 ===
      else if (cardId === 'ghost_walk') {
        for (const [dr, dc] of [[-1,0],[1,0],[0,-1],[0,1]]) {
          const nr = player.row+dr, nc = player.col+dc;
          if (nr>=0 && nr<BOARD_SIZE && nc>=0 && nc<BOARD_SIZE && !(nr===other.row && nc===other.col)) {
            highlightFloor(nr, nc, 'highlight-special');
          }
        }
      }
      // === 憑依/リモート操作: 相手の隣接マスをハイライト ===
      else if (cardId === 'ghost_hyoui' || cardId === 'prof_remote') {
        for (const [dr, dc] of [[-1,0],[1,0],[0,-1],[0,1]]) {
          const nr = other.row+dr, nc = other.col+dc;
          if (nr>=0 && nr<BOARD_SIZE && nc>=0 && nc<BOARD_SIZE &&
              !(nr===player.row && nc===player.col) &&
              !isWallBetween(other.row, other.col, nr, nc)) {
            highlightFloor(nr, nc, 'highlight-special');
          }
        }
      }
      // === 電光石火: 2歩以内の到達可能マス ===
      else if (cardId === 'electric_raiden') {
        for (let r = 0; r < BOARD_SIZE; r++) {
          for (let c = 0; c < BOARD_SIZE; c++) {
            if (r === player.row && c === player.col) continue;
            if (r === other.row && c === other.col) continue;
            const dist = Math.abs(r - player.row) + Math.abs(c - player.col);
            if (dist <= 2 && canReachInSteps(player.row, player.col, r, c, 2, other)) {
              highlightFloor(r, c, 'highlight-special');
            }
          }
        }
      }
      // === 超電導マグネット: 自分の隣接マスをハイライト ===
      else if (cardId === 'electric_magnet') {
        for (const [dr, dc] of [[-1,0],[1,0],[0,-1],[0,1]]) {
          const nr = player.row+dr, nc = player.col+dc;
          if (nr>=0 && nr<BOARD_SIZE && nc>=0 && nc<BOARD_SIZE && !(nr===other.row && nc===other.col)) {
            highlightFloor(nr, nc, 'highlight-special');
          }
        }
      }
      // === 壁操作カード: 全壁をハイライト ===
      else if (['sumo_teppou', 'construction_move', 'construction_rotate', 'construction_blast',
                'slide', 'rotate', 'walldestroy'].includes(cardId)) {
        gameState.walls.forEach(wall => {
          const cell = document.querySelector(`.cell[data-row="${wall.cornerRow}"][data-col="${wall.cornerCol}"]`);
          if (cell) cell.classList.add('highlight-wall');
        });
      }
      // === 壁回収(旧) ===
      else if (cardId === 'recover') {
        gameState.walls.forEach(wall => {
          if (wall.owner === gameState.currentPlayer) {
            const cell = document.querySelector(`.cell[data-row="${wall.cornerRow}"][data-col="${wall.cornerCol}"]`);
            if (cell) cell.classList.add('highlight-wall');
          }
        });
      }
    }

    function highlightFloor(boardRow, boardCol, className) {
      const gridRow = boardRow * 2;
      const gridCol = boardCol * 2;
      const cell = document.querySelector(`.cell[data-row="${gridRow}"][data-col="${gridCol}"]`);
      if (cell) cell.classList.add(className);
    }

    function clearHighlights() {
      document.querySelectorAll('.cell').forEach(cell => {
        cell.classList.remove('highlight', 'highlight-special', 'highlight-wall', 'highlight-corner', 'goal-reachable');
      });
    }

    function highlightValidMoves() {
      clearHighlights();
      
      // ゴールマスの点灯切り替え
      document.querySelectorAll('.goal1').forEach(el => el.classList.toggle('goal-active', gameState.currentPlayer === 1));
      document.querySelectorAll('.goal2').forEach(el => el.classList.toggle('goal-active', gameState.currentPlayer === 2));

      if (gameState.cardPhase) return;
      if (gameState.gameOver) return;

      if (gameState.mode === 'move') {
        const player = gameState.players[gameState.currentPlayer];
        
        // 足止め中はハイライトしない
        if (player.frozen) return;
        
        const other = gameState.players[gameState.currentPlayer === 1 ? 2 : 1];
        const directions = [[-1, 0], [1, 0], [0, -1], [0, 1]];

        const goalRow = gameState.currentPlayer === 1 ? BOARD_SIZE - 1 : 0;

        for (const [dr, dc] of directions) {
          const nr = player.row + dr;
          const nc = player.col + dc;

          if (nr >= 0 && nr < BOARD_SIZE && nc >= 0 && nc < BOARD_SIZE) {
            if (!isWallBetween(player.row, player.col, nr, nc)) {
              if (!(nr === other.row && nc === other.col)) {
                // ゴールに止まれるマスは特別に光らせる
                if (nr === goalRow) {
                  highlightFloor(nr, nc, 'goal-reachable');
                } else {
                  highlightFloor(nr, nc, 'highlight');
                }
              }
            }
          }
        }
      } else if (gameState.mode === 'wall') {
        // 壁モード: 置ける交差点をハイライト
        const player = gameState.players[gameState.currentPlayer];
        if (player.walls > 0) {
          highlightValidWallCorners();
        }
      }
    }

    function highlightValidWallCorners() {
      // すべての交差点をチェック
      for (let r = 1; r < GRID_SIZE; r += 2) {
        for (let c = 1; c < GRID_SIZE; c += 2) {
          const wall = { 
            cornerRow: r, 
            cornerCol: c, 
            orientation: gameState.wallOrientation 
          };
          
          if (canPlaceWall(wall)) {
            const cell = document.querySelector(`.cell[data-row="${r}"][data-col="${c}"]`);
            if (cell) {
              cell.classList.add('highlight-corner');
            }
          }
        }
      }
    }

    function updateUI() {
      const player1Info = document.getElementById('player1-info');
      const player2Info = document.getElementById('player2-info');

      player1Info.classList.toggle('active', gameState.currentPlayer === 1);
      player2Info.classList.toggle('active', gameState.currentPlayer === 2);

      // 凍結状態の表示
      player1Info.classList.toggle('frozen-state', gameState.players[1].frozen);
      player2Info.classList.toggle('frozen-state', gameState.players[2].frozen);

      document.getElementById('walls1').textContent = gameState.players[1].walls;
      document.getElementById('walls2').textContent = gameState.players[2].walls;

      document.getElementById('walls1-display').textContent = '🧱'.repeat(gameState.players[1].walls);
      document.getElementById('walls2-display').textContent = '🧱'.repeat(gameState.players[2].walls);

      // カード使用状況をドット表示で更新
      const remaining1 = getRemainingCards(1);
      const remaining2 = getRemainingCards(2);
      const max1 = playerHands[1] ? playerHands[1].length : MAX_CARDS_PER_PLAYER;
      const max2 = playerHands[2] ? playerHands[2].length : MAX_CARDS_PER_PLAYER;
      const used1 = max1 - remaining1;
      const used2 = max2 - remaining2;

      const card1 = document.getElementById('card-status1');
      const card2 = document.getElementById('card-status2');

      // 手札がある場合は「1番 2番 3番」で表示（相手に中身を見せない）
      const handLabel = (pNum) => {
        const hand = playerHands[pNum];
        if (!hand) return '';
        const p = gameState.players[pNum];
        return '<div style="font-size:0.7em;margin-top:2px;color:#888;">' +
          hand.map((cardId, i) => {
            const isUsed = (p.cardsUsed[cardId] || 0) >= 1;
            return isUsed ? `<s>${i+1}番</s>` : `${i+1}番`;
          }).join(' ') + '</div>';
      };

      const frozenLabel = (p) => p.frozen ? ` <span class="frozen-badge">❄️ 足止め${p.frozenTurns > 1 ? `(${p.frozenTurns})` : ''}</span>` : '';
      const hiddenLabel = (p) => p.hidden ? ' <span class="frozen-badge" style="background:#2ecc71;">🃏 隠れ</span>' : '';
      card1.innerHTML = `🎴 カード: ${remaining1}枚` +
        frozenLabel(gameState.players[1]) + hiddenLabel(gameState.players[1]) +
        handLabel(1) +
        '<div class="card-usage-bar">' +
        Array.from({length: max1}, (_, i) =>
          `<span class="card-usage-dot${i < used1 ? ' used' : ''}"></span>`
        ).join('') + '</div>';
      card1.classList.toggle('used', remaining1 === 0);

      card2.innerHTML = `🎴 カード: ${remaining2}枚` +
        frozenLabel(gameState.players[2]) + hiddenLabel(gameState.players[2]) +
        handLabel(2) +
        '<div class="card-usage-bar">' +
        Array.from({length: max2}, (_, i) =>
          `<span class="card-usage-dot${i < used2 ? ' used' : ''}"></span>`
        ).join('') + '</div>';
      card2.classList.toggle('used', remaining2 === 0);

      // ターンインジケータ: 現在のプレイヤーを明確に表示
      const turnEl1 = document.getElementById('turn-indicator');
      const turnEl2 = document.getElementById('turn-indicator2');
      const turnCount = `<span class="turn-count">ターン ${gameState.turn}</span>`;

      const c1 = CHARACTERS.find(c => c.id === playerCharacters[1]);
      const c2 = CHARACTERS.find(c => c.id === playerCharacters[2]);
      const e1 = c1 ? c1.emoji : '🐈';
      const e2 = c2 ? c2.emoji : '🐈‍⬛';

      if (gameState.currentPlayer === 1) {
        turnEl1.innerHTML = `${e1} きみのターン！` + turnCount;
        turnEl2.innerHTML = `${e1} あいてのターン...` + turnCount;
      } else {
        turnEl1.innerHTML = `${e2} あいてのターン...` + turnCount;
        turnEl2.innerHTML = `${e2} きみのターン！` + turnCount;
      }
      // ターン切り替えアニメーション
      const activeTurnEl = gameState.currentPlayer === 1 ? turnEl1 : turnEl2;
      activeTurnEl.classList.remove('turn-switch');
      void activeTurnEl.offsetWidth; // reflow
      activeTurnEl.classList.add('turn-switch');

      const currentPlayer = gameState.players[gameState.currentPlayer];
      const wallsDisabled = currentPlayer.walls <= 0;
      const cardsDisabled = getRemainingCards(gameState.currentPlayer) === 0;

      // ちゃとら用コントロール
      document.getElementById('btn-wall-h').disabled = wallsDisabled;
      document.getElementById('btn-wall-v').disabled = wallsDisabled;
      document.getElementById('btn-card').disabled = cardsDisabled;

      // くろねこ用コントロール
      document.getElementById('btn-wall-h2').disabled = wallsDisabled;
      document.getElementById('btn-wall-v2').disabled = wallsDisabled;
      document.getElementById('btn-card2').disabled = cardsDisabled;

      // 対面モード：現在のプレイヤーのコントロールだけアクティブ
      document.getElementById('player1-controls').classList.toggle('inactive', gameState.currentPlayer !== 1);
      document.getElementById('player2-controls').classList.toggle('inactive', gameState.currentPlayer !== 2);
    }

    function getRemainingCards(playerNum) {
      const player = gameState.players[playerNum];
      const hand = playerHands[playerNum];
      const maxCards = hand ? hand.length : MAX_CARDS_PER_PLAYER;
      return maxCards - player.totalCardsUsed;
    }

    function resetGame() {
      // キャラの絵文字を設定
      const char1 = CHARACTERS.find(c => c.id === playerCharacters[1]);
      const char2 = CHARACTERS.find(c => c.id === playerCharacters[2]);

      gameState = {
        currentPlayer: 1,
        mode: 'move',
        turn: 1,
        players: {
          1: { row: 0, col: 3, walls: 6, emoji: char1 ? char1.emoji : '🐈', color: '#ffffff', cardsUsed: {}, totalCardsUsed: 0, frozen: false, frozenTurns: 0, hidden: false, sealedCards: {} },
          2: { row: 6, col: 3, walls: 6, emoji: char2 ? char2.emoji : '🐈‍⬛', color: '#333333', cardsUsed: {}, totalCardsUsed: 0, frozen: false, frozenTurns: 0, hidden: false, sealedCards: {} }
        },
        walls: [],
        gameOver: false,
        selectedCard: null,
        cardPhase: null,
        wallOrientation: 'h',
        previewWall: null,
        slideTargetWall: null,
        slideCardId: null,
        copyingCard: false,
        pendingMoveOk: false
      };

      // 履歴をクリア
      historyStack = [];

      document.getElementById('winner-overlay').classList.remove('show');
      document.getElementById('card-modal').classList.remove('show');
      document.getElementById('slide-modal').classList.remove('show');
      document.getElementById('copy-modal').classList.remove('show');
      setMode('move');
      initBoard();
      highlightValidMoves();
    }

    // Floating paw prints
    function initFloatingPaws() {
      const paws = ['🐾', '⭐', '✨', '🐱', '💫'];
      document.querySelectorAll('.floating-paws').forEach(container => {
        for (let i = 0; i < 12; i++) {
          const paw = document.createElement('div');
          paw.className = 'floating-paw';
          paw.textContent = paws[Math.floor(Math.random() * paws.length)];
          paw.style.left = Math.random() * 100 + '%';
          paw.style.fontSize = (1 + Math.random() * 1.5) + 'rem';
          paw.style.animationDuration = (8 + Math.random() * 12) + 's';
          paw.style.animationDelay = Math.random() * 10 + 's';
          container.appendChild(paw);
        }
      });
    }
    initFloatingPaws();

    // 初期化：モード選択画面を表示
    document.getElementById('game-container').classList.add('hidden');
    initBoard();

    function toggleInstructions() {
      document.getElementById('instructions').classList.toggle('open');
    }